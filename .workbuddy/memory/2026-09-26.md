# 2026-09-26

## 流程「轴」步骤运算列缺少移动命令（已修）
- 问题：流程页「运算」列对所有功能共用一份 OperationOptions（修改/修改为/加/减…），轴步骤看不到「移动」类命令；且旧「修改/修改为」在 ExecAxis 里被当成绝对移动，用户困惑「轴位置修改为0 为什么不移动」。
- 改动：
  - 新增 `Views/FunctionToOperationsConverter.cs`（IValueConverter）：功能=轴 → 运算下拉显示「绝对移动 / 相对移动 / 回零 / 停止」；其它功能 → 通用运算。
  - `Views/FlowPage.xaml` 运算列 ComboBox 的 ItemsSource 改为 `{Binding Function, Converter={StaticResource FunctionToOperationsConverter}}`，并注册转换器。
  - `ViewModels/FlowRunnerService.cs` 的 `ExecAxis` 重写：按运算词精确分发（绝对移动→MoveAxisAbs；相对移动→MoveAxisRel；回零/停止；"修改/修改为"作向后兼容仍走绝对移动）；属性=速度 时把设置值当速度下发（修掉旧版把属性名当数值解析、永远不调速的 bug）；回零/停止不再要求解析设置值。
- 说明：未加「设零」命令——IHardwareBridge 接口未声明 SetAxisZero（加密实现层会编译失败），如需清零命令后续再扩接口。
- 构建：PowerShell + 绝对 dotnet 构建到 C:\tmp\nocode_out\，EXIT=0，0 错误。
- 用户注意：流程运行走 FlowRunnerService（OperatorViewModel 的 RunAll），加密的 FlowViewModel 单步路径未改；bin 被运行中的 exe 锁定，需先关闭再正常构建或用临时产物验证。

## 流程「实际值」列改为显示真实内容并 1 秒刷新（已修）
- 问题：流程页「实际值」列只显示一个圆点，没有文字内容；旧 `RefreshActualValues()` 仅对 `Function=="变量"` 回填变量值，其它功能（轴/IO/气缸/相机）的 ActualValue 始终为空，导致表格看起来只有蓝点。
- 改动：`ViewModels/FlowViewModel.cs` 的 `RefreshActualValues()` 扩展为按「功能 + 属性」读取真实状态：
  - 变量 → 当前变量值（原逻辑保留）。
  - 轴·位置/编码器位置 → `AxisRuntimeState.Get` 当前位置（格式化 0.###）。
  - 轴·速度 → 轴配置速度；轴·使能 → 已使能/未使能。
  - IO·输出状态 → `SimRuntime.GetOutput`；IO·输入状态 → `HardwareBridge.Current.ReadInput`。
  - 气缸 → 伸出/缩回。
  - 相机 → 已连接/未连接。
  - 定时器仍为构造时启动的 1 秒 DispatcherTimer；单步读取异常只清空该步，不影响其它。
- 说明：圆点仍保留作为状态/当前行指示器；内容填充后，圆点左侧 + 文字右侧同时显示。若 AxisRuntimeState 没有该轴的位置（如实机运行未回写），位置显示为 0；后续如需把真实卡位置回写 AxisRuntimeState，应在桥接层统一处理。
- 构建：PowerShell + 绝对 dotnet 构建到 C:\tmp\nocode_out\，EXIT=0，0 错误（608 个既有警告）。

## 流程「运算」列改为随「功能 + 属性」联动，删除「修改」保留「修改为」（已修）
- 需求：运算列需根据「功能」和「属性」两列自动变化；删除「修改」，保留「修改为」。
- 改动：
  - `Views/FunctionToOperationsConverter.cs` 由 IValueConverter 改为 **IMultiValueConverter**（接收 Function + Property），按 (功能,属性) 返回运算项：
    轴+位置/编码器位置→绝对移动/相对移动/回零/停止；轴+速度/扭矩/电流/加速度→修改为/加/减/乘/除/取模；轴+已回零→比较；
    IO+输出状态→修改为/置位/复位，其它→比较；气缸+电磁阀→伸出/缩回/复位，其它→比较；modbus→修改为/置位/复位；
    变量+数值→修改为/加减乘除取模取反/比较，字符串→修改为/等于/大于/小于，布尔→修改为/取反/等于；点位/系统→修改为/等于。
    旧「修改」已从所有列表移除，统一「修改为」。
  - `Views/FlowPage.xaml` 运算列 ComboBox 改用 `<MultiBinding>`（Function + Property）绑定同一转换器（CellComboStyle 不设 ItemsSource，安全）。
  - `ViewModels/FlowRunnerService.cs`：ExecIo 加 op 参数（置位→1 / 复位→0 / 其余按设置值）；ExecCylinder 优先按 op（伸出/缩回/复位），退回兼容设置值；ExecAxis/ExecVar 去掉「修改」case（默认分支等值），保留「修改为」。
  - `ViewModels/FlowViewModel.cs`：变量赋值判断接受「修改为」或旧「修改」。
  - `Services/ProjectTemplateCatalog.cs`：内置模板 Operation 迁移（轴位置→绝对移动；气缸→电磁阀+伸出/缩回；IO/系统/相机/点位/modbus/速度→修改为；WaitIO 是否等于→等于），避免模板步骤在新下拉里空白。
  - `Services/AiProjectExchange.cs`：LegalOperations/GeneralOperations/DefaultOperation/MapAction 全改中文运算（去掉 修改 / MoveAxisAbs / HomeAxis / WriteOutput / CylinderMove / CommSend 等），与 UI 下拉一致，保证 AI 复制/粘贴往返不产生下拉里没有的值。
- 构建：obj 的 `NoCodeMotion_MarkupCompile.cache` 被 Visual Studio(devenv)+多个 MSBuild 节点锁住（MarkupCompilePass1 删除被拒 → MSB4018），且沙箱禁止 Stop-Process/删除；**破解办法：每次用全新的 `-p:IntermediateOutputPath=C:\tmp\nocode_intN\` + 全新 `-p:OutDir=C:\tmp\nocode_outN\`**（空目录无缓存可删即可过 CleanupCacheFiles），成功产出 NoCodeMotion.exe/dll，0 错误，仅既有 CA1416/CS86xx 警告。
- 注意：正式 bin 仍被运行中的 exe 锁定；验证前请关闭程序并在 VS 里 Ctrl+Alt+F7 重新生成。

## 功能列：IO 拆成「输入IO / 输出IO」（已修）
- 需求：功能列把原来的「IO」拆成「输入IO」和「输出IO」两项。
- 改动：
  - `Views/FlowPage.xaml` FunctionOptions：`IO` → `输入IO` + `输出IO`。
  - `Views/FunctionToNamesConverter.cs`：输入IO→`Catalog.InIoNames`、输出IO→`Catalog.OutIoNames`（保留 IO→IoNames 兜底）。
  - `Views/FunctionToPropertiesConverter.cs`：输入IO→{输入状态,脉冲状态,报警状态}；输出IO→{输出状态}。
  - `Views/FunctionToOperationsConverter.cs`：输入IO→比较运算；输出IO→修改为/置位/复位。
  - 执行分派补 case：`FlowRunnerService`、`SimFlowPlayer`、`FlowViewModel`（ExecuteHardwareStep + RefreshActualValues + 设备步骤判定）。
  - 模板：`ProjectTemplateCatalog` 的 WaitIO→输入IO、SetIO→输出IO；`FlowViewModel.AddTemplateSteps` **新增 `NormalizeTemplateStep`**，把内置 StepDef 的 API 动作名（MoveAxisAbs/WriteOutput/ReadInput/Wait/CylinderMove/CylinderReset/HomeAxis/WaitAxisStop/CommSend）归一为中文词，并把旧 `Function="IO"` 按属性拆成 输入IO/输出IO（否则新建工程的步骤在这些列会空白）。
  - `AiProjectExchange`：NormFunction/NormProperty/NormOperation、LegalProperties/LegalOperations/DefaultOperation/MapAction、对象名校验都按 输入IO/输出IO 处理（泛称 IO→输出IO；含「输入」→输入IO、含「输出」→输出IO）。
- 构建：全新 `IntermediateOutputPath`+`OutDir`，`-c Debug --no-restore`（上一次 restore 撞到被锁的 `obj\*.nuget.g.props` 报 Access denied），0 错误。

## 「实际值」列改为全功能覆盖（不留空行）（已修）
- 问题：流程表「实际值」列很多行只有圆点、没有文字。两个原因：①`RefreshActualValues` 开头 `if (string.IsNullOrWhiteSpace(step.Name)) continue;` 把「延时/循环/注释」等**无名称控制行**直接跳过；②只有 变量/轴/IO/气缸/相机 有分支，`点位/modbus/系统/延时` 以及 轴 的 已回零/扭矩/电流 等组合落到 default → 空。
- 改动：`ViewModels/FlowViewModel.cs` 重写为 `ReadActualValue(step, bridge)`，按「功能 + 属性」全覆盖、读不到显示「—」（不编造），并去掉「无名称就跳过」：
  - 变量→变量值；轴·位置/编码器位置→真实位置（桥读优先、回退 `AxisRuntimeState`）、速度→轴配置速度、加速度→`Accel`、使能→已使能/未使能、已回零/扭矩/电流→「—」。
  - 输入IO·输入状态/脉冲状态→读输入电平；输入IO·报警状态→「—」；输出IO·输出状态→`SimRuntime.GetOutput`。
  - 气缸→伸出/缩回；点位→目标点位名（无则第一个点位）；modbus→发送内容；相机→最近结果/匹配分数或连接状态。
  - 系统·延时/循环→设置值（无名称也能显示）、注释→文本、急停状态→急停/正常；延时→设置值；其它→设置值或「—」。
- 说明：设备类步骤若无「名称」显示「—」。刷新仍是构造时启动的 1 秒 `DispatcherTimer`。
- 构建：全新 IntermediateOutputPath+OutDir、`-c Debug --no-restore`，0 错误。

## 节点图属性面板：轴名称下拉 + 实际位置 1 秒刷新 + 运行时真动（已修）
- 需求：节点图「轴运动」节点的「轴」要下拉选择；属性面板要有「实际位置」并 1 秒刷新；运行时要真的动轴。
- 改动：
  - **轴名称下拉**：`ViewModels/NodeGraphNodeViewModel.cs` 的 `NgPropViewModel` 加 `IsAxisProp`(Name=="轴") / `IsPlainText`；`Views/NodeGraphPage.xaml` 属性行在文本框之外增加一个「轴」ComboBox（`ItemsSource="{x:Static svc:Catalog.AxisNames}"`，根加 `xmlns:svc`），文本框改用 `IsPlainText` 控制显隐。
  - **实际位置 1 秒刷新**：`NodeGraphNodeViewModel` 加 `ShowsActualPosition`(轴运动/回零/等待轴到位)、`ActualPositionText`、`RefreshActualPosition()`（`HardwareBridge.Current.ReadAxisPosition` 优先、回退 `AxisRuntimeState.Get`）；`NodeGraphViewModel` 加 1 秒 `DispatcherTimer` → `SelectedNode?.RefreshActualPosition()`，并加 null 安全的 `ShowActualPositionRow`，选中节点时立即刷一次；XAML 在属性列表下加「实际位置」只读行。
  - **运行时真动**：`NgRunner` 原来是**构造时捕获** `HardwareBridge.Current`（节点图 VM 启动即建、此时还没连卡 → 一直是 Stub → 运行时轴不动）。改为 `private IHardwareBridge _bridge => HardwareBridge.Current;`（执行时实时取当前桥），删掉构造参数；`NodeGraphViewModel` 的 `new NgRunner(...)` 同步去掉首参。
- 构建：全新 IntermediateOutputPath+OutDir、`-c Debug --no-restore`，0 错误。

## 「相对移动」在各执行 / 仿真路径的识别补齐（已修）
- 问题：流程「运算」下拉新增的中文词「相对移动」原先只在 `FlowRunnerService.ExecAxis` 被识别；`SimFlowPlayer`（3D 仿真 / 预览）只认 `"相对"/"rel"/"相对运动"`，`FlowViewModel.ExecuteHardwareStep`（单步路径）**根本不看「运算」** → 都按绝对移动处理，表现「相对移动不生效」。
- 改动：
  - `Services/SimFlowPlayer.cs`：`AxisAction` 的相对分支加 `"相对移动"`；`NodeToActions` 的节点「模式」判断改为 `relMode = 相对|rel|相对移动`。
  - `ViewModels/FlowViewModel.cs`：`ExecuteHardwareStep` 的轴分支读取 `step.Operation`，`回零 / 停止 / 相对移动(相对/rel)` 分别走 `HomeAxis / StopAxis / MoveAxisRel`，其余才 `MoveAxisAbs`。
  - `Services/NgRunner.cs`：节点「模式」判断放宽为 `相对|相对移动|rel`。
- 构建：全新 IntermediateOutputPath+OutDir、`-c Debug --no-restore`，EXIT=0、0 错误（602 警告）。
