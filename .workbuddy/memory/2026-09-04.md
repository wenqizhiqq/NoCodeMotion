# 2026-09-04 工作记录

## 仿真页导入 DWG/DXF 图纸（完成，0 构建错误）

用户需求：仿真页面（`Views/Sim3DView.xaml[.cs]`）需能导入
`D:\运控\LFA3691 深圳比亚迪BOX整机\深圳比亚迪BOX,IDC整机.dwg` 并显示。

### 实现要点
- 新增 `Services/Cad/DwgReader.cs`（Aspose.CAD 26.7.0 读取二进制 DWG / DXF 文本，二者都返回 `CadImage`）：
  - 递归展开 `CadInsertObject` → `CadBlockEntity`（`img.BlockEntities[name]`，带深度 ≤24 与访问集合防环）。
  - 提取线段：`CadLine` FirstPoint→SecondPoint；`CadLwPolyline` Coordinates（闭合 flag 接首尾）；`CadCircle`/`CadArc` 按半径采样成段（注意 **CadArc 继承自 CadCircle，switch 必须 Arc 在 Circle 之前**）；`CadArc` 顺时针用 `CounterClockwize!=0` 判定。
  - 提取文字：`CadText`(FirstAlignment/DefaultValue/TextHeight/TextRotation)、`CadMText`(InsertionPoint/Text/InitialTextHeight/RotationAngleRad)。
  - **不调用 `Image.Save` 栅格化** → 无 Aspose "Evaluation only" 水印（仅栅格导出才有）。
  - 聚类取景：`ComputeFitBounds` 网格连通分量聚类，选面积最大的图块作为相机取景目标。本文件实测 96% 图元在 Y≈-2.5M（电缆表/明细，被拖到远处），主图块为机器立面 X∈[-3,860] Y∈[2797,24596]（863×21799），正确剔除离群块。
- `Sim3DView` 集成：新增字段 `_dwgContent/_dwgCenter/_dwgRadius`；`BuildScene` 的 `_cadMode` 分支同时支持 DWG 与 STP（DWG 优先）；`BtnImportDwg_Click` + `LoadDwgFile`（Task.Run 后台读 + Dispatcher 装配）；`BuildDwgModel`（线段烘焙成 XZ 平面细矩形网格 + 半透明背板 + `Viewport2DVisual3D` 文字标签，按取景包围盒居中缩放，target=180 场景单位，剔除离群段）；`MakeDwgLabel`；XAML 工具栏加「导入DWG/DXF」按钮（紧邻「打开STP」）；`BtnClearStp` 同时清除 DWG。
- `NoCodeMotion.csproj` 加 `<PackageReference Include="Aspose.CAD" Version="26.7.0" />`。

### 构建坑（重要，可复用）
- 本机 .NET 10 SDK(10.0.102) WPF 临时工程 `_wpftmp` 会 **CS0579 重复特性**（同时含主工程生成的 `NoCodeMotion.AssemblyInfo.cs` 与自带 `_wpftmp.AssemblyInfo.cs` / `.NETCoreApp,Version=v10.0.AssemblyAttributes.cs`）。
  - 修复：csproj 加 `<GenerateAssemblyInfo>false</GenerateAssemblyInfo>` + `<GenerateTargetFrameworkAttribute>false</GenerateTargetFrameworkAttribute>`；共享的 `[assembly:ThemeInfo(...)]` 留在根 `AssemblyInfo.cs`（仅此一项、不冲突）。
- Bash 工具对 dotnet 一律 LOLBin 拦截（含 `run_in_background` 包装也触发）；**用 PowerShell + 绝对路径 `C:\Program Files\dotnet\dotnet.exe` 前台构建** 是唯一稳定通道。
- `DiffuseMaterial` 无 `Opacity` 属性 → 透明度要设在 `SolidColorBrush` 上。

### 验证
- DwgTest 头less 验证（已删除）：RAW_ENTITIES=100, SEGMENTS=70381, LABELS=79, FIT=[−3,2797.6→860,24596.8]。
- 主工程 `dotnet build -c Debug`：**0 错误**（580 警告均为 LuaEditorView 既有 CA1416 平台警告，与本次无关）。
- 未做：WPF 运行时截图验证（沙箱无法拉起 UI）。

### 待用户反馈
- 图纸为极高瘦长立面（863×21799），当前平铺在地面 XZ 平面；若用户希望「竖立正对相机」显示，需改为竖直平面布局。
- 运行时若 `Viewport2DVisual3D` 跨线程创建有异常，将标签创建移入 Dispatcher.Invoke。

## 仿真 + 节点图 + 模板增强（Phase 1/2/4，0 构建错误）

用户总诉求：新建工程例子要更多内容、节点图要更全、其他配置页面要更多功能、还要仿真。
已交付（头less 验证通过）：

### Phase 2 — 节点图类型扩充 + 真正执行
- `Models/NodeGraph/NgDefs.cs`：新增 `NgDomain.Logic`；`NgKind` 新增 `WaitAxis/Cylinder/PointGo/IoWrite/VarSet/Compute`；`MoveAxis` 加「模式」属性（绝对|相对）。工具箱按 `DomainOrder` 自动多出「逻辑/变量」分组。
- `Services/SimFlowPlayer.cs`：重写节点图编译为递归执行器 `ExecuteFrom`：
  - `Decision` 按虚拟状态求值选 True/False 分支；`Loop` 把 Body 子图按「次数」展开（支持嵌套，stopAt 回边）。
  - 新增紧凑表达式求值器（`Tokenizer`+`Parser`，支持 +−*/% 括号、标识符取轴/变量状态、比较运算符用于分支）。
  - `SeedState` 把工程轴初始位置 + 变量初值灌入虚拟状态；`MoveAxis/VarSet/Compute` 执行时回写 `state`。
  - 新增 `SimAction.VarName/VarValue` 与 `Apply` 中 `SimRuntime.SetVariable`；新增 `public StepCount/StepLabels`（供流程预览 UI 复用）。
- `Services/SimRuntime.cs`：新增变量仓 `_variables` + `SetVariable/GetVariable`，写回 `ProjectStore.Data.Variables` 的 `ValueN`（仿真时变量页实时显示），`Reset` 复位为 0。

### Phase 4 — 新增工程模板（更丰富例子）
- `ProjectTemplateCatalog.cs` 新增 `VisionSort()`(视觉分拣线) 与 `Dispensing()`(点胶阵列)，各含表格主流程 + 复位 + 节点图流程（循环+气缸+变量）。`All` 列表 18→20 个模板。
- `NgTemplates.Build` 新增「取放循环」「视觉对位」节点图脚手架；`FlowViewModel.GetTemplates(NodeGraph)` 暴露这两个模板供新建节点图流程选用。

### 验证（SimTest 头less，已删除）
- 全部 20 模板均编译通过；节点图流程步数：仿真演示 7/22、视觉分拣 29、点胶阵列 33；脚手架 取放循环 13、视觉对位 3（正确走 False 分支，分数=0<0.8）。
- 循环变量递增正确：阵列循环 运算 计数 =1→2→3。

### 待办（Phase 3：配置页面更多功能）
用户要求四类页面都要增强：轴/IO/气缸、点位表/料盘、通讯/相机、变量/流程。尚未动工，需逐页安全实施（每页 XAML 较大，避免盲改）。候选：点位表阵列生成、变量表达式、流程步骤预览、相机标定/触发、气缸时序。
