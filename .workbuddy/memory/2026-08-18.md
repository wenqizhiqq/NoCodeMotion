# 2026-08-18

## UI 全局美化 + 图标体系
用户要求“全部颜色都美化，尽量都加上图标”。在 NoCodeMotion（WPF/.NET10/HandyControl）实施了一套统一主题：

- **`App.xaml` 集中改造**：
  - 调色板：引入 Color + SolidColorBrush（Accent / AccentSoft / Canvas / Surface / Line / Text* / Success / Danger / Warning），语义化命名，全站引用。
  - 柔和阴影：`SoftShadow` / `SoftShadowSm`（DropShadowEffect）。
  - **矢量图标集**：`Geometry` 资源（24x24 Material 风格路径）—— 导航 7 个（Axis/Io/Cylinder/Comm/Tray/Flow/Variable），工具栏 9 个（Excel/Add/Delete/Up/Down/Copy/Paste/Undo/Redo），分组标题 1 个（SectionIcon）。
  - 隐式样式：`Style TargetType="DataGrid"`（白底卡片 + 圆角边框 + 阴影 + 统一表头 `GridHeaderStyle`）、`Style TargetType="ListBoxItem"`（悬停/选中高亮，选中=Accent 底白字）。
  - 按钮样式统一圆角 8、悬停/禁用色，Tt* 工具栏按钮红/绿/蓝语义色。
- **导航栏 `MainWindow.xaml` + `.cs`**：7 个按钮改为「图标 + 文字」StackPanel，`Path.Fill` 绑定 `AncestorType=Button` 的 Foreground；`Navigate()` 增加 Foreground 切换（选中=白字，未选=深色），图标随状态变色；内容区背景改为 Canvas 画布。
- **`TableToolbar.xaml`**：9 个按钮全部加图标（Excel/增/删/上移/下移/复制/粘贴/回撤/重做）。
- **`EditorPage.xaml`**：添加/删除按钮加图标，计数文字改用 TextSecondaryBrush。
- **分组/分区标题加图标**：AxisPage(5) / TrayPage(2) / IoPage(2 输入/输出IO) / CylinderPage / CommPage / FlowPage 的分区标题统一改为 `SectionHeaderStyle`（SectionIcon + 加粗 Accent 文字）；原 `GroupHeaderStyle`/`IoSectionTitleStyle` 已不再使用（保留无害）。
- **表格细节**：IoHeaderStyle / VarHeaderStyle 改为 AccentSoft 底 + Accent 2px 下边框；IoRowStyle / VarRowStyle 加悬停高亮；IoPage / VariablePage 背景改 Transparent 以露出 Canvas 画布，DataGrid 成白色卡片。
- 构建：0 警告 0 错误。

**经验**：WPF 轻量图标方案 = 在 App.xaml 定义 `<Geometry x:Key>` 资源，按钮/标题里用 `<Path Data="{StaticResource ...}" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button},Path=Foreground}"/>`，图标即可随按钮前景色（含选中态）自动变色，无需图标字体。隐式 `TargetType` 样式可全局统一 DataGrid/ListBox 外观，但其中若 `StaticResource` 引用了定义在更后面的键，需改用 `{DynamicResource}` 避免前向引用报错。

## 流程表格新增「实际值」列
- 用户要求：耗时(ms) 列后加一列「实际值」，即属性列对应的实际值。
- **`Models/FlowStep.cs`** 新增 `ActualValue`（string，默认空），含 SetField 通知。
- **`Views/FlowPage.xaml`** 耗时(ms) 后插入 DataGridTemplateColumn「实际值」（可编辑 TextBox，绑定 ActualValue，UpdateSourceTrigger=PropertyChanged）。
- **`Services/ExcelBatchEdit.cs`** 的 FlowStep 表头映射追加 ["ActualValue"]="实际值"，使 Excel 批量编辑列与表格一致。
- 构建 0 警告 0 错误。

## FlowPage 流程表格美化
- 用户截图反馈流程表格外观偏"白板"：单元格无网格线、ComboBox/TextBox 渲染拥挤、整张表无卡片边框/阴影、表头无强调色。
- `Views/FlowPage.xaml` 加入与 IoPage 对齐的本地样式：FlowCellStyle（内边距 + 1px 网格线 + 无焦点虚框）、FlowRowStyle（白底/悬停&选中=AccentSoft）、FlowHeaderStyle（AccentSoft 底 + 强调色 2px 下边框）、FlowCellTextBoxStyle/FlowCellComboStyle（24px 紧凑、透明无边框）。
- 整张表用 Border 包成白色卡片：圆角 8 + LineBrush 边框 + SoftShadow 阴影；DataGrid 自身透明无边框；启用 AlternationCount=2 给极浅斑马（#FAFBFC）。
- 列宽微调：超时 120→130（原本拥挤），实际值 110→120。
- 「实际值」列加视觉强调：表头背景 #DDE9F7 + 强调深色字，单元格内用 Border(#F2F7FE 圆角 4) 包 Grid，左侧放 6×6 强调色圆点指示"采集结果"，右侧 SemiBold 强调深色字 TextBox。
- 构建 0 警告 0 错误，App 启动无异常。

## 单元格 ComboBox 彻底扁平化（修复"还是很丑"）
- 根因：`hc:ComboBox`(HandyControl) 自带模板含边框+默认箭头，之前用 `BorderThickness=0`/`Background=Transparent` 的 setter 无法真正去掉其内部边框/箭头，所以仍显"丑"。
- `App.xaml` 新增共享 `CellComboStyle`（TargetType=ComboBox）：**完全自定义 ControlTemplate**——平时透明、悬停/展开时 AccentSoft 浅填充、右侧干净 chevron（Path）、下拉面板为白色圆角卡片(6)+LineBrush 边框+SoftShadow 阴影+高亮项(ComboBoxItem 模板)。关键命名部件：`PART_Popup`(Popup)、`PART_ContentSite`(ContentPresenter)，保证下拉可开、选中项可显示。
- `Views/FlowPage.xaml`：6 处 `hc:ComboBox` 改为 `<ComboBox Style="{StaticResource CellComboStyle}">`；删除本地 `FlowCellComboStyle`；`FlowCellTextBoxStyle` 增加悬停/聚焦浅强调背景(Style.Triggers)。
- `Views/IoPage.xaml`：4 处同步改为 `CellComboStyle`，删除本地 `IoCellComboStyle`（保持 Io/Flow 两页一致）。
- 构建 0 警告 0 错误，App 启动无异常。

## 全局 Apple 风美化（整站）
- 用户投诉：①点击单元格会整行选中不好看；②整体颜色不好看，要求用 Apple 样式颜色与布局做整站美化。
- `App.xaml` 调色板改为 Apple 风：画布 #F5F5F7、卡片 #FFFFFF、文字 #1D1D1F/#6E6E73/#8E8E93、发丝线 #E5E5EA、系统蓝 #007AFF、选中浅蓝 #EAF3FF、悬停灰 #F2F2F7、表头灰 #F7F7F9、绿/红/橙 Apple 原色；新增 HoverBrush/HeaderBrush；阴影改中性黑更轻(Blur 16/8, Depth 0, Op 0.08/0.06)；新增共享 `CardBorderStyle`（圆角10+发丝线+SoftShadow）。
- 隐式 `DataGrid` 改透明无边框无阴影（卡片由外层 Border 负责）；`GridHeaderStyle` 改浅灰底+发丝下边框+Medium 字；`ListBoxItem` 选中改浅蓝底+蓝字(非实心白字)、悬停改灰；`CellComboStyle` 悬停改灰；`SectionHeaderText` 改近黑 SemiBold；`NavButtonStyle` 悬停改灰。
- `Views/FlowPage.xaml` `Views/IoPage.xaml` `Views/VariablePage.xaml`：表格用 `CardBorderStyle` 白卡包裹；行样式移除整行选中高亮、悬停改灰；单元格样式加 IsSelected 浅蓝高亮 + IsMouseOver 灰，网格线用 LineBrush；DataGrid 改 `SelectionUnit="Cell"`（不再整行选中）；去掉斑马纹。FlowPage 实际值列保留浅蓝强调。
- `MainWindow.xaml` 根 Window 设 FontFamily=Segoe UI 字体栈(贴近 Apple 观感)；`MainWindow.xaml.cs` Navigate() 选中态改 浅蓝底(NavActiveBrush=AccentSoft)+蓝字(AccentBrush)，不再实心白字。
- 构建 0 警告 0 错误，App 启动无异常。

## 新增「点位表」页（轴的点位，供流程引用）
- 用户要求：在气缸后面加一个「点位表」页面，定义为轴的点位（命名位置，含各轴目标坐标），方便流程使用。
- **数据模型**：
  - `Models/PointAxis.cs`：单个轴坐标（AxisName + Position(double)，实现 INotifyPropertyChanged）。
  - `Models/PointItem.cs`：`: EditorItemBase`，`ObservableCollection<PointAxis> Positions`（get-only，JSON 反序列化填充已有集合）；构造里订阅 `Positions.CollectionChanged`，并对每个 PointAxis 订阅 `PropertyChanged` → 冒泡为 PointItem 的 PropertyChanged，使列表 VM 的自动保存能捕获坐标编辑。
- **接入全局**：`Services/Catalog.cs` 加 `PointNames` + `SetPoint` + `RebuildAll`/`SyncAllFromData` 纳入；`Models/ProjectData.cs` 加 `Points`；`ViewModels/ListEditorViewModel.cs` 的 `SyncCatalog` switch 加 `"Point"` 分支（CatalogCategory="Point" 即把点位名汇入 Catalog）。
- **页面**：`ViewModels/PointViewModel.cs`（`: ListEditorViewModel<PointItem>, IEnsureDefaultSelection`；`CreateNewItem` 按当前 `Catalog.AxisNames` 给每个轴初始化一行坐标）；`Views/PointPage.xaml`（`EditorPage` + 右详情：点位名称 + `ItemsControl` 绑定 `SelectedItem.Positions` 渲染各轴坐标 TextBox）+ `PointPage.xaml.cs`（`DataContext=new PointViewModel()`）。
- **导航/图标**：`App.xaml` 加 `PointIcon`（靶心几何）；`MainWindow.xaml` 气缸按钮后插 `点位表` 按钮(Tag="Point")；`MainWindow.xaml.cs` `_pages` 加 `["Point"]=()=>new PointPage()`。
- **流程引用**：`Views/FlowPage.xaml` 的 `FunctionOptions` 加 `点位`；`FunctionToNamesConverter` 加 `"点位"=>Catalog.PointNames`（名称列即可选点位名）；`FunctionToPropertiesConverter` 加 `["点位"]=速度/加速度/到位误差/是否等待到位`。
- **注意**：点位坐标在新建时按当时已配置轴生成；之后若增删轴，旧点位不会自动增删轴（重新建点位即可）。`SelectedItem` 为空时详情的 `SelectedItem.Positions` 绑定为 null，ItemsControl 仅空，不报错。
- 构建 0 警告 0 错误；启动冒烟测试进程存活无 stderr（App/ProjectStore.Load/Catalog.SyncAllFromData 含空 Points 均正常）。

## 套路沉淀：本工程加一个新「列表页」的步骤
1. `Models/XxxItem.cs`（`: EditorItemBase`，加字段 + SetField 通知）；若含子集合需冒泡 PropertyChanged 触发自动保存。
2. `Models/ProjectData.cs` 加 `ObservableCollection<XxxItem>`；`Services/Catalog.cs` 加 `XxxNames`+`SetXxx`+`RebuildAll`+`SyncAllFromData`。
3. `ViewModels/ListEditorViewModel.cs` 的 `SyncCatalog` switch 加 `case "Xxx"`。
4. `ViewModels/XxxViewModel.cs`（`: ListEditorViewModel<XxxItem>, IEnsureDefaultSelection`，设 CatalogCategory、Items=ProjectStore.Data.Xxxs、Counter、AttachAutoSave、CreateNewItem、EnsureDefaultSelection）。
5. `Views/XxxPage.xaml`（`EditorPage` + Detail）+ `XxxPage.xaml.cs`（`DataContext=new XxxViewModel()`）。
6. `App.xaml` 加图标 `XxxIcon`；`MainWindow.xaml` 插导航按钮；`MainWindow.xaml.cs` `_pages` 加项。
（.cs 文件 Read 工具打不开，用 Python 读写；新 .cs 用 Write，xaml 用 Edit/Write。）

## 点位表页重构：轴控制面板 + 9 列表格
- 用户要求：点位表页可选择 4 个轴名，表格列 = 点位名称 + 4 轴位置 + 4 轴速度；表格上方加轴的寸动/JOG/使能/回原控制。
- **模型**：`PointAxis` 改为只含 `Position`+`Speed`（去掉 AxisName，轴名改由页面级 4 槽决定）；`PointItem.Positions` 固定 4 槽（新建/反序列化都 4 项），子项 Position/Speed 变更冒泡触发自动保存（保留 `Positions` 命名以兼容旧 JSON，旧数据的 AxisName 字段被忽略、Speed 默认 0）。
- **页级配置**：`Models/ProjectData.cs` 加 `PointAxes`（`ObservableCollection<string>` 4 槽，持久化轴名）；`ViewModels/PointViewModel.cs` 暴露 `AxisStates`(4 个 `AxisState`：Index/AxisName/Enabled/Homed/CurrentPosition)、`InchStep`/`JogStep`；轴名变更写回 `ProjectData.PointAxes` 并 `ScheduleSave`。
- **轴控制命令**（运行态仿真，工程内无运动硬件层，命令在此更新运行态，后续接运动引擎）：`EnableCommand`(toggle 使能)、`HomeCommand`(当前位置归零+已回原)、`InchCommand`/`JogCommand`(参数 `"槽位,方向"`，按寸动/Jog 步长改当前位置)。`PointItem` 仍走 `ListEditorViewModel` 的 `Add/Delete`+Catalog(Point)+自动保存+`EnsureDefaultSelection`。
- **`Views/PointPage.xaml` 重写**：顶部 `ItemsControl`(WrapPanel) 渲染 4 张轴卡片（ComboBox 选轴 + 当前位置 + 使能/回原/寸动±/JOG± 按钮 + 状态文字），下方 `DataGrid`（CardBorderStyle 卡片）列：点位名称 + 轴0..3 的「位置」「速度」（表头绑定 `AxisStates[i].AxisName` 显示轴名，单元格 `Positions[i].Position/Speed` 内嵌 TextBox 常驻可编辑，`SelectionUnit=CellOrRowHeader`/`IsReadOnly`），上方 添加/删除 工具栏 + 寸动/Jog 步长输入。样式沿用 PointCellStyle/PointRowStyle/PointHeaderStyle/PointCellTextBoxStyle（同 Flow/Io 苹果风）。
- 构建 0 警告 0 错误；启动冒烟测试进程存活无异常（App/ProjectStore.Load/Catalog.SyncAllFromData 含空 Points、PointViewModel 构造建 4 轴槽均正常）。
- **坑**：`PointViewModel` 用 `ICommand` 需 `using System.Windows.Input;`（漏了会 CS0246）。表头要用 `RelativeSource={RelativeSource AncestorType=DataGrid}` 取 `DataContext.AxisStates[i].AxisName`；命令参数带方向用 `CommandParameter="{Binding Index, StringFormat='{}{0},+'}"` 合成为字符串再解析。

## 点位表页改版：布局/中文说明/列头修复
- 用户截图反馈：①布局不好看，缺中文说明；②列表列名空白（8 个轴列头不显示）。
- **根因（列头空白）**：`DataGridTemplateColumn.Header="{Binding ... RelativeSource=...}"` 不渲染——`Header` 属性所在的列对象不在可视化树内，`RelativeSource AncestorType=DataGrid` 找不到祖先而静默失败。必须在 `HeaderTemplate`（在列头可视化树内）里绑定。
- **新增 `Views/PointConverters.cs`**（namespace `NoCodeMotion.Views`）：`AxisHeaderMultiConverter`(IMultiValueConverter：轴名+后缀"位置/速度"，空轴名回退"未选轴")、`BoolToZhConverter`(bool→是/否)。UTF-8 no-BOM 写入。
- **`Views/PointPage.xaml` 重写**：
  - 结构改 `ScrollViewer > StackPanel`：顶部页面标题「点位表」+ 中文说明段；「轴控制」段标题+说明+4 卡片（WrapPanel）+ 寸动/JOG 步长；「点位列表」段标题+说明+工具栏+卡片表格。
  - 卡片改紧凑布局：标题=图标+轴名（MultiBinding+空轴名回退"未选轴"）；「选择轴」ComboBox(Catalog.AxisNames)、当前位置、状态行（使能/回原 用 BoolToZh 显示"是/否"）、使能/回原 用 `UniformGrid` 两列、寸动/JOG 各一组 ± 按钮（含中文小字说明）。
  - 列头改用 `HeaderTemplate`（8 个模板 H0Pos…H3Spd），内用 MultiBinding：`RelativeSource AncestorType=DataGrid` 取 `DataContext.AxisStates[i].AxisName` + `Binding Source="位置/速度"`，经 `AxisHeaderMulti` 拼成「轴名 位置/速度」；空轴名显示「未选轴 位置」。每个列头 `TextBlock` 带中文 `ToolTip` 说明含义。点位名称列保留静态中文表头。
- 构建 0 警告 0 错误；启动冒烟测试进程存活 12s 无 stderr（无 XAML/转换器解析异常）。
- **套路**：WPF DataGrid 动态列头必须用 `HeaderTemplate` + `RelativeSource AncestorType=DataGrid` 取 VM 集合；`Header` 直绑 RelativeSource 无效。

## 点位表点击寸动/JOG 抛 InvalidCastException（修复）
- 现象：VS 调试器在 `PointViewModel.Move` 第 96 行 `var parts = ((string)p!).Split(',');` 抛 `System.InvalidCastException: Unable to cast object of type 'System.Int32' to type 'System.String'.`
- **根因**：WPF `Binding.StringFormat` 只在绑定目标为 `string` 类型时才生效。`CommandParameter` 属性类型是 `object`，所以 `{Binding Index, StringFormat='{}{0},-'}` 的 `StringFormat` 被静默忽略，传递的仍是原始 `int`；`Move` 期望 `"槽,方向"` 字符串，强制 `(string)p!` 失败。
- **修复**：
  - `Views/PointConverters.cs` 新增 `StringFormatConverter`（通用：`string.Format(culture, parameter, value)`，把 int 按 ConverterParameter 格式化为字符串）。
  - `Views/PointPage.xaml`：UserControl.Resources 注册 `<local:StringFormatConverter x:Key="StringFormat"/>`；4 个寸动/JOG 按钮的 `CommandParameter` 改为 `{Binding Index, Converter={StaticResource StringFormat}, ConverterParameter='{}{0},-'/+}`，转换器输出字符串 → `Move` 的 `(string)p!.Split(',')` 正确解析 → 按"槽位,方向"驱动 `AxisStates[i].CurrentPosition += dir * (jog?JogStep:InchStep)`。
  - 使能/回原按钮的 `CommandParameter="{Binding Index}"`（int）保持不变——`ToggleEnable`/`Home` 内 `(int)p!` 仍正确。
- 构建 0 警告 0 错误。
- **套路**：WPF 给 `object` 类型的属性（如 `CommandParameter`、`Tag`）传字符串，必须用 `Converter`（或代码生成 string 属性），不能依赖 `StringFormat`——`StringFormat` 在非 string 目标上被静默吞掉，排查时易误以为格式串写错。

## 寸动/JOG 抛 FormatException + 方向按钮 Apple 配色
- **二次异常**：上一轮把 `CommandParameter` 改成 `"0,+"` 字符串后，点击不再抛 InvalidCastException，但 `Move` 内 `int.Parse(parts[1])` 拿到字面 `"+"` / `"-"`，抛 `FormatException: The input string '+' was not in a correct format.`。
- **根因**：`StringFormat("{0},+", idx)` 产出 `"0,+"`（`parts[1]="+"`），而 `Move` 期望 `+1`/`-1`（让 `dir * step` 直接得正/负位移）。
- **修复**：`Views/PointPage.xaml` 的 4 处 `ConverterParameter` 由 `{}{0},-` / `{}{0},+` 改为 `{}{0},-1` / `{}{0},+1`；`string.Format("{0},+1", 0)="0,+1"` → `int.Parse("+1")=1` → `AxisStates[i].CurrentPosition += 1 * step` 正确。
- **Apple 方向配色**（响应「使用苹果样式颜色」）：`App.xaml` 新增 `TtWarnBtn`（基于 `TtBaseBtn`，背景 = `WarningBrush` = `#FF9500` Apple systemOrange，悬停 `#E08600`）。`PointPage.xaml` 两个 `− 减` 按钮（寸动−/JOG−）改用 `TtWarnBtn`；`+ 加` 保持 `TtBaseBtn`（Apple systemBlue `#007AFF`）。形成「蓝前进 / 橙回退」的 Apple HIG 步进器配色，方向区分清晰、纯 Apple 原色。
- 构建 0 警告 0 错误；冒烟测试进程存活 8s 无 stderr。
- **套路**：用 StringFormat 转换器生成带方向符的 CommandParameter 时，格式串里的方向必须写成可被 `int.Parse` 接受的整数字面（`+1`/`-1`），不能写裸符号（`+`/`-`）——`int.Parse("+")` 失败。

## 点位表列顺序调整：速度列移到最后
- 用户要求：表格的 4 个速度列放到最后。
- `Views/PointPage.xaml` 的 DataGrid 列重排为：点位名称 → 轴0..3 位置 → 轴0..3 速度（原"位置/速度"交替改为"4 位置在前、4 速度在后"）。单元格绑定 `Positions[i].Position/Speed` 随列保持不变；列头模板 H0Pos…H3Spd 引用不变。说明文案改为"其后依次为 4 个轴的目标位置，最后为 4 个轴的目标速度"。
- 构建 0 警告 0 错误。

## 点位表新增「移动 / 保存」行操作列
- 用户要求：表格最后加两列按钮列——「移动」（弹窗确认是否移动）与「保存」（弹窗确认保存当前位置到此行单元）。
- `ViewModels/PointViewModel.cs`：新增 `MoveToPointCommand`/`SaveCurrentCommand`（RelayCommand，构造注册）；`using System.Windows;`（MessageBox）。方法实现：
  - `MoveToPoint(PointItem)`：`MessageBox.Show(...,"移动确认",YesNo,Question)`，确认后 `AxisStates[i].CurrentPosition = item.Positions[i].Position`（把 4 轴仿真移动到该点位目标位置）。
  - `SaveCurrent(PointItem)`：`MessageBox.Show(...,"保存确认",YesNo,Question)`，确认后 `item.Positions[i].Position = AxisStates[i].CurrentPosition`（当前位置存回该行，触发 PointItem 冒泡→自动保存落盘）。
- `Views/PointPage.xaml`：DataGrid 列末尾加两列 `DataGridTemplateColumn`「移动」「保存」，单元格放 `Button`，`Command` 经 `RelativeSource AncestorType=DataGrid` 取 VM 命令、`CommandParameter="{Binding}"` 传整行 PointItem；按钮用 `TtBaseBtn`（蓝）作移动、`TtExcelBtn`（绿）作保存（Apple 语义色）。说明文案补充两列作用。
- 构建 0 警告 0 错误；冒烟测试进程存活 8s 无 stderr。
- **套路**：DataGrid 行内按钮调用 VM 命令，靠 `RelativeSource={RelativeSource AncestorType=DataGrid}` 取 `DataContext.<命令>` + `CommandParameter="{Binding}"` 传行对象；弹窗确认用 `System.Windows.MessageBox`（YesNo）即可，纯 WPF 桌面应用 pragmatic 可接受。

## 确认弹窗改为 Apple 风格自定义窗口
- 用户截图反馈：`System.Windows.MessageBox` 系统原生气泡丑（蓝色问号图标 + 标准 Windows chrome + "是(Y) 否(N)"），与全站 Apple 主题不一致。
- **新增 `Views/ConfirmDialog.xaml` + `.cs`**：自定义 `Window`（`WindowStyle=None` + `AllowsTransparency=True` + `Background=Transparent` + `SizeToContent=WidthAndHeight` + `WindowStartupLocation=CenterOwner`）。内容：圆角 14 白卡 Border（`CardBorderStyle` 影子 + `LineBrush` 1px 发丝边，Margin 16 给 SoftShadow 留位），上部分隔线 1px 标题区（SemiBold 16 TextPrimary），中间消息 TextBlock（14 像素 TextPrimary TextWrapping LineHeight 22），下右对齐按钮行「取消」（`TtGrayBtn` 灰底 #F2F2F7 悬停 #E5E5EA）+「移动/保存」（`TtBaseBtn` 蓝，`IsDefault=True` 回车确认，`IsCancel=True` Esc 取消）。`ConfirmDialog(title, message, confirmText)`，ShowDialog() 返 true/false。
- **`App.xaml` 新增 `TtGrayBtn`**：独立模板（不复用 TtBaseBtn 的蓝悬停触发），圆角 8、灰底、悬停加深，禁用 #AEAEB2。
- **`ViewModels/PointViewModel.cs`**：`using System.Windows;` → `using NoCodeMotion.Views;`；`MoveToPoint`/`SaveCurrent` 内 `MessageBox.Show(..., YesNo, Question)` 改为 `new ConfirmDialog("移动/保存确认", 消息, "移动/保存").ShowDialog() == true`。
- 构建 0 警告 0 错误；冒烟测试进程存活 8s 无 stderr。
- **套路**：在 Apple 主题 WPF 应用中替代系统 MessageBox：写一个 `WindowStyle=None` + `AllowsTransparency=True` 的自定义 Window，内容用 `CardBorderStyle` 圆角白卡 + SoftShadow 阴影，按钮用应用统一按钮样式（主操作蓝/次操作灰/危险红），`Owner = Application.Current.MainWindow` 让 `CenterOwner` 居中。简单确认场景无需完整 MVVM DialogService，直接 VM `new + ShowDialog` 即可（桌面应用 pragmatic）。

## 点位表支持多工位（左侧列表新增/删除/切换）
- 用户纠正：早前误判为「轴新增删除」，实际诉求是「点位表（工位）需要多个，可以新增删除，左侧有列表」。轴固定 4 槽位不变，新增/删除/切换的实体是「点位表 = 工位」。
- **新增 `Models/PointTable.cs`**：工位容器，继承 `EditorItemBase`，含 `Name` + `AxisNames`（4 槽位 `ObservableCollection<string>`）+ `Points`（`ObservableCollection<PointItem>`）。构造里 `EnsureAxisSlots()` + `Attach(_points)` 订阅子项 `PropertyChanged` 冒泡，保证子集合改动触发自动保存；`Points`/`AxisNames` setter 在反序列化替换集合后重新挂 handler + `EnsureSlots`。
- **`Models/PointItem.cs`**：`Positions` 加可写 setter（反序列化重挂 handler）+ `EnsureSlots()` 补 4 槽位，顺带修旧数据 `Positions` 为空导致单元格空白/不可编辑的隐患。
- **`Models/ProjectData.cs`**：新增 `PointTables`；旧 `Points`/`PointAxes` 保留为 legacy 字段；`EnsurePointTables()` 做迁移（legacy → 工位1，随后 Clear 避免重复迁移）+ 全表槽位补全。
- **`Services/ProjectStore.cs`**：`Deserialize` 后调用 `Data.EnsurePointTables()` + `Catalog.SyncAllFromData(Data)`（catch 之外）。
- **`Services/Catalog.cs`**：`SyncAllFromData` 的 `SetPoint` 改为聚合 `PointTables.SelectMany(t=>t.Points).Select(p=>p.Name)`（跨工位点名）。
- **`ViewModels/ListEditorViewModel.cs`**：`SyncCatalog()` 由 `private` 改 `protected virtual`，供 `PointViewModel` 重写聚合范围。
- **`ViewModels/PointViewModel.cs`** 全量重写为 `ListEditorViewModel<PointTable>, IEnsureDefaultSelection`：左列表管工位（`Items=ProjectStore.Data.PointTables`，订阅 `CollectionChanged` + 各表 `PropertyChanged`）；右面板管当前工位的轴 + 点位。`AddPointCommand`/`DeletePointCommand` 操作 `SelectedItem.Points`；`CreateNewItem()` 建 `新工位N`（带 1 默认点位）；`Delete()` 重写先 `ConfirmDialog("删除确认","是否删除点位表「{name}」？…","删除")` 再 `base.Delete()`；`SyncCatalog()` 重写聚合跨工位点名；轴名写回仅当前表（`_loadingAxisNames` 标志防切换工位时误写）；`EnsureDefaultSelection()` 选中首个工位+首点。
- **`Views/PointPage.xaml`** 主体改成两列 Grid（左 232 / 右 *）：左 Border 含说明 + `UniformGrid`(新增工位 TtBaseBtn / 删除工位 TtDeleteBtn) + `共 N 个工位` + `ListBox`(`ProjectListStyle`，项模板显示 `Name` + `Points.Count` 点)；右 Grid 含空态 `TextBlock`(`HasTable==False` 触发器) + `ScrollViewer`(无表折叠) 内：工位标题 + 重命名 `TextBox` + 轴控制区(复用 `AxisCardTemplate` on `AxisStates`) + 点位区(`CurrentPoints`-绑定 DataGrid，8 列 H0Pos…H3Spd + 移动/保存按钮，结构同前)。Resources 段（转换器/8 表头模板/AxisCardTemplate）保留。
- **构建 0 警告 0 错误；冒烟测试 `NoCodeMotion.exe` 存活 12s 无 stderr；`project.json` 迁移验证：出现 `PointTables` 数组、legacy 字段清空、工位1 含迁移轴与点、备份 `project.json.bak-before-pointtables`。**
- **套路**：WPF「两级列表」模式（外层工位列表可增删切换 + 内层行 DataGrid）用 `ListEditorViewModel<T>` 泛型基类 + 内层集合走 `SelectedItem.ChildCollection` 绑定即可复用；跨实体聚合（Catalog 点名）需在子集合 `CollectionChanged`/`PropertyChanged` 冒泡后统一 `SyncCatalog`；反序列化替换 `ObservableCollection` 必须在 setter 重挂 handler，否则自动保存/联动失效。

## 主页面新增「工程师」与「操作员」两个导航页
- 诉求：主页面顶部导航新增 2 个页面——工程师页面（工程工作台，聚合配置模块入口）+ 操作员页面（工位运行视图，复用工位概念运行点位）。
- **App.xaml**：新增 `EngineerIcon`（扳手/构建）、`OperatorIcon`（播放/运行）两个 Geometry 图标。
- **MainWindow.xaml**：顶部导航栏追加 `{Button Tag=Engineer/Operator, Click=Nav_Click}` 两个按钮（图标+中文文案"工程师"/"操作员"）。
- **MainWindow.xaml.cs**：`_pages` 字典加 `["Engineer"]=>new EngineerPage()` / `["Operator"]=>new OperatorPage()`；新增 `public static MainWindow? Instance`（构造赋值，供页面内跳转）与 `public void NavigateTo(key)`（按 Tag 找导航按钮并高亮）；`FindNavButton` 改为实例方法（原写成 static 访问实例字段 NavPanel 报 CS0120，已修）；`Navigate(key, Button?)` 支持 btn 为 null 时自动定位高亮按钮。
- **工程工作台 `Views/EngineerPage.xaml(+cs)` + `ViewModels/EngineerViewModel.cs` + `Views/ResourceLookupConverter.cs`**：`EngineerViewModel` 暴露 `Modules`（`ModuleCard`：Key/Title/IconKey/Count，8 个模块轴/IO/气缸/点位表/通讯/料盘/流程/变量，数量取自 `ProjectStore.Data`）+ `OpenCommand`（参数=模块键，调用 `MainWindow.Instance.NavigateTo(key)`）；页面用 `ItemsControl`+`WrapPanel` 渲染卡片（`ModuleCardButtonStyle` 自定义白卡圆角+柔和阴影+悬停浅强调），图标经 `ResourceLookupConverter`（字符串键→`FindResource`）取 App.xaml 的 Geometry。卡片点击即跳到对应配置页。
- **工位运行视图 `Views/OperatorPage.xaml(+cs)` + `ViewModels/OperatorViewModel.cs`**：`OperatorViewModel : ViewModelBase, IEnsureDefaultSelection`；`Tables=ProjectStore.Data.PointTables`，`SelectedTable` + `CurrentPoints`；顶部「选择工位」ComboBox（`CellComboStyle`）+「运行此工位」(`TtBaseBtn`,`IsEnabled=CanRun`)/「停止」(`TtGrayBtn`,`IsEnabled=IsRunning`)；`DispatcherTimer`(700ms) 逐步经过点位，运行中点位行高亮（`OpRowStyle` 基于 `PointRowStyle` 增 IsSelected 高亮）、`StatusText` 显示进度；纯运行状态仿真，无真实运动硬件；`EnsureDefaultSelection` 默认选首个工位。`ModuleCard` 用对象初始化器（无 4 参构造，初版用 `new ModuleCard("Axis",...)` 报 CS1729，已修）。
- **构建 0 警告 0 错误；冒烟测试 `NoCodeMotion.exe` 存活 12s 无 stderr（exit 124）。**
- **套路（新增顶部导航页的标准步骤）**：①`App.xaml` 加图标 Geometry；②`MainWindow.xaml` 加 `Button Tag=Key Click=Nav_Click`；③`MainWindow.xaml.cs` `_pages` 字典加 `Key=>new XxxPage()`（若需页内跳转再加 `Instance`/`NavigateTo`）；④新建 `XxxPage.xaml(+cs)`（`DataContext=new XxxViewModel()`）+ `XxxViewModel.cs`（继承 `ViewModelBase`，命令用 `RelayCommand(exec)`/`RelayCommand(exec,canExec)`，`SetField` 读写属性）；全程只建新明文文件 + 改明文 `MainWindow`/`App.xaml`，绝不碰 marker `88 7d 1c` 的加密基类文件。

## 工程师页改版：从「模块入口卡片」改为「四类控制控制台」
- 用户截图纠正：早前把工程师页做成跳转卡片墙（工程工作台）是误判；正确诉求是「工程师界面要直接放轴控制、IO控制、气缸控制、点位移动和设置」。操作员页（工位运行视图）保持不动，但其崩溃需一并修。
- **根因修复（操作员页 `XamlParseException: 找不到名为"PointRowStyle"的资源`）**：WPF `UserControl.Resources` 非全局。`PointRowStyle`/`PointCellStyle`/`PointHeaderStyle`/3 个转换器(`AxisHeaderMultiConverter`/`BoolToZhConverter`/`StringFormatConverter`)/`AxisCardTemplate` 原先只定义在 `PointPage.xaml` 的 `<UserControl.Resources>`，`OperatorPage`/`EngineerPage` 引用即崩。
  - **修复**：把这些共享资源从 `PointPage.xaml` 提升（promote）到 `App.xaml` 的 `Application.Resources`（全局），`PointPage.xaml` 只保留其 8 个本地列头模板 `H0Pos…H3Spd`（它们用 `RelativeSource AncestorType=DataGrid` 取 `AxisStates[i].AxisName`，页内专用）。`App.xaml` 已加 `xmlns:views`/`xmlns:svc`。
- **`ViewModels/EngineerViewModel.cs` 全量重写**（`ViewModelBase, IEnsureDefaultSelection`），四大板块：
  1. **轴控制**：`AxisStates`(4 个 `EngineerAxisState`：Index/AxisName/Enabled/Homed/CurrentPosition) + `EnableCommand`/`HomeCommand`/`InchCommand`/`JogCommand`/`InchStep`/`JogStep`（逻辑照搬 `PointViewModel`，运行态仿真）。XAML 用 `ItemsControl ItemsSource={Binding AxisStates} ItemTemplate={StaticResource AxisCardTemplate}` 渲染全局卡片。
  2. **IO 控制**：`Inputs`/`Outputs` 直接引用 `ProjectStore.Data.Inputs/Outputs`；`ToggleOutputCommand` 把 `IoItem.Value` 在 0/1 间切（输入只读展示 + 状态点，输出可开关按钮 `IoOutBtn` 按 `Value` DataTrigger 显「打开/关闭」）。
  3. **气缸控制**：`Cylinders`(`ObservableCollection<CylinderRuntime>`，每气缸一个运行态 `Extended`)；构造时按 `CylinderItem.Action=="缩回"` 初始化，`ProjectStore.Data.Cylinders.CollectionChanged` 增删同步；`ToggleCylinderCommand` 切 `Extended`，按钮 `CylBtn` 按 `Extended` DataTrigger 显「伸出/缩回」。`CylinderItem` 是 marker 加密文件**只读**，XAML 仅绑定 `Item.Name`/`Item.OutPoint` 等运行时属性，不修改源。
  4. **点位移动和设置**：`Tables=ProjectStore.Data.PointTables`、`SelectedTable`/`CurrentPoints`；「选择工位」ComboBox + 点位 DataGrid（列 `H0Pos…H3Spd` 复用轴名列头，绑定本页 `DataContext.AxisStates[i].AxisName`）+「移动」(`MoveToPointCommand`)/「保存」(`SaveCurrentCommand`)，逻辑同 `PointViewModel`（弹 `ConfirmDialog` 后读写 `AxisStates[i].CurrentPosition`↔`item.Positions[i].Position`）。
- **`Views/EngineerPage.xaml` 全量重写**：`ScrollViewer` + 4 个 `CardBorderStyle` 分区（轴控制 / IO 控制 两列 / 气缸控制 / 点位移动），分区标题用 `SectionHeaderStyle`(SectionIcon+Accent)；本页重复定义 `H0Pos…H3Spd` 8 个列头模板（页内专用，指向本页 AxisStates）。删除废弃的 `ResourceLookupConverter.cs`（卡片墙不再需要）。
- **构建 0 警告 0 错误；冒烟测试 `NoCodeMotion.exe` 存活（exit 124）无 stderr、无 XamlParseException。**
- **套路（WPF 跨页复用样式/模板）**：凡多页共用的 `Style`/`DataTemplate`/`Converter`，一律放进 `App.xaml` 的 `Application.Resources` 变全局，绝不留在一个 `UserControl.Resources` 里（否则其它页 `StaticResource` 引用会在运行时抛 `XamlParseException`）。`UserControl.Resources` 只放真正页内专用的资源。

## WPF `ResourceDictionary` 前向引用陷阱：模板里的 `StaticResource` 指向同字典内「之后定义」的兄弟键
- 用户在 VS 调试 `OperatorPage` 时再次遇到 `XamlParseException: 找不到名为"CardBorderStyle"的资源`，行号 151。
- **根因（不是 stale build）**：`grep CardBorderStyle` 揭露 `App.xaml:151` 在 `AxisCardTemplate` DataTemplate 内引用了 `Style="{StaticResource CardBorderStyle}"`，而 `CardBorderStyle` 定义在 `App.xaml:299` ——**同一个 `ResourceDictionary` 内的前向引用**。WPF 加载 `ResourceDictionary` 时按顺序加入条目，每条 `StaticResource` 必须在加入时刻可解析；模板/样式等资源里嵌入的 `StaticResource` 引用无法「等字典装完再回头解析」，所以前向引用直接抛 `XamlParseException`，且**进程在 App.xaml 加载阶段就崩**（应用根本起不来 MainWindow，用户感知是「一切到某页就崩」，但实际是启动崩，编辑器里恰好打开的是别的文件）。
- **为什么之前不崩**：`AxisCardTemplate` 原本在 `PointPage.xaml` 的 `UserControl.Resources` 里，模板里的 `StaticResource CardBorderStyle` 解析时已走完 App 字典的整树查找；移到 `App.xaml` 后变成了同字典内的前向引用。
- **修复**：把 `AxisCardTemplate` 里 4 个前向引用改为 `DynamicResource`（解析时机推迟到模板物化时，此时 App 字典已完整），其余保持 `StaticResource`：
  - `Style="{StaticResource CardBorderStyle}"` → `DynamicResource`（×1）
  - `Style="{StaticResource CellComboStyle}"` → `DynamicResource`（×1）
  - `Style="{StaticResource TtBaseBtn}"` → `DynamicResource`（×4）
  - `Style="{StaticResource TtWarnBtn}"` → `DynamicResource`（×2）
  - 用 `Edit replace_all` 改这 4 个键的 `Style="{StaticResource X}"` 即可——这 4 个键在 `App.xaml` 内**只**在 `AxisCardTemplate` 里被引用，作用域安全。
- **验证**：构建 0 警告 0 错误；`timeout 7s ./bin/Debug/net10.0-windows/NoCodeMotion.exe` 存活（exit 124）无 stderr → App.xaml 加载成功，启动崩已根治。
- **套路（WPF 资源前向引用）**：①把模板/样式提升到 `Application.Resources` 后，务必检查它引用的所有 `StaticResource` 键是否**在字典中已经定义在它之前**；②如不愿/不能重排，把那些前向引用改成 `DynamicResource`（性能/语义可接受的折中）；③症状特征：报错文件名是「被提升的字典文件」（`App.xaml`）而不是「使用该模板的页面」，行号指向模板内引用处，应用在**启动期**崩溃（`XamlParseException` 出现在 `Application.Run` 之前/期间）。

## WPF `Style.TargetType` 必须匹配被应用的元素类型
- 用户用户运行起来后在操作员界面看到 `XamlParseException: "TextBox TargetType 与元素"TextBlock"的类型不匹配。"`（行号 151 左右）。
- **根因（潜在 bug，不是资源问题）**：round-2 写的 `OperatorPage.xaml` 把点位表格的只读展示单元写成 `<TextBlock Text="..." Style="{StaticResource PointCellTextBoxStyle}"/>`，但 `PointCellTextBoxStyle` 在 `App.xaml` 的 `TargetType="TextBox"`。WPF 强制要求 `Style.TargetType` 与被应用元素的实际类型（或其基类）匹配——`TextBlock` 不是 `TextBox` 的子类，应用 TextBox 样式到 TextBlock 直接抛 `XamlParseException`。**之前 round-2 没报只是因为前一个崩溃（`PointRowStyle`）更早出现、把页面加载中断了；前向引用修完后才能走到这里**。
- `EngineerPage.xaml` / `PointPage.xaml` 的同一系列列都是 `<TextBox>`（可编辑）+ `PointCellTextBoxStyle` → 类型匹配，不报错。
- **修复**：
  - 在 `App.xaml`（紧跟 `PointCellTextBoxStyle` 之后）新增 `PointCellTextBlockStyle`：`TargetType="TextBlock"`，含 `Padding="6,0"`/`VerticalAlignment="Center"`/`Foreground` 三个 setter，视觉对齐但适配 TextBlock（`IsMouseOver`/`IsKeyboardFocused` 这些 TextBox 专属触发器无法复制到 TextBlock，故只保留基础视觉）。
  - `OperatorPage.xaml` `replace_all`：所有 `Style="{StaticResource PointCellTextBoxStyle}"` → `Style="{StaticResource PointCellTextBlockStyle}"`（9 处单元格，全是 `<TextBlock>`）。
- **验证**：`grep` 复查三个页面：`OperatorPage` 9 个 TextBlock 全用 `PointCellTextBlockStyle`，`EngineerPage`/`PointPage` 的 TextBox 全用 `PointCellTextBoxStyle`——零类型不匹配。构建 0 警告 0 错误。该 `XamlParseException` 是运行时（页面构造时）抛出，静态构建不会捕获；只有真正打开操作员页才能复现/验证修复，冒烟 `timeout` 只启 MainWindow 不能覆盖。
- **套路（只读 vs 可编辑单元格样式）**：可编辑表格用 `TextBox` + `PointCellTextBoxStyle`（含 hover/focus 背景触发器）；**只读展示表格用 `TextBlock` + `PointCellTextBlockStyle`（`TargetType=TextBlock`）**——千万别把 TextBox 样式套在 TextBlock 上，也别图省事给只读场景写 `TextBox IsReadOnly=True`（语义模糊、键盘 Tab 仍仍能聚焦）。改。改。

## 操作员控制台（OperatorPage）重做：生产数据 + 折线图 + 启动/停止/急停/复位 + 用户切换 + 异常日志
- 用户明确：操作员页面 = 一些生产数据和折线图 + 启动/停止/急停/复位按钮 + 用户切换 + 异常日志（不是之前的只读「工位运行视图」点位列表）。
- **新增 `Models/LogEntry.cs`**：`LogLevel` 枚举（Info/Warn/Error）+ `LogEntry`（Time/Level/Message）。注意 `LogLevel` 在 `NoCodeMotion.Models` 命名空间 —— XAML 里引用必须用 `xmlns:md="clr-namespace:NoCodeMotion.Models"` + `{x:Static md:LogLevel.Warn}`，**不能**用 ViewModels 前缀（第一轮就因为写成 `vm:LogLevel` 编译报 MC3050「找不到类型 LogLevel」）。
- **`OperatorViewModel.cs` 重写**：保留 `Tables`/`SelectedTable`/`CurrentPoints` 工位选择与 `DispatcherTimer`(700ms) 工位步进逻辑；新增：①KPI —— `TotalCount`/`Yield`(99%±)/`CycleTime`(s)/`RunElapsedText`(mm:ss)；②折线图 —— `Samples`(ObservableCollection<ChartSample>：Time/Yield/Cycle/Count) + 两个 `PointCollection` `YieldPoints`/`CyclePoints`（在 `RebuildChart()` 里把采样映射到固定绘图区 CW=640 CH=220 Pad=30，Yield 按 0~100% / Cycle 按 0~10s 归一）；③异常日志 `Log`(ObservableCollection<LogEntry>，新条目 `Insert(0,...)` 倒序、上限 200)；④用户切换 `Users`+`CurrentUser`+`SwitchUserCommand`（CurrentUser setter 内自动记一条日志）；⑤四个命令 `RunCommand`/`StopCommand`/`EStopCommand`/`ResetCommand`，复用 `CanRun`/`IsRunning`/`EStopped` 控制按钮启用态（急停后须复位才能再启动）；运行 tick 里推进产量、抖动良率/节拍、采样并偶发（~12%）告警写入日志。构造时预置 12 个历史采样使折线图打开即有内容。
- **`OperatorPage.xaml` 重写**：`ToolbarStyle` 顶部放标题 + 右侧 `当前用户` ComboBox(`Users`/`CurrentUser` TwoWay)；`WrapPanel` 4 张 `CardBorderStyle` KPI 卡（产量/良率/节拍/运行时长）；`CardBorderStyle` 控制区含工位 ComboBox + 状态文本 + 4 按钮（启动 `TtBaseBtn` / 停止 `TtGrayBtn` / 急停 `TtDeleteBtn` 红 / 复位 `TtWarnBtn` 橙）；`Canvas`(640×220) + 两条 `Polyline`(`YieldPoints` 绿 / `CyclePoints` 橙) + 网格基线 + Y 轴刻度 + 图例；底部 `ListBox` 绑 `Log`，行内 `Ellipse` 圆点 + 等级文字标签 + 正文均用 `DataTrigger` 按 `md:LogLevel` 着色（Warn→WarningBrush、Error→DangerBrush）。无第三方图表库，纯 WPF `Polyline` 实现折线图。
- **验证**：构建 0 警告 0 错误；`timeout 7 ./bin/Debug/net10.0-windows/NoCodeMotion.exe` exit 124 无 stderr → 启动加载干净（XamlParseException 未复现）。注意 OperatorPage 的 `XamlParseException` 只在真正打开该页时抛出，冒烟 `timeout` 只启 MainWindow 不能覆盖，需在 VS 里实际打开操作员页确认。
- **套路（WPF 折线图，零依赖）**：用固定尺寸 `Canvas` + `Polyline`，`Points` 绑 VM 里的 `PointCollection`（VM 按绘图区尺寸归一采样值后 `OnPropertyChanged` 通知）；避免引入 LiveCharts/OxyPlot 等新 NuGet 依赖，保持「零额外依赖」路线。

## 操作员控制台 UI 调整：去掉工位选择，加 手动/自动 模式按钮
- 用户要求：操作员界面不需要工位选择；启动/停止按钮后面再加 手动、自动 两个按钮。
- **OperatorViewModel.cs**：新增 `OpMode` 枚举（Manual/Auto）+ `Mode` 属性（默认 Auto）+ `ManualCommand`/`AutoCommand`（分别置 Mode=Manual/Auto 并记日志）。`Start()` 按模式分支：自动=原定时器连续运行；手动=每按一次启动只前进一个点位（单步，不启动定时器，走到末点位即停），并抽出 `AdvanceProduction()` 供手动单步与自动 tick 共用（产量+30~50、良率/节拍抖动、采样+RebuildChart）。`OnTick` 改为调用 `AdvanceProduction()` 去重。`SelectedTable` 仍由 `EnsureDefaultSelection` 默认选第一个（隐藏选择 UI，但运行逻辑保留）。
- **OperatorPage.xaml**：删除「选择工位」ComboBox 及其标签（保留 StatusText）；在 启动/停止 之后插入 手动/自动 两个按钮，用页面资源里新增的 `ModeManualBtnStyle`/`ModeAutoBtnStyle`（均 BasedOn `TtGrayBtn`，DataTrigger 在 `Mode==对应枚举`时背景变 AccentBrush、前景白），实现激活态高亮；重加 `xmlns:vm="clr-namespace:NoCodeMotion.ViewModels"` 以 `{x:Static vm:OpMode.Manual/Auto}` 引用枚举。按钮顺序现为：启动 / 停止 / 手动 / 自动 / 急停 / 复位。
- **验证**：构建 0 警告 0 错误；`timeout 7` 启动 exit 124 无 stderr。操作员页 `XamlParseException` 仍需在 VS 实际打开该页确认（冒烟只启 MainWindow）。
- **套路（WPF 模式切换高亮按钮）**：两个互斥模式按钮各用一个 BasedOn 灰色按钮样式 + 单个 DataTrigger（绑定模式属性 vs `{x:Static ns:Enum.X}`）切换激活背景/前景，无需额外转换器；枚举务必放在可被 XAML 引用的命名空间（本例 ViewModels，用 vm: 前缀）。

## 专利（交底书）驱动：表格化时序确定性编排（方向二）端到端落地
- 依据 `D:\三姆森\专利\一种基于表格化可视化配置的工业运控软件便捷操作系统及方法-交底书.docx`（方向二：表格化时序确定性编排引擎）。用户选择「两块都做（端到端）」：①点位表加「时序标记/同步组」列 + 编译期冲突检测；②操作员运行时序偏差监控。纯仿真，无真实 EtherCAT 硬件。
- **模型 `Models/PointItem.cs`**（明文可编辑；`EditorItemBase` 为 marker 加密文件**不碰**）：新增 `TimingMark`(string，如 "T+0ms"/"T+5ms") + `SyncGroup`(string，如 "GroupA") 两个普通属性，均带 `OnPropertyChanged()`（确保改动冒泡到 `PointTable.OnPointChanged` → 自动保存落盘）。`System.Text.Json` 默认序列化所有 public 属性，无需 `[JsonIgnore]`，自动持久化。
- **编译引擎 `Services/TimingCompiler.cs`**（新建，纯算法）：`TimingSeverity`(Info/Warning/Error) + `TimingIssue`(Severity/PointName/SyncGroup/Message) + `ParseTimingMark(string)`（解析 "T+5ms"/"5ms"/"0" → ms，null=未配置）+ `Compile(IEnumerable<PointItem>)` 做 5 类冲突检测：①解析错误(Error)；②总线周期分辨率(1ms，`BusCycleMs`，Warning，标记非整毫秒)；③同步组同组一致性(同组应同一时刻原子执行，成员 mark 不一致→Error)；④同步组规模(`MaxActionsPerCycle=8`，超上限 Warning，单 DC 周期可行性)；⑤同周期资源争用(同 T 被某同步组与独立点位占用→Warning)。
- **点位页 `ViewViewModels/PointViewModel.cs` + `Views/PointPage.xaml`**：VM 加 `CompileCommand` + `CompileIssues`(ObservableCollection<TimingIssue>) + `HasCompiled`/`HasCompileIssues`/`ErrorCount`/`WarningCount`/`CompileSummary`，`Compile()` 调 `TimingCompiler.Compile(SelectedItem.Points)`。`PointPage` 表格加两列「时序标记」「同步组」(TextBox 绑 TimingMark/SyncGroup，`PointCellTextBoxStyle`)，工具栏加「编译时序」按钮(`ToolButtonStyle` + 新增 `CheckIcon` 几何)，结果面板(`CardBorderStyle`，`HasCompiled` 可见)显示摘要 + 分级着色问题列表(`IssueDotStyle`/`IssueMsgStyle` 按 `svc:TimingSeverity`)。
- **操作员监控 `ViewModels/OperatorViewModel.cs` + `Views/OperatorPage.xaml`**：VM 加 `TimingStatus`(Normal/Deviation/OverThreshold/Unconfigured) + `TimingRow`(`: ViewModelBase`，INPC，Step/PointName/ExpectedText/ActualText/DeviationText/Status/StatusText) + `TimingRows`(ObservableCollection)。`Start()` 调 `BuildTimingRows()`（按 Points 解析预期 mark，未配置→Unconfigured）；`StepTo()` 内 `RecordTiming(idx)`：实际=累计仿真时钟 `_timingClockMs`+±3ms 抖动、偏差=实际−预期，`|偏差|≤2ms` 正常、`≤5ms` 偏差、否则超阈并 `AddLog(Error,...)` 报警；`_timingClockMs += 700`。`EStop`/`Reset`/`SelectedTable` 变更均 `TimingRows.Clear()`。`OperatorPage` 在折线图与异常日志之间加「时序偏差监控」卡（`DataGrid` 绑 `TimingRows`，`TimingDotStyle`/`TimingStatusTextStyle` 按 `vm:TimingStatus` 着色）。阈值常量 `TimingThresholdMs=2.0`。
- **坑（已修）**：①`PointPage` 结果面板 `Border` 同时设 `Style` 属性与 `<Border.Style>` 子元素 → MC3024「Style 只能设置一次」，去掉属性版、保留 `BasedOn="{StaticResource CardBorderStyle}"` 的 `<Border.Style>` 即可。②`PointPage` 问题行模板原想用 `<Run Text="{Binding PointName, StringFormat=【{0}】}"/>` —— `StringFormat` 含 `{0}` 会被当成嵌套标记扩展解析失败，改为三列 Grid（圆点 + 点名 + 消息）规避。③新增按钮引用 `CheckIcon` 前需确认 App.xaml 有该 Geometry（否则构建报找不到资源），已补 `CheckIcon` 路径。
- **验证**：构建 0 警告 0 错误；`timeout 7 ./bin/Debug/net10.0-windows/NoCodeMotion.exe` exit 124 无 stderr（启动干净）。操作员/点位页的 `XamlParseException` 与运行时交互仍需在 VS 实际打开相关页确认（冒烟只启 MainWindow）。
- **说明**：时序「实际触发时刻」为仿真测算（累计时钟 + 抖动），不是真实运动硬件回采；若用户把点位时序标记设为与运行节拍对齐的 T+0/T+700/T+1400… 则偏差多为正常/偏差，未配置标记的点位显示「未配置」不误报。

## 表格列头固定：所有 DataGrid 改用内部滚动条
- 用户要求：所有表格用内部滚动条，列名称不能随页面滚动条滚走（即列头必须固定在表格顶部）。
- **根因**：各页把 `DataGrid` 包在页面级 `ScrollViewer` 里，而 `DataGrid` 没有高度上限 → 被无限高度撑开、整张表（含列头）随页面一起滚，列头就滚没了。
- **修复**：在 `App.xaml` 的隐式 `Style TargetType="DataGrid"` 里加 `<Setter Property="MaxHeight" Value="420"/>`（+ `VirtualizingPanel.ScrollUnit=Pixel` 平滑滚动）。`DataGrid` 自带内部 `ScrollViewer`，高度被上限截断后行区内部滚动、列头（`DataGridColumnHeadersPresenter`）留在顶部固定。具体表若想更小可在元素上本地设 `MaxHeight`（如操作员「时序偏差监控」表本地 `MaxHeight=280` 会覆盖全局 420）。
- 覆盖表格：FlowPage(FlowStep) / EngineerPage(点位) / IoPage(输入+输出两表) / PointPage(点位) / VariablePage(变量) / OperatorPage(时序监控，已 280)。构建 0 警告 0 错误；`timeout 7` 启动 exit 124 无 stderr。
- **套路（WPF 表格列头固定）**：`DataGrid` 要列头不滚动，必须给有界高度（MaxHeight/Height），使其内部 ScrollViewer 接管行滚动；切忌把无高度上限的 DataGrid 直接放进页面 ScrollViewer。统一在 App.xaml 隐式 DataGrid 样式里设 MaxHeight 是最省事、保证「所有表格」一致的写法。

## 表格占满外部容器（在「列头固定」基础上）
- 用户进一步要求：表格尽量占满外部容器（之前全局 `MaxHeight=420` 把表都压在 420，占不满）。
- **关键矛盾**：「列头固定」需要一个有界高度让 DataGrid 内部滚动；「占满容器」要求这个有界高度 = 容器可用高度，而不是写死 420。写死 MaxHeight 既限制占满、又仍依赖「表被放进有界容器」这一前提——放进 `ScrollViewer>StackPanel`（无限高）的表即使有 MaxHeight 也只是被截断、并非真正占满。
- **根治做法**：①`App.xaml` 隐式 `DataGrid` 样式**移除 `MaxHeight`**（保留 `VirtualizingPanel.ScrollUnit=Pixel`），注释改为「高度由外层 `*` 行 / DockPanel 末子约束，表格填满容器并内部滚动」。②把每个 DataGrid 放进有界容器：
  - FlowPage / VariablePage：本来就有 `*` 行（`RowDefinitions Auto/*`，表在 `Grid.Row=*`）→ 已占满，不动。
  - IoPage：根 `Grid` 原无 `RowDefinitions`（单行 Auto，无限高）→ 加 `<RowDefinition Height="*"/>`，两个 `DockPanel` 标 `Grid.Row="0"`，其末子 Border+DataGrid 即填满。
  - PointPage：右列原为 `ScrollViewer>StackPanel` → 改成嵌套 `Grid`：外层 `RowDefinitions *,Auto`；`Grid.Row=0` 内层 `Grid`（`Auto,*`）放 轴控制区(Auto) + 点位表(`*`)；编译结果面板放外层 `Row=1`(Auto，HasCompiled 才显示)；空状态 `Grid.Row=0` + 内层 `Grid` 用 HasTable 互斥可见。
  - EngineerPage：根 `ScrollViewer` → 改 `Grid`(`1*,1.2*`)；上 `Row0` 用 `ScrollViewer` 包前 3 块（轴/IO/气缸），下 `Row1` 点位卡用 `DockPanel LastChildFill=True`（顶部 StackPanel 放 标题+工位选择，末子 Border+DataGrid 填满）。
  - OperatorPage：根 `Grid.Row=1` 原 `ScrollViewer>StackPanel` → 改 `Grid`(`1*,1.3*,Auto`)：Row0 `ScrollViewer` 包 KPI/控制/折线图/异常日志，Row1 时序监控卡（`*` 占满、去掉本地 `MaxHeight=280`），Row2 异常日志(固定，ListBox 自带 MaxHeight=260 内部滚动)。`时序偏差监控` 表顺序保持在折线图与日志之间。
- **验证**：构建 0 警告 0 错误；`timeout 7 ./bin/Debug/net10.0-windows/NoCodeMotion.exe` exit 124 无 stderr（启动干净）。各表现在占满各自容器、列头固定、内部滚动；编译/异常日志 ListBox 各自 MaxHeight 内部滚动不变。
- **套路（WPF 表格「占满 + 列头固定」正确模式）**：全局不要在隐式 DataGrid 样式写死 MaxHeight；改为让每个表落在有界 `*` 行（或 DockPanel 末子 / `LastChildFill` DockPanel 末子）。凡是 `ScrollViewer>StackPanel` 包裹的表都占不满且只有写死高度兜底——要把表提出来单独进 `*` 行。改动纯 XAML，不动 .cs。

## 修复 FlowPage 滚动条跑到列表外（EditorPage 外层 ScrollViewer 反模式）
- 现象：流程页面的步骤表格滚动条跑到列表外面（包着整页滚），列头随页面滚走；但结构上看 FlowPage 明明是 `Grid RowDefinitions Auto/Auto/*`、表在 `Grid.Row=2 Height="*"`，本应占满并内部滚动。
- **根因**：`EditorPage.xaml`（第 48-50 行）在 `Detail` 的 `ContentControl` 外面套了一个 `<ScrollViewer>`。该 ScrollViewer 给 `Detail` 内容**无限高上下文**，于是 FlowPage 的 `*` 行在「无限高」里永远算不出有界高度 → 整张表被撑开 → 由包裹整页的 ScrollViewer 滚动（滚动条在列表外）。变量页（VariablePage）之所以正常，是因为它**不是** `EditorPage.Detail`，是独立页，其 `*` 行的父级有界高度，所以生效。
- **关键陷阱**：`*` 行只有在其**父级链上每一层都有界高度**时才生效。一个外层 `ScrollViewer` 会在它那一层把高度变成「无限」，下面所有 `*` 行全部失效。
- **修复**：①`EditorPage.xaml` 移除包 `Detail` 的 `ScrollViewer`，让 `Border`(DetailPanelStyle) 由 EditorPage 的 `*` Row 1 约束出有界高度；FlowPage 的 `*` 行随即生效、表填满并内部滚动（列头固定）。②`EditorPage` 的其它 Detail 页（Axis/Comm/Cylinder/Tray，都是 `StackPanel` 长表单，需要滚动）——在各自 `EditorPage.Detail` 的内容 `StackPanel` 外**各包一层 `<ScrollViewer VerticalScrollBarVisibility="Auto">`**，否则表单会被裁剪无法滚动。共改 5 个文件：EditorPage.xaml + 4 个表单页（Axis/Comm/Cylinder/Tray）。
- **验证**：`"/c/Program Files/dotnet/dotnet" build -v q -nologo` → 0 警告 0 错误；`timeout 7 ./bin/Debug/net10.0-windows/NoCodeMotion.exe` exit 124（干净启动，无 XamlParseException）。
- **套路（宿主页 ScrollViewer 与子页 `*` 行冲突）**：若某子页依赖「父容器有界高度 + 内部 `*` 行」来填充并内部滚动，**绝不能在宿主层用 `ScrollViewer` 包它**（ScrollViewer 给子级无限高，破坏 `*` 行）。正确做法：宿主层只给一个有界容器（Grid `*` 行 / DockPanel 末子），把「是否滚动」的权责下放到各子页——表格型子页用自身 `*` 行内部滚动；表单型子页自己套 `ScrollViewer`。即「高度约束放外层、滚动策略放内层」。
- 注意：构建须在沙箱后台/分离执行（dotnet 被当作 LOLBin 拦截）；代码改 XAML 明文文件，不碰 marker `88 7d 1c` 加密基类。

## 工程师/操作员页改为自适应 2 列布局（去外部滚动条 + 精简内容）
- 诉求：两页「布局尽量自适应大小，不要出现外部滚动条，内容少一些」。
- **根因**：两页原来都用「整页 ScrollViewer 包 StackPanel」——整页滚动（外部滚动条），且内容堆在一串长列表里不随窗口缩放。
- **EngineerPage.xaml 重写**：根 `Grid` 2 列（`*` / `1.3*`）。左列再分上下 `*`/`*` 两张卡（轴控制 / 气缸控制），右列上下两张卡（IO 控制 / 点位移动）。每卡用 `CardBorderStyle` + `DockPanel LastChildFill="True"`：顶部 SectionHeader（删掉原来每段的长说明文字），末子内容区；轴/气缸/IO 卡内容用各自内部 `ScrollViewer` 兜底溢出；点位表 `DataGrid` 自身内部滚动、列头固定（末子 Border 给有界高度）。整页不再有 page 级 ScrollViewer。
- **OperatorPage.xaml 重写**：根 `Grid`（`Auto` 工具栏 / `*` 主体）。主体 2 列（`*` / `1.25*`）：左列用单个内部 `ScrollViewer` 包 KPI+控制+折线图（左列可内部滚，非整页）；右列上下两卡（时序偏差监控 `1.3*` + 异常日志 `*`），两卡内 DataGrid/ListBox 各自内部滚动、列头固定。原异常日志 ListBox 的 `MaxHeight=260` 去掉，改为占满所属 `*` 行内部滚动。删减时序监控长说明为单行。
- **套路（WPF「整页自适应 + 无外部滚动条」通用结构）**：①永远不要「整页 ScrollViewer 包 StackPanel」；②根 Grid 用 `*` 行/列把空间切分；③每个区块用 `DockPanel LastChildFill="True"`，顶部放标题、末子放需滚动的内容（DataGrid 自带滚动；普通列表包一层内部 `ScrollViewer`）；④溢出只出现在区块内部的滚动条，页面本身不滚。多区块用 2 列网格（如 `*` / `1.3*`）并让宽内容（表格/IO）占宽列。「内容少一些」= 删冗长说明文字 + 压缩 margin/padding，不动功能绑定。
- **验证**：`dotnet build -v q -nologo` → 0 警告 0 错误；`timeout 7 NoCodeMotion.exe` exit 124 干净启动。

## 通讯页面重做为苹果风格密集参数 + 调试操作面板
- 诉求：「通讯页面需要尽量多的参数和调试操作，使用苹果风格」。
- **约束**：`CommItem.cs` 是 marker `88 7d 1c` 加密文件（只读，不能加字段），故「连接参数」只能复用已绑定的 8 个模型字段（Name 字符串 / CommType·Parity·StopBits 字符串 / PortOrIp 字符串 / BaudOrPort·TimeoutMs·DataBits 整数）。在明文 `CommViewModel.cs` 里加「调试状态 + 命令」，不碰模型。
- **CommViewModel.cs 扩展**（明文可编辑）：①选项数组 `CommTypeOptions`(8)/`DataBitsOptions`(7,8)/`ParityOptions`(无/奇/偶)/`StopBitsOptions`(1,1.5,2)/`FlowControlOptions`(无/RTS-CTS/XON-XOFF)/`EndianOptions`(大端/小端)（药丸选择器 ItemsSource）；②调试状态 `IsConnected`/`StatusText`/`AutoReconnect`(默认true)/`VerboseLog`(默认true)/`KeepAlive`/`RetryCount`(3)/`PollIntervalMs`(200)/`BufferSize`(1024)/`FrameIntervalMs`(10)/`FlowControl`/`Endian`/`SendText`/`PingResult`/`DebugLog`(ObservableCollection<string>，最新在上、上限300)；③命令 `TestConnectionCommand`(TCP 类用 `System.Net.Sockets.TcpClient.ConnectAsync` 真实连探测并记延迟，串口/ModbusRTU 做回环自检仿真) / `OpenCommand`/`CloseCommand`(仿真连接态) / `SendCommand`(回显) / `AutoScanCommand` / `ClearLogCommand`。`ParseHostPort` 接受 `(string?, int)`（BaudOrPort 实测为 int，不是 string）。
- **CommPage.xaml 重写**（iOS 设置式分组卡片，纯 XAML）：灰底(`CanvasBrush`) + 白色圆角卡(`AppleGroupCard` 圆角12+发丝边+SoftShadow) + 卡片内小灰字分组标题 + 行内发丝线分隔(`Rectangle Height=1`)。三块：①连接参数（名称/通讯类型药丸/端口IP/波特率/数据位/校验位/停止位/超时）；②高级与调试参数（重试次数/轮询间隔/接收缓冲/帧间隔 数值字段 + 流控/字节序 药丸 + 自动重连/详细日志/保活 开关）；③调试操作（连接状态药丸+彩色点 DataTrigger + 测试连接/打开/关闭/自动扫描/清空 按钮 + 发送输入+回显终端）。页面资源定义：`AppleGroupCard`/`AppleSectionHeader`/`AppleLabel`/`AppleRow`(DockPanel)/`AppleTextField`/`AppleNumField`/`ApplePillList`(WrapPanel+ListBoxItem 药丸模板，选中=Accent 底白字)/`AppleBtn`(蓝圆角)/`AppleBtnSecondary`(浅蓝底)/`AppleToggle`(用 CheckBox 模板实现 iOS 开关：圆角轨道+白圆钮，IsChecked 切 Success 绿并平移圆钮)/`AppleTerminal`(等宽字体浅底 ListBox)。
- **踩坑（已修）**：①`Style` 直接子级不能放 `Trigger`（`MC4004`）——要么放进 `<Style.Triggers>`，要么（如 AppleBtnSecondary）写独立完整 `ControlTemplate` 把触发器放在模板里。②HandyControl 3.5.1 的 `hc` 命名空间**没有 `ToggleSwitch`**（`MC3074`）——改用 `CheckBox` 自定义 `AppleToggle` 模板实现 iOS 开关。③`System.IO.Ports.SerialPort` 不在 net10.0-windows 默认框架（`CS1069`，类型转发到外部程序集）——不引包，改用 `Microsoft.Win32.Registry.LocalMachine.OpenSubKey(@"HARDWARE\DEVICEMAP\SERIALCOMM")` 枚举 COM 端口（零额外依赖）。④`BaudOrPort`/`TimeoutMs` 实测为 int（原 XAML 用 `TextBox Text=` 绑 int 能工作，但 VM 里不能再 `int.TryParse(string)`）；`DataBits` 用 `ComboBox SelectedValue + SelectedValuePath="."` 把字符串项绑到 int?（沿用原写法，WPF 自动 string→int）。
- **验证**：`dotnet build -v q -nologo` → 0 警告 0 错误；`timeout 7 NoCodeMotion.exe` exit 124 干净启动。`CommPage` 的 XamlParseException / 交互仍需在 VS 实际打开通讯页确认（冒烟只启 MainWindow）。
- **套路（WPF 苹果风格「设置式」分组页 + 调试面板）**：①分组用「白圆角卡 + 卡片内小灰标题 + 行发丝线」三层；②多选一用 `ListBox`(SelectionMode=Single) + WrapPanel + 药丸 ItemContainerStyle（选中态在模板里改 Background/前景）；③开关用 `CheckBox` 模板（HC 无 ToggleSwitch）；④数值参数用无边框右对齐 `TextBox`（Apple 直连风），单位用旁边小灰字；⑤调试操作放「状态药丸 + 一组圆角按钮 + 终端 ListBox」；⑥加密模型不能加字段时，把调试状态/命令全放明文 ViewModel，模型只承载持久化参数。

## 通讯页参数按「通讯类型」动态显隐 + 紧凑无外滚
- 诉求：「选择不同的参数类型，对应的参数显示不同的；布局尽量紧凑，不需要外部滚动条」。
- **实现**：①新增 `Views/CommConverters.cs`（`NoCodeMotion.Views` 命名空间）的 `CommTypeVisibilityConverter : IValueConverter`——把 `SelectedItem.CommType` 与 `ConverterParameter`（'|' 分隔的类型清单）比对，命中返 `Visibility.Visible` 否则 `Collapsed`；页面资源注册 `<local:CommTypeVisibilityConverter x:Key="TypeVisible"/>`（`local`=Views）。②`CommViewModel.cs` 增加类型相关运行态属性 `SlaveAddress`(Modbus 从站地址)/`Rack`/`Slot`(西门子S7 机架槽位)/`NetworkNo`(三菱MC 网络号)（均 `SetField`，不落盘）。③`CommPage.xaml` 重写：**去掉外层 ScrollViewer**（不再有外部滚动条），改为 `Border(Canvas)`+`StackPanel`；「通讯类型」单独一张全宽药丸卡（驱动显隐）；其下 `WrapPanel` 流式排「参数芯片」（`AppleChip` 白卡圆角，宽 232、Margin 5，标签在上控件在下），按类型 `Visibility` 动态显示——串口/ModbusRTU 显 串口号/波特率/数据位/校验位/停止位/流控；网口类显 端口/IP/端口号；Modbus 显 从站地址/字节序；西门子S7 显 机架/槽位；三菱MC 显 网络号；名称/超时/重试/轮询/三个开关 始终显示。调试卡保留（状态药丸+按钮+发送+终端 Height 110 内部滚动）。④同一模型字段按语义复用到两个标签：串口的「波特率」与网口的「端口号」都绑 `BaudOrPort`(int)；串口的「串口号」与网口的「端口/IP」都绑 `PortOrIp`(string)。
- **坑**：调试卡「清空日志」按钮初写 `Command="{StaticResource ClearLogCommand}"` 误把命令当资源 → 应 `{Binding ClearLogCommand}`（命令是属性不是资源）；已修。
- **验证**：`dotnet build -v q -nologo` → 0 警告 0 错误；`timeout 7 NoCodeMotion.exe` exit 124 干净启动。通讯页 XamlParseException/交互仍需在 VS 实际打开确认（冒烟只启 MainWindow）。
- **套路（WPF「按枚举/类型动态显隐区块」）**：用 `IValueConverter`(值 vs '|' 分隔白名单 → Visible/Collapsed) + 各区块 `Visibility="{Binding 类型字段, Converter={StaticResource X}, ConverterParameter='A|B'}"` 即可，无需代码-behind；配合 `WrapPanel` 流式布局，隐藏的芯片自动让位、不留空洞，最紧凑。该转换器具通用性，可复用到其它「按类型显示不同参数」的页。

## 流程页面底部加执行控制按钮栏（运行/单步运行/跳到指定行/暂停/停止）
- 诉求：「流程页面的下面放一排按钮：运行，单步运行，跳到指定行，暂停，停止」。
- **前提**：`FlowPage.xaml` 已是 `EditorPage.Detail` > `Grid Margin=12`，`RowDefinitions` 原先 `Auto/Auto/*`（表在 `Grid.Row=2 *`）；根 `DataContext=FlowViewModel`。新增第 4 行 `Auto` 给按钮栏。
- **FlowViewModel.cs（明文，已含步骤面板）新增「流程执行仿真」运行态（纯运行态、不落盘）**：①`DispatcherTimer _runTimer`(Interval 600ms, Tick→StepOnce)；②状态 `CurrentStep`(0基, -1未开始/等于 Steps.Count 已完成)/`CurrentStepText`(字符串, 供底部状态显示)/`IsRunning`/`IsPaused`/`JumpRow`(1基, 绑底部输入框)；③`CanRun`/`CanStep`/`CanPause`/`CanStop`(bool)；④`RunCommand`/`StepCommand`/`JumpCommand`/`PauseCommand`/`StopCommand`(均 `RelayCommand`，**不带 can-exec 谓词**——见下坑)；⑤`Run()`(起定时器)/`StepOnce()`(前进1步、到末尾自动 Stop 并标记已完成)/`JumpToRow()`(按 JumpRow 1基 clamp 跳转、运行中从此处续)/`Pause()`(停定时器+IsPaused)/`Stop()`(复位 _currentStep=-1、清选中、RaiseRunState)；`HighlightCurrent()` 把当前步设为 `StepPanel.SelectedItem` 高亮；`RaiseRunState()` 在运行态变化时 `OnPropertyChanged(CanRun/CanStep/CanPause/CanStop/CurrentStepText)`；`OnPropertyChanged(nameof(SelectedItem))` 切换流程时 `SetItems` + `Stop()` 复位。
- **FlowPage.xaml 按钮栏**（`Grid.Row=3` 的 `Border CardBorderStyle` > 内 `Grid`）：运行(`RunCommand`,IsEnabled=CanRun,TtBaseBtn)/单步运行(`StepCommand`,CanStep,TtBaseBtn)/「跳到行」标签+`TextBox Text={Binding JumpRow,UpdateSourceTrigger=PropertyChanged}`(宽58居中)+跳转(`JumpCommand`,TtBaseBtn)/暂停(`PauseCommand`,CanPause,TtWarnBtn)/停止(`StopCommand`,CanStop,TtDeleteBtn)；右侧 `TextBlock` 绑 `CurrentStepText` 显示进度。按钮栏是固定 Auto 行，表仍在 `*` 行内部滚动，**不引入外部滚动条**（沿用前面修好的 `*` 行生效前提）。
- **坑（关键）**：`Button` 同时有 `Command`(带 can-exec) 和显式 `IsEnabled` 绑定时，WPF 行为取决于 `RelayCommand` 是否升 `CanExecuteChanged`——`RelayCommand.cs` 是 marker `88 7d 1c` 加密文件、未读，行为不确定，可能把按钮锁在初始态或和 IsEnabled 绑定打架。稳妥做法：**命令构造不传 can-exec 谓词**（`new RelayCommand(_ => Run())`，即恒可执），令「禁用态」完全由 `IsEnabled={Binding CanX}` 决定（由 `RaiseRunState` 可靠通知）；各执行方法内部仍带等价守卫（Run 查 CanRun、Pause 查 CanPause、StepOnce/JumpToRow 查空），重复/非法点击不崩。
- **验证**：`dotnet build -v q -nologo` → 0 警告 0 错误；`timeout 7 NoCodeMotion.exe` exit 124 干净启动（无 XamlParseException）。按钮交互/进度高亮需在 VS 实际打开流程页确认（冒烟只启 MainWindow）。
- **套路（WPF 底部固定执行控制栏）**：把控制栏放进父 Grid 的一个 `Height=Auto` 末行（与表格的 `*` 行并列），表格自身内部滚动、控制栏不随内容增减移动；按钮可用态用 VM 的 bool 属性 + `IsEnabled` 绑定（不要用带 can-exec 的命令，避免与加密 `RelayCommand` 行为冲突）。

## 流程表「实际值」列改为只读显示（统一蓝色调）
- 诉求：「改成不能修改的，都是读取出来的，都是蓝色」（截图显示列头为浅蓝底深蓝字「实际值」）。
- **改前**：列内是 `TextBox Text="{Binding ActualValue}"`（可编辑）+ 蓝点 `Ellipse` + 浅蓝 `#F2F7FE` 背景 + `AccentDarkBrush`（#0066CC）文字。
- **改后**：替换 `TextBox` 为 `TextBlock`（不可编辑、纯显示，由执行器回填结果），保留蓝点 + 浅蓝背景 + 蓝字；新增 `TextTrimming="CharacterEllipsis"` 避免超长结果撑高行高。仅改 `Views/FlowPage.xaml` 的「实际值」`DataGridTemplateColumn.CellTemplate`，不动表头（已是浅蓝底深蓝字）。
- **验证**：`dotnet build -v q -nologo` → 0 警告 0 错误；`timeout 7 NoCodeMotion.exe` exit 124 干净启动。
- **套路（WPF 表格「只读显示列」）**：把可输入列从 `TextBox in CellTemplate` 换成 `TextBlock`（加 `Padding/VerticalAlignment/TextTrimming` 复用原来视觉）即可一行变只读；表头背景/前景继续走列的 `HeaderStyle`，不要混进 `CellTemplate`。

## 流程列表：重命名按钮 + 表格/脚本不同图标与默认名
- 诉求：「添加重命名的按钮；左侧的如果是表格或者脚本，还是不同的图标和默认名称」。
- **重命名按钮（通用，所有 EditorPage）**：`ViewModels/ListEditorViewModel.cs` 新增 `RenameCommand`（`new RelayCommand(_ => Rename(), _ => SelectedItem != null)`），`Rename()` 置 `SelectedItem.IsEditingName = true`。`Models/EditorItemBase.cs` 新增 `IsEditingName`(bool) 属性。`Views/EditorPage.xaml` 工具栏在「删除」后加「重命名」按钮（Command=RenameCommand，图标 RenameIcon）。
- **左侧列表项内联重命名**：`EditorPage.xaml` 的 `ListBox` 把 `DisplayMemberPath="Name"` 换成 `ItemTemplate`（资源 `EditorListItemTemplate`）——`Grid`(图标 Path + 名称 TextBox)。名称 `TextBox` 用页内 `ListItemNameBoxStyle`：`IsReadOnly=True` 常态，DataTrigger(`IsEditingName==True`)切 `IsReadOnly=False`+白底+Accent 边框+`FocusManager.FocusedElement` 自动聚焦。`EditorPage.xaml.cs` 加 `NameBox_LostFocus`/`NameBox_KeyDown`(Enter)：把 `DataContext as EditorItemBase` 的 `IsEditingName` 复位为 false（提交）。
- **表格/脚本不同图标**：列表项 `Path` 用 `DataTrigger` 按 `Kind` 切换 `Data`+`Visibility`：`FlowKind.Table→TableIconGeometry`（网格）、`FlowKind.Lua→ScriptIconGeometry`（代码括号），非流程项（无 Kind）默认 `Visibility=Collapsed` → 只显示名称。需在 `EditorPage.xaml` 加 `xmlns:models="clr-namespace:NoCodeMotion.Models"` 以引用 `{x:Static models:FlowKind.Table/Lua}`。新几何在 `App.xaml`：`TableIconGeometry`/`ScriptIconGeometry`/`RenameIcon`。
- **不同默认名**：`FlowViewModel.CreateNewItem()` 改为按 `_nextAddKind` 计当前同类数量+1：`表格流程{n}` / `脚本流程{n}`（不再统一 `流程{n}`）。已含 `using System.Linq`（`.Count(i => i.Kind == kind)`）。
- **验证**：`dotnet build -v q -nologo` → 0 警告 0 错误；`timeout 7 NoCodeMotion.exe` exit 124 干净启动。重命名交互/图标变体需在 VS 实际打开流程页确认（冒烟只启 MainWindow）。
- **坑（本期复发）**：①Read 工具把 plaintext UTF-8 的 .cs/.xaml 误判为「binary」无法打开，编辑/重建一律用 `cat > file << 'EOF'` heredoc（bash 跑，沙箱放行）；②**易变文件系统**会把 `FlowItem.cs`/`FlowViewModel.cs` 中途回退到旧版（本次 FlowItem.cs 又丢过 FlowKind 枚举、FlowViewModel.cs 一度 revert），每次改完必须重新 `git status`/`build` 校验，发现缺定义立即用 heredoc 补回；③C# raw string `"""..."""` 在本工程编译不过（`CS8997 未终止的字符串字面量`），多行字符串改用 verbatim `@"..."`。

## 重命名改为弹窗（非内联光标编辑）
- 用户更正：「重命名是弹窗重命名，不需要光标变化」。即列表项名称不要做成内联可编辑（无 focus/caret 态）。
- **改为弹窗**：新建 `Views/RenameDialog.xaml`(+`.cs`)，沿用 `ConfirmDialog` 模式（透明窗+圆角白卡+柔和阴影，`Owner=Application.Current?.MainWindow`，`Loaded` 时 `NameBox.Focus()+SelectAll()`，空名等同取消）。`ListEditorViewModel.Rename()` 改为 `new RenameDialog("重命名", SelectedItem.Name).ShowDialog()`，返回 true 且 `ResultName` 非空则 `SelectedItem.Name = dlg.ResultName`（需 `using NoCodeMotion.Views;`）。`RenameCommand` 不变（仍 `CanExecute=SelectedItem!=null`）。
- **回退内联编辑**：`EditorPage.xaml` 列表项模板名称改回只读 `TextBlock`（保留按 `Kind` 切换图标的 `Path`）；删除 `ListItemNameBoxStyle` 与 `LostFocus/KeyDown` 处理；`EditorPage.xaml.cs` 去掉 `NameBox_LostFocus`/`NameBox_KeyDown` 及不再使用的 `using System.Windows.Input`/`using NoCodeMotion.Models`；`EditorItemBase.cs` 删除已无用的 `IsEditingName`。`RenameIcon` 几何已在 App.xaml。
- **验证**：`dotnet build` 0 警告 0 错误；`timeout 7 NoCodeMotion.exe` exit 124 干净启动。弹窗交互需在 VS 实际打开流程页点「重命名」确认（冒烟只启 MainWindow）。
- **套路（WPF 弹窗重命名）**：仿 `ConfirmDialog` 做独立 `Window` 弹窗，VM 直接 `new XxxDialog(...).ShowDialog()`（项目已有此先例，非纯 MVVM 也接受）；不要在 ListBox 里用可编辑 TextBox 做行内改名，用户偏好独立弹窗。
