# 2026-08-31 工作日志

## 工程存储从 JSONC 切换到 XLSX（续：sheet 排序对齐菜单顺序）
- 上一轮已完成 ProjectManager / ProjectStore 切到 XlsxProjectStore（明文反射通用读写、嵌套子表、字符串/数组/枚举/DateTime/bool 转换、JSON→XLSX 一次性迁移）。
- 本轮完成「页的顺序按主界面顶部菜单按钮顺序」需求（用户原始截图显示 menu 是 项目管理 / 控制器 / 轴 / IO / 气缸 / 点位表 / 通讯 / 料盘 / 相机 / 变量 / 流程 / 工程师 / 操作员）。
- 实现位置：`D:\wqz\code\NoCodeMotion\Services\XlsxProjectStore.cs` `Save(...)` 内，按 `MenuSheetNames` 索引对 sheet key 排序：
  - 父页按菜单索引排序（不在菜单里的页置末尾）。
  - 嵌套子页（key 含 `.`，如 `点位表.点位`）紧随父页之后。
  - LINQ `OrderBy` 返回元组 `(rank, childFlag, key)` 即可，按字典稳定排序。
- 顺手补 `SheetNameOverrides["Cameras"] = "相机"`，否则 Cameras 集合生成的 sheet 名为英文 "Cameras"，不匹配菜单第 8 位。
- **关键 bug**：第一次实现误写 `var kv = sheets[key]; ... kv.Key / kv.Value`，编译器报 CS1061（DataTable 没有 Key/Value）。根因是 `sheets[key]` 返回的是 value，不是 KeyValuePair，正确的写法是直接 `var dt = sheets[key]; ... key`。复刻"foreach(var kv in dict) → foreach(var key in dict.Keys.OrderBy(...))"重构时要立刻删掉原 KVP 访问，否则全错。修后 build 0 errors（24 警告为 FlowRunnerService / VisionEngine / OperatorViewModel 的可空性，无新增）。
- 验证：在 `D:\wqz\code\_projtrash\AnnoTest\Program.cs` 加 sheet 顺序断言（用 ClosedXML 反读 xlsx 比对 MenuSheetNames 顺序），跑通 19/19 断言。样例副本 `D:\wqz\code\_projtrash\sample_project.xlsx` 同步刷新（5483 字节）。
- 实际顺序：项目管理 → 控制器 → 轴 → IO → 气缸 → 点位表 → 点位表.点位 → 点位表.轴名 → 通讯 → 料盘 → 料盘.格子 → 相机 → 变量 → 流程 → 流程.步骤 → 流程.VisualSteps → 点位 → 点位轴 → 点位.Positions（末尾三项点位/点位轴/点位.Positions 不在 MenuSheetNames，自动置末尾）。
- harness csproj 补 `PackageReference ClosedXML 0.105.1` + `using System.Collections.Generic;`（项目本身是 `<ImplicitUsings>disable</ImplicitUsings>`）。

## 工程存储：嵌套子表合并到一页（用户：「合并到一页，其他的分页了都需要合并」）
- 上一轮分页方案：`点位表` + `点位表.点位` + `点位表.轴名`、`料盘` + `料盘.格子`、`流程` + `流程.步骤` + `流程.VisualSteps`，每个父概念分 2-3 个 sheet。用户嫌翻页麻烦，要求全部合并到一页。
- 用 AskUserQuestion 选了 **「父表 + 子表分组堆叠(分行块)」** 而非「父表单行 + 内联子列」。
- 实现位置：`D:\wqz\code\NoCodeMotion\Services\XlsxProjectStore.cs`
  - `ExcludedProperties` 加 `Points` / `PointAxes`（legacy 字段，EnsurePointTables 后已清空，避免写空 sheet）。
  - 删除 `CollectNestedSheets` / `RestoreNestedCollections` 的导出/导入路径调用（保留旧方法作为 dead code，编译期不报错）。
  - 新增 `AddMergedBlockSheets` / `RestoreMergedBlockSheets` 调度入口，按 sheet 名分派。
  - 新增 `BuildPointTableSheet` / `RestorePointTableSheet`、`BuildTraySheet` / `RestoreTraySheet`、`BuildFlowSheet` / `RestoreFlowSheet`，每页采用统一列布局：
    - `点位表` (17 列)：类型 / 名称 / 父项名称 / 时序 / 同步组 / 轴1-4 位置+速度 / 轴1-4 名。每个 PointTable 一块：1 行"工位" + N 行"点位" + 1 行"轴名" + 1 空行分隔。
    - `料盘` (12 列)：类型 / 名称 / 父项名称 / 行数 / 列数 / 起点X/Y / 间距X/Y / 行号 / 列号 / 已占用。每个 TrayItem 一块：1 行"料盘" + M 行"格子" + 1 空行。
    - `流程` (51 列)：类型 / 名称 / 父项名称 / 类型标记 / 角色 / Lua源码 / 状态 / 逻辑 / 功能 / 属性 / 操作 / 设值 / 超时 / 时长ms / 实际值 / 视步类型 / 使能 / 相机ID / 保存路径 / 曝光ms / 宽度 / 高度 / 源类型 / 文件夹路径 / 模板路径 / 分数阈值 / 角度范围 / 匹配模式 / 模板框X/Y/W/H / 算法 / 最小面积 / 最大面积 / 阈值 / 检测模式 / 测量模式 / 标定 / 单位 / 协议 / 目标 / 内容 / 预处理操作 / 预处理参数1-2 / 预处理ROI / 第二图路径 / 运行时长ms / 上次成功 / 上次结果。每个 FlowItem 一块：1 行"流程" + M 行"步骤" + N 行"视步" + 1 空行。
  - 用 `using NoCodeMotion.Models;` 让 XlsxProjectStore 直接引用 PointTable / PointItem / PointAxis / TrayItem / TrayCell / FlowItem / FlowStep / VisualFlowStep / FlowKind / FlowRole / FlowStatus。
  - 反序列化用 `类型` 列做路由：先扫"工位"行建 PointTable，再按"父项名称"挂点位/轴名；同理 料盘/流程。
- **新加 24 个反射小工具**：`GetCollection` / `SetStr` / `GetInt(object,prop)` / `GetInt(string) / `GetDouble` / `GetBool` / `TrySet`（含 Enum 支持）。
- 验证：`D:\wqz\code\_projtrash\AnnoTest\Program.cs` 加 3 个新断言（无 parent.child 分页 / 点位表类型列结构 / 流程类型列结构），跑通 21/21 断言。
- 实际 sheet 顺序（合并后）：项目管理 → 控制器 → 轴 → IO → 气缸 → 点位表 → 通讯 → 料盘 → 相机 → 变量 → 流程（共 11 个 sheet，从 19 缩到 11）。
- 样例副本 `D:\wqz\code\_projtrash\sample_project.xlsx` 同步刷新（9787 字节，比之前的 5483 字节大——因为多了 VisualFlowStep 的几十个空列）。
- 注意点：
  - 旧的"点位表.点位"/"料盘.格子"/"流程.步骤"等 sheet 已不再生成，老工程 xlsx 重新打开后这些子集合（Points/Cells/Steps/VisualSteps/AxisNames）会被清空（用户需重新保存一次以新格式落盘）。
  - 流程 sheet 列很宽（51 列），Excel 横向滚动难免。如果用户想要更紧凑，可以后续按 FlowItem.Kind（Table/Lua/Vision）拆 3 个 sheet，或用"当前 Kind"决定列布局。
  - ExcludedProperties 新增的 `Points` / `PointAxes` 也影响导入（不再写回 legacy 字段），但 `EnsurePointTables` 在 ProjectStore.Load() 已有调用，所以流程不变。

## 工程存储：XLSX 加「_说明」文档 sheet（用户：「下面添加注释说明，方便人和AI读取修改xlsx」）
- 目标：在 xlsx 末尾追加一张纯说明 sheet，列 = 主题 / 说明，涵盖（a）文件结构总览（sheet 顺序=菜单、合并页概念、文档页约定）；（b）行类型约定（类型 列：工位/点位/轴名/料盘/格子/流程/步骤/视步；IO.类型 输入/输出；项目管理 属性/值）；（c）编辑提示（勿改类型/父项名称、保留空行、布尔写法、列名即字段）；（d）枚举取值参考（FlowKind Table/Lua/Vision、FlowRole Main/Reset、FlowStatus Idle/Running/Done/Error）；（e）各合并页列说明（点位表 17 / 料盘 12 / 流程 51）。
- 实现位置：`D:\wqz\code\NoCodeMotion\Services\XlsxProjectStore.cs`
  - 新增 `BuildDocumentationSheet()`（private static，返回名为 `_说明` 的 DataTable，25 行说明）。
  - `ExportToDataTables` 末尾追加 `dict["_说明"] = BuildDocumentationSheet();`。
  - `Save` 排序：以 `_` 开头的 sheet 强制置末（`return (int.MaxValue, 0, k)`），保证 _说明 永远在最后。
  - `ImportFromDataTables` 开头过滤：构造 `dataTables`（排除 key 以 `_` 开头的 sheet），后续 IO 拆分 / 合并块还原 / 标量回填全部走 `dataTables`，文档页不会被当作数据回填。
  - 类级 `<summary>` 同步补充说明末尾有 _说明 文档 sheet。
- 验证：`D:\wqz\code\_projtrash\AnnoTest\Program.cs` 加 1.8（_说明 存在/末尾/>=10 行）+ 6.5（重开后数据不变、再保存 _说明 仍在末尾）两类断言；build 0 error（48 警告为历史遗留），跑通 **全部断言通过**（含之前的 21 → 现共覆盖 1.6/1.7/1.8/3/4/5/6.5）。样例 `D:\wqz\code\_projtrash\sample_project.xlsx` 同步刷新（12 个 sheet：原 11 + _说明）。
- 约定总结（供后续 AI/人改 xlsx）：**以 `_` 开头的 sheet 都是文档/非数据页，导入自动跳过**；新增文档页时只要 key 以 `_` 开头即可，无需改导入逻辑。

## 工程存储：说明合并进「项目管理」页（用户：「说明也合并到项目管理页的xlsx」）
- 上一节用独立 `_说明` sheet，用户要求合并进「项目管理」页。
- 改动 `D:\wqz\code\NoCodeMotion\Services\XlsxProjectStore.cs`：
  - 删除 `BuildDocumentationSheet()`（独立 `_说明` DataTable），新增 `AppendDocRows(DataTable meta)`：把说明行追加进「项目管理」meta 表（用其既有列 属性=主题 / 值=说明），先插一行空行分隔标量元数据与说明，再加标题行「===== 说明（仅供人和 AI 阅读，不参与数据回填） =====」+ 5 段内容（文件结构总览 / 行类型约定 / 编辑提示 / 枚举取值参考 / 合并页列说明）。
  - `ExportToDataTables`：`AppendDocRows(meta);` 再 `dict["项目管理"] = meta;`（删掉原 `dict["_说明"] = ...`）。
  - 回退 `Save` 排序里的 `_` 前缀置末分支（已无 `_` sheet）；回退 `ImportFromDataTables` 的 `dataTables` 过滤，改回直接用 `tables`（共 4 处 `dataTables.` → `tables.`：集合循环 / IO / 合并块 / 标量；漏改集合循环那处导致 CS0103，已修）。
  - 类级 `<summary>` 改为说明「项目管理 页末尾附说明段」。
- 为什么导入安全：标量回填只处理「属性 与根对象标量属性同名」的行，说明行的中文标题不会命中任何属性 → 被忽略；`LoadMeta` 只读 CreatedAt/UpdatedAt/Remark 三行，也不受影响。
- 验证（`D:\wqz\code\_projtrash\AnnoTest`）：1.8 改判「无 _说明 sheet + 项目管理含说明段（属性含『说明（仅供人和 AI 阅读』/『文件结构总览』/『枚举取值参考』）」；6.5 改判「重开数据不变 + 再保存 项目管理 仍有说明段 + 仍无 _说明」。build 0 error（48 历史警告），harness 全过。
- **重要坑：sample_project.xlsx 正被 Tencent Docs 预览占用，File.Copy 覆盖被静默吞掉（try/catch 为空）** → 离线核对改用 `C:\tmp\sample_verify.xlsx`（不受锁），确认 11 sheet + 说明落盘后，再用 python `shutil.copy2` 强制覆盖 `sample_project.xlsx`（本次 COPIED_OK）。教训：写被预览锁定的样例文件时，先写临时副本核对，再强制覆盖；或在 harness 里把 copy 改为非静默以便发现锁文件。

## 「全功能完整工程」模板补脚本/视觉/相机（用户：「需要脚本，视觉，相机」）
- 位置：`D:\wqz\code\NoCodeMotion\Services\ProjectTemplateCatalog.cs` 的 `FullFeatured()`。
- 现状：模板只有 5 主流程 + 3 复位（全是 Table 类型），2 个 CameraItem，但**没有 Vision flow（FlowKind.Vision+VisualSteps）和 Lua flow（FlowKind.Lua+LuaSource）**。视觉引导功能只有「视觉引导抓取」模板演示，对「全功能完整工程」必须补齐。
- 改动：
  - **Description**：末尾加「+ 视觉流程 + Lua 脚本流程」。
  - **Summary**：从 `5 主流程 · 3 复位` 改为 `5 主流程 · 1 视觉流 · 1 脚本流 · 3 复位`。
  - **Highlights**：加两行「视觉流程：相机2 拍照 → 模板匹配 → 缺陷检测 → 输出位姿到变量」+「脚本流程：Lua 脚本（轴联动 + IO 读写 + 延时）」；最后一行改为 `5 个主流程 + 1 视觉流程 + 1 脚本流程 + 3 个复位流程`。
  - **Factory**：在 3 个复位流程之后、return d 之前，新增两个 flow：
    1. **视觉检测**（FlowKind.Vision, FlowRole.Main）—— 4 个 VisualSteps：
       - 图像采集（CameraId="下视相机", ExposureMs=8, W=2448, H=2048, SavePath="Images/inspection/"）
       - 模板匹配（CameraId="下视相机", TemplatePath="Templates/part_a.bmp", Score=0.85, Angle=360, MatchMode="灰度匹配", TemplateRoiX=1100/Y=900/W=200/H=200）—— **注意字段名是 TemplateRoiX/Y/W/H，不是 TemplateRectX/Y/W/H**（VisualFlowStep.cs 字段名）。
       - 缺陷检测（Algorithm="NCC", MinArea=100, MaxArea=100000, Threshold=128, DetectMode="阈值面积"）
       - 输出位姿（Protocol="变量", Target="匹配分数", Content="{匹配分数},{X},{Y},{R}"）
    2. **脚本流程**（FlowKind.Lua, FlowRole.Main）—— LuaSource 内含完整 Lua 脚本：读变量、while 等待 IO、急停判断、轴 MoveAbs、Cylinder.Out/Back、Print 字符串。演示 Lua 解释器的常用 API（Variable.Get/Set、IO.Get/Set、Axis.MoveAbs、Cylinder.Out/Back、Delay、EStop、Print）。
- 弹窗自动跟随：NewProjectDialog.xaml 直接 `{Binding Summary/Description/Highlights/Name}`，无需改 XAML，预览立即显示新 Summary。
- 验证：dotnet build 0 error（48 警告为历史遗留）；AnnoTest harness exit 0（断言不受影响——它只测自己的 src ProjectData，与 FullFeatured 模板无关）。
- 后续可能补：①「全功能完整工程」是「全功能」级别，可考虑也给它加一个测量 (Measure) 类型的视步；② Lua 脚本里的 API 是「推测的命名」，真正可执行还要看 LuaEditorView / LuaInterpreter 里的真实函数签名——本轮没接解释器，等用户接硬件后按需对齐。

## 项目管理页「需求」输入框失焦/失灵修复（用户：「点击项目管理的这里的输入框，容易失能没有」）
- 现象：`需求` TextBox（ProjectManagerPage.xaml 第 136 行）打字时输入框容易"失能"——光标/焦点丢失、无法继续输入。
- 根因：`Text="{Binding RequirementsText, UpdateSourceTrigger=PropertyChanged}"` 每敲一个键都会把 `RequirementsText` 写回并重新绑定 `TextBox.Text`，WPF 在每次重绑时重置 caret/焦点，导致输入"断流"。同页的 `备注` TextBox（第 115 行，DetailRemark）一直用 `LostFocus` 所以没问题。
- 修复：第 136 行改为 `Text="{Binding RequirementsText, UpdateSourceTrigger=LostFocus}"`，并把 ToolTip 改成「每行一条需求。写清楚动作、IO、气缸、视觉、点位等。点击【保存需求】或失焦后生效。」——与备注框行为一致：焦点离开或点【保存需求】才提交，打字过程不再被打断。
- 顺手 grep 全页：现仅 115/136 两处 TextBox，均已 `LostFocus`，无其他 `PropertyChanged` 隐患。
- 验证：`dotnet build D:\wqz\code\NoCodeMotion\NoCodeMotion.csproj -c Debug` → EXITCODE=0，日志确认 `NoCodeMotion -> ...\bin\Debug\net10.0-windows\NoCodeMotion.dll` 产出。此前一次构建报 [BUILD=1 errors=0] 的异常是工具链瞬时误报（日志无 error CS 行），本次重跑 0 error 证实 XAML 改动合法。