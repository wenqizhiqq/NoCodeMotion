# 2026-09-01 工作日志

## 1. 视觉流程结果图：旋转框 WPF 叠加层（参考 GrayMatch.Wpf）
- **背景**：之前的实现把匹配框用 `Cv2.Rectangle(dst, Rect, ...)` 烧到 display Mat 上，
  只能画轴对齐矩形，且 `results[0]` 单条。Angle≠0 时方向错，多目标也只显一个。
- **新方案**：完全照 GrayMatch.Wpf 的渲染思路——底图保持原始像素，
  结果用 WPF 矢量叠加层画旋转矩形 + 文本。
- **改动清单**（5 文件，构建 0 error）：
  - `Models/MatchBox.cs`（新）：单条匹配结果在源图像素坐标下的框
    （LeftTopX/Y、TemplateWidth/Height、Angle、Score、Scale、Pass）。
  - `Services/Vision/VisionEngine.cs`：
    - `VisionReport` 增加 `List<MatchBox> Matches { get; }`。
    - `RunMatch` 改为遍历 `results` 全部（不只 [0]）填 `report.Matches`；
      Pass = Score >= Threshold；删除 `Cv2.Rectangle` + `DrawMarker` 轴对齐烧入
      （改由叠加层画旋转框）。仍保留 `featurePts.Add(最佳中心)` 喂测量步。
  - `Views/VisualFlowDetailViewModel.cs`：
    - 新增 `MatchResults` DP（ObservableCollection<MatchBox>）+ `HasMatchResults`。
    - 三个运行方法（RunMatchAsync / RunAsync / RunStepAsync）都从 `report.Matches` 回填。
    - `OnSelectedStepChanged` 切步骤时清空。
    - 状态文本附加"匹配 N 个目标"。
  - `Views/VisualFlowConverters.cs`：重写为 `AngleNegateConverter`
    （OpenCV 逆时针 → WPF 顺时针，参考 GrayMatch.AngleSignConverter）。
  - `Views/VisualFlowPage.xaml`：
    - 资源里注册 `<local:AngleNegateConverter x:Key="AngleNegate"/>`。
    - RoiCanvas 之后插入 `MatchOverlay` Canvas + ItemsControl，绑定 MatchResults，
      ItemTemplate 画旋转 Rectangle（Pass=True Lime / False #DC2626，StrokeThickness=2，
      RenderTransformOrigin 0.5,0.5，RotateTransform 用 AngleNegate）+ 相似度/角度文本。
  - `Views/VisualFlowPage.xaml.cs`：
    - 新增 `UpdateMatchOverlayTransform()`：按 ResultImageView 的 Stretch=Uniform 缩放/居中
      把 MatchOverlay 的 RenderTransform 设为 `Translate(ox,oy) + Scale(s,s)`，把
      像素坐标映射到屏幕。公式与 `ApplyTemplateRoi` 完全一致（共用同一 scale/offset）。
    - 订阅 `ImageHost.SizeChanged` + `_vm.PropertyChanged(ResultImage/MatchResults)` 触发重算。

## 2. 状态栏：点击错误可弹窗显示全部错误
- **背景**：状态栏右边显示 `ExceptionText`，XAML 用 `TextTrimming="CharacterEllipsis" MaxWidth="420"`
  截断，长错误只看到 `...无法对"NoCodeMotion.Services.Vision.MatchOutco...`。
- **改动清单**（3 文件）：
  - `Views/ExceptionDetailDialog.xaml`（新）：Apple 风格弹窗（WindowStyle=None +
    AllowsTransparency + 圆角 14 白卡 + SoftShadow），标题"错误详情"，
    主体 IsReadOnly TextBox（Consolas 13px + SecondaryBackgroundBrush + MaxHeight=320 自动滚动），
    按钮"复制"（TtBaseBtn）+ "关闭"（TtGrayBtn）。
  - `Views/ExceptionDetailDialog.xaml.cs`：Message DP 喂文本；CopyButton 用
    `Clipboard.SetDataObject(text, true)` 复制（即使程序退出剪贴板仍可用），
    catch 剪贴板被占用异常。复制成功后按钮文案变"已复制"且禁用。
  - `Views/StatusBarView.xaml`：异常 TextBlock 包进可点 Border（淡红 `#22DC2626` 圆角 +
    Cursor=Hand + ToolTip + MouseLeftButtonUp），视觉明确是 action target。
  - `Views/StatusBarView.xaml.cs`：新增 `ShowException_Click(sender, MouseButtonEventArgs)`
    → new ExceptionDetailDialog { Owner = Window.GetWindow(this), Message = full } → ShowDialog。

## 3. 经验沉淀
- **GrayMatch.Wpf 渲染范式**：旋转框不要走 Cv2 烧入路线——Mat 上画旋转矩形要么
  WarpAffine（慢、占内存），要么 4 段 Line 拼（丑、像素锯齿）。WPF 矢量层 Rectangle
  + RotateTransform 是最干净的方案，缩放也走 LayoutTransform 一致处理。
- **Stretch=Uniform 下的像素→屏幕对齐**：用 TranslateTransform(ox,oy) + ScaleTransform(s,s)
  作为子元素 RenderTransform，公式与 ROI 拖拽共用同一组 scale/offset，不要各算各的。
- **异常展示可点击**：状态栏截断是常态，给用户"点开看完整"的入口比加宽更靠谱；
  弹窗里提供复制按钮方便把堆栈贴到 Bug 反馈里。

## 4. 编译期两个坑（用户截图反馈，dotnet build 漏报）
- **CS1061 "TextBox 不包含 Show"**：`ExceptionDetailDialog.xaml` 里 TextBox `x:Name="MessageBox"`
  会生成同名字段，把 `System.Windows.MessageBox` 类型遮蔽，catch 块里 `MessageBox.Show(...)`
  误解析成 TextBox（无 Show 方法）。
  - 修法：TextBox `x:Name` 改为 `MessageView`，所有 `MessageBox.Text` 改 `MessageView.Text`；
    catch 里用全限定 `System.Windows.MessageBox.Show(...)` 双保险。
- **XDG000 "AngleNegateConverter 不存在于 NoCodeMotion.Views"**：类确实存在并被 dotnet build
  编译通过（0 error / 0 warning）。这是 VS 设计期 XAML IntelliSense 缓存陈旧所致——
  `dotnet build` 只生成运行时 `.g.cs`，不生成设计期 `.g.i.cs`，VS 还在引用旧缓存。
  - 修法：在 VS 里 `Ctrl+Alt+F7` Rebuild Solution 或关掉 VS 重开即可清除。
  - 验证手段：`dotnet build` exit 0 + bin/Debug/net10.0-windows/NoCodeMotion.dll 已生成。
- **教训**：XAML 里控件名禁止与常用类型同名（MessageBox、Button、Canvas、Text…），
  即便只在本类内用，partial 字段也会遮蔽类型导致编译器在 catch 等位置歧义解析。

## 5. 弹窗 StaticResource 解析失败（运行后崩溃）
- **症状**：用户截图显示运行时 XAML 异常
  `Exception: 无法找到名为"SecondaryBackgroundBrush"的资源。资源名称区分大小写。`
- **根因**：`ExceptionDetailDialog.xaml` 用了 `Background="{StaticResource SecondaryBackgroundBrush}"`，
  这个键在本项目（NoCodeMotion）**未定义**（grep 结果：只有 `SurfaceBrush` / `TextSecondaryBrush` /
  `LineBrush` / `TextPrimaryBrush` 等）。新建的弹窗 Window 没有继承 App 资源作用域里某些键。
- **修法**：把 `Background` 改成硬编码 `#F8FAFC`（与 SurfaceBrush 同一白调，区分于外层白卡用 1px
  LineBrush 边框）。后续新建弹窗务必 grep `**\*Dialog.xaml` 复核 StaticResource 键都已在
  `Resources/AppStyles.xaml` 定义，没定义的硬编码或本地声明。
- **教训**：弹窗 Window 不继承主窗口资源（资源作用域止于 App.Resources），新建弹窗前先看
  ConfirmDialog / FlowModifyDialog 等已存在弹窗用了哪些 StaticResource；用不存在的键会在
  ShowDialog 时抛 XAML 解析异常，比编译期 XDG000 还隐蔽（编译过、运行崩）。