# 2026-08-19 工作记录

## Lua 编辑器参照 LuaStudio 重写（AvalonEdit）

用户指向 `D:\wqz\code\LuaStudio\LuaStudio`（AvalonEdit 6.3.1.120 + MoonSharp 2.0.0 的完整 Lua 编辑器）作为参考，要求把 NoCodeMotion 的 Lua 编辑器照此重写，且**调试按钮放到底部**。

### 改动清单
- `NoCodeMotion.csproj`：新增 `AvalonEdit 6.3.1.120`（并 `<NoWarn>NU1701</NoWarn>`，因 net10 引用 net6 库触发框架回退提示）；`Assets/Lua.xshd` 作为 EmbeddedResource。
- `Editing/` 移植自 LuaStudio（命名空间改为 `NoCodeMotion.Editing`，并在每个文件顶部加 `#nullable disable` 以匹配参考实现的 `Nullable disable`，规避可空警告）：
  - `LuaApi.cs`（关键字/标准库/模块成员/片段符号库）、`LuaDocumentAnalyzer.cs`（扫描文档提取用户函数/变量/表字段）、`LuaCompletionData.cs`（ICompletionData）、`LuaIndentationStrategy.cs`（自动缩进）、`LineHighlightRenderer.cs`（整行背景高亮）。
  - `Assets/Lua.xshd` 从 LuaStudio 原样复制。
- `Views/FlowPage.xaml`：Lua 块改造——`<TextBox>` 换成 `<avalonEdit:TextEditor ShowLineNumbers>`；调试工具栏从 Row1 移到 **Row4（底部）**，按钮改用 `Command` 绑定（自动按 CanExecute 启停）并新增「智能提示」按钮（绑定 `LuaHintCommand`）；删除旧 Popup 浮层。因 `EditorPage.Detail` 独立 namescope 禁止 x:Name（MC3093），编辑器**不用 x:Name**，改由代码后台 `FindVisualChild<TextEditor>` 定位。
- `Views/FlowPage.xaml.cs`：重写——加载 xshd 语法高亮、设置缩进策略、挂 `LineHighlightRenderer`（当前行蓝 / 出错行红，订阅 VM `LuaCurrentLine`/`LuaErrorLine`）、`TextChanged` 手动双向同步到 `SelectedItem.LuaSource`（用 `_settingText` 防止程序化赋值回灌）、AvalonEdit 补全窗口（输入字母/`.`/`:` 自动弹出，Ctrl+Space 由 `LuaHintCommand`→`RequestShowCompletion` 事件触发）、`LuaDocumentAnalyzer`+`LuaApi` 提供候选（运行时变量优先级最高）。
- `ViewModels/FlowViewModel.cs`：新增 `event Action? RequestShowCompletion` 与 `ICommand LuaHintCommand`（执行时触发该事件）。

### 验证
- 构建 **0 警告 / 0 错误**（`dotnet build -v m`）。
- 冒烟测试 `timeout 7 NoCodeMotion.exe` 退出码 **124**（干净启动无崩溃）。
- 保留先前已验证的 MoonSharp 调试内核（`Services/LuaDebugger.cs`：运行/单步/跳进/跳出/暂停/停止/1 秒语法检测/点击变量查看值/一键格式化），未改动。

### 关键约束（沿用）
- `Models` 为纯 POCO（`FlowItem.LuaSource` 已实现 INPC），仅 ViewModel 继承 INPC。
- 沙箱中 dotnet 被拦截，构建需 `dangerouslyDisableSandbox + run_in_background`；用户偏好 VS rebuild 验证。
- 文件系统有回退风险，改动后需重读核对。
