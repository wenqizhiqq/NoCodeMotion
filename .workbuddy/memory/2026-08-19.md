# 2026-08-19 工作记录

## Lua 编辑器参照 LuaStudio 重写（AvalonEdit）

用户指向 `D:\wqz\code\LuaStudio\LuaStudio`（AvalonEdit 6.3.1.120 + MoonSharp 2.0.0 的完整 Lua 编辑器）作为参考，要求把 NoCodeMotion 的 Lua 编辑器照此重写，且**调试按钮放到底部**。

### 改动清单
- `NoCodeMotion.csproj`：新增 `AvalonEdit 6.3.1.120`（并 `<NoWarn>NU1701</NoWarn>`，因 net10 引用 net6 库触发框架回退提示）；`Assets/Lua.xshd` 作为 EmbeddedResource。
- `Editing/` 移植自 LuaStudio（命名空间改为 `NoCodeMotion.Editing`，并在每个文件顶部加 `#nullable disable` 以匹配参考实现的 `Nullable disable`，规避可空警告）：
  - `LuaApi.cs`（关键字/标准库/模块成员/片段符号库）、`LuaDocumentAnalyzer.cs`（扫描文档提取用户函数/变量/表字段）、`LuaCompletionData.cs`（ICompletionData）、`LuaIndentationStrategy.cs`（自动缩进）、`LineHighlightRenderer.cs`（整行背景高亮）。
  - `Assets/Lua.xshd` 从 LuaStudio 原样复制。
- `Views/FlowPage.xaml`：Lua 块改造——`<TextBox>` 换成 `<avalonEdit:TextEditor ShowLineNumbers>`；调试工具栏从 Row1 移到 **Row4（底部）**，按钮改用 `Command` 绑定（自动按 CanExecute 启停）并新增「智能提示」按钮（绑定 `LuaHintCommand`）；删除旧 Popup 浮层。因 `EditorPage.Detail` 独立 namescope 禁止 x:Name（MC3093），编辑器**不用 x:Name**，改由代码后台 `FindVisualChild<TextEditor>` 定位。
- `Views/FlowPage.xaml.cs`：重写——加载 xshd 语法高亮、设置缩进策略、挂 `LineHighlightRenderer`（当前行蓝 / 出错行红，订阅 VM `LuaCurrentLine`/`LuaErrorLine`）、`TextChanged` 手动双向同步到 `SelectedItem.LuaSource`（用 `_settingText` 防止程序化赋值回灌）、AvalonEdit 补全窗口（输入字母/`.`/`:` 自动弹出，Ctrl+Space 由 `LuaHintCommand`→`RequestShowCompletion` 事件触发）、`LuaDocumentAnalyzer`+`LuaApi` 提供候选（运行时变量优先级最高）。
- `ViewModels/FlowViewModel.cs`：新增 `event Action? RequestShowCompletion` 与 `ICommand LuaHintCommand`（执行时触发该事件）。

### 验证
- 构建 **0 警告 / 0 错误**（`dotnet build -v m`）。
- 冒烟测试 `timeout 7 NoCodeMotion.exe` 退出码 **124**（干净启动无崩溃）。
- 保留先前已验证的 MoonSharp 调试内核（`Services/LuaDebugger.cs`：运行/单步/跳进/跳出/暂停/停止/1 秒语法检测/点击变量查看值/一键格式化），未改动。

### 关键约束（沿用）
- `Models` 为纯 POCO（`FlowItem.LuaSource` 已实现 INPC），仅 ViewModel 继承 INPC。
- 沙箱中 dotnet 被拦截，构建需 `dangerouslyDisableSandbox + run_in_background`；用户偏好 VS rebuild 验证。
- 文件系统有回退风险，改动后需重读核对。

## Lua 界面整体替换为 LuaStudio 全部代码（决定性指令）

用户最终指令：**"你把原来的lua界面都删除吧，直接全部都使用 D:\wqz\code\LuaStudio\LuaStudio 的全部代码代替就好"** —— 删除 NoCodeMotion 原有 Lua 界面，直接完整搬入 LuaStudio 的 Lua 子系统（UI + 调试内核 + 编辑辅助）。仅保留 NoCodeMotion 应用外壳（列表/表格流程编辑、EditorPage 宿主）。调试按钮仍在底部（沿用先前要求）。

### 移植/新增清单
- `Editing/` 整组从 LuaStudio 原样覆盖（命名空间 `NoCodeMotion.Editing` + `#nullable disable`）：`BreakpointMargin.cs`、`LineHighlightRenderer.cs`、`LuaApi.cs`、`LuaCompletionData.cs`、`LuaDocumentAnalyzer.cs`、`LuaIndentationStrategy.cs`。
  - **修正**：发现先前移植的 `BreakpointMargin` 箭头颜色误用绿色（`0x2E7D32`），LuaStudio 实为黄色执行箭头（`ArrowBrush=0xF2B01E`、`ArrowEdge=0xB57F06`），已覆盖纠正。
- `Services/VarInfo.cs`（新，源自 LuaStudio `Debugging/VarInfo.cs`，命名空间 `NoCodeMotion.Services`）：`VarInfo`/`PauseInfo`/`ExecutionEndedInfo`。
- `Services/LuaDebugSession.cs`（新，源自 LuaStudio `Debugging/LuaDebugSession.cs`）：`LuaDebugSession : IDebugger` 用 MoonSharp 原生行断点（`_debugService.ResetBreakPoints(_sourceCode, lines)` / `SetDebugService` / `SetSourceCode` / `DebuggerCaps.HasLineBasedBreakpoints`），比手动行暂停更稳健；变量/调用栈快照（`BuildLocals`/`SnapshotGlobals`/`Describe`/`FormatValue`）。
  - **新增** `CheckSyntax(string)` 静态方法（满足「1 秒语法检测」要求）：`new Script(CoreModules.Preset_Complete).LoadString(...)` 捕获 `SyntaxErrorException`/`InterpreterException`，返回 `(ok, line, msg)`。
- `Views/LuaEditorView.xaml` + `.xaml.cs`（新，独立 UserControl）：承载 LuaStudio 完整 UI——编辑器卡 + 变量 `TreeView` + 调用栈 `ListBox` + 输出 `ListBox` + 诊断 `TextBlock` + **底部调试工具栏**（BtnRun/BtnPause/BtnStop/BtnStepOver/BtnStepIn/BtnStepOut/BtnBreakpoint/BtnClearBreakpoints/BtnHint + 状态点/计数/光标）。`public FlowItem LuaItem` 依赖属性（`OnLuaItemChanged` 载入文本、清空标记/会话）；`DispatcherTimer(Interval=1s)` 驱动 `CheckSyntaxNow()`。
  - 关键：用独立 UserControl 独立 namescope 规避 `EditorPage.Detail` 的 MC3093（其内 x:Name 会报 MC3093），故 Lua 表面整体封装进 `LuaEditorView` 而非内联。
- `Views/FlowPage.xaml`：删除原 Lua `<Grid>`（Row0–4），换成单行 `<local:LuaEditorView LuaItem="{Binding SelectedItem}" Visibility="{Binding IsKindLua, Converter={StaticResource BoolToVis}}"/>`；表格流程编辑与加流程按钮保留。
- `Views/FlowPage.xaml.cs`：重写为极简外壳（`DataContext = new FlowViewModel()`）。
- `ViewModels/FlowViewModel.cs`：移除所有定制 Lua 成员（`_luaDbg`、`LuaRunCommand`、`LuaVariables`、`LuaDebugStatus`、`_luaCheckTimer` 等），仅保留表格流程模拟 + `IsKindTable`/`IsKindLua`；保留 `using NoCodeMotion.Services;`（ProjectStore 所在）。
- `Services/LuaDebugger.cs`：删除（被移植的 `LuaDebugSession` + `VarInfo` 取代）。

### 构建修复（达成 0/0）
- `FlowViewModel.cs` 补 `using NoCodeMotion.Services;` → 消除 2 处 `ProjectStore` CS0103。
- `LuaEditorView.xaml.cs` 补 `using System.IO;`（修复 `Stream` CS0246）+ 顶部 `#nullable disable`（消除全部 32 个可空警告，与其余移植文件一致）。
- 结果：`dotnet build -v m` → **0 警告 / 0 错误**；冒烟 `timeout 7 NoCodeMotion.exe` → 退出码 **124**（干净启动）。

### 验收
- 移植后自动获得先前待办特性：断点边栏接线、断点驱动暂停、签名智能提示、点击/悬停变量名+值查看。
- 影响面小：定制 Lua 成员仅存在于 `FlowViewModel.cs`/`FlowPage.xaml`/`FlowPage.xaml.cs`/`Services/LuaDebugger.cs`，删除前已确认。

## Lua 示例脚本（用户指令"lua示例脚本放上去"）

- `Models/FlowItem.cs`：`DefaultLuaTemplate` 常量由 3 行占位升级为**可运行的示例脚本**，覆盖变量、表格、`local function`、for 循环、if 条件、`print`（接输出面板）、`return true`；新建议脚本流程（Kind=Lua）直接以此预填编辑器。
- `Views/LuaEditorView.xaml` + `.xaml.cs`：底部调试工具栏新增「插入示例」按钮（`BtnExample` → `BtnExample_Click`），把 `DefaultLuaTemplate` 载入当前编辑器并写回 `LuaItem.LuaSource`（用 `_settingText` 防止回灌），CLI/语法检测即时刷新。
- 构建 **0 警告 / 0 错误**；冒烟测试退出码 **124**（干净启动）。
- 备注：`print` 已由 `LuaDebugSession` 的 `_script.Options.DebugPrint` 接至输出面板，示例脚本的 print 会正常显示。

## Lua 编辑器：变量语义着色 + 调试按钮闪烁修复（用户指令）

### 1) 变量/其他元素的颜色区分
- 新增 `Editing/LuaSemanticColorizer.cs`（`DocumentColorizingTransformer`）：在语法高亮（关键字/字符串/数字/注释）之上，对用户符号（`LuaDocumentAnalyzer.Analyze`）与标准库（`LuaApi.Globals`）着区别色：
  - 变量 / 表字段（`SymbolKind.Variable/Field`）：青绿 `#0E7C7B`；
  - 函数（`SymbolKind.Function`）：棕 `#795E26`（与 xshd `FunctionCall` 一致）；
  - 标准库全局名（`print`/`table`/`math`…）：蓝绿 `#1F7A8C`（与 xshd `StdLib` 一致）。
  - 仅对不在注释/字符串内的标识符着色（行内先做长度保持的 mask）；按文档文本缓存符号集合避免每行重复分析。
- `Views/LuaEditorView.xaml.cs` 的 `SetupEditor` 中注册：`view.LineTransformers.Add(new LuaSemanticColorizer());`（在 `TextView view` 声明之后）。

### 2) 调试时按钮闪烁的原因与修复
- **原因**：NoCodeMotion 调试按钮用 HandyControl 的 `TtBaseBtn/TtWarnBtn/TtDeleteBtn/TtGrayBtn`，这些样式在 `IsEnabled` 变为 false 时会把**彩色背景换成灰色背景**；而调试运行时 `SetSessionState` 会在每次单步 / 每次快速运行结束（脚本毫秒级跑完）时反复切换 `IsEnabled`，于是彩色→灰→彩反复切换，视觉上闪。LuaStudio 不闪是因为它的 `ToolButton`（App.xaml）背景是 `Transparent`，禁用时仅 `Opacity=0.35` 淡化、不换底色、无动画。
- **修复**：`Views/LuaEditorView.xaml` 的 `UserControl.Resources` 新增 `LuaDbgButton` 样式（参照 LuaStudio `ToolButton`：透明背景、悬停浅蓝、按下略深、禁用 `Opacity=0.4`、无动画、并 `Focusable=False` 防止点击后焦点抢走导致的二次闪烁），并把底部 10 个调试按钮（运行/中断/停止/单步/步入/步出/断点/清除断点/智能提示/插入示例）全部改用该样式；输出面板的「清空」按钮保留原 `TtGrayBtn` 不变。
- 构建 **0 警告 / 0 错误**；冒烟测试退出码 **124**（干净启动）。

### 验证要点
- 着色是纯静态渲染叠加，不影响 `TextChanged` 写回 `LuaItem.LuaSource`、语法检测、断点、单步逻辑。
- 着色颜色刻意与现有 `Lua.xshd` 的 `FunctionCall`/`StdLib` 保持一致，避免同一标识符两种色。

## 调试按钮改成明显彩色 + Lua 颜色对齐 LuaStudio（用户指令）

### 1) 底部调试按钮改为明显彩色
- `Views/LuaEditorView.xaml` 的 `LuaDbgButton` 样式改为**实色背景 + 白字**：默认 `Background` 占位色，悬停/按下用白色半透明叠层（Opacity 0.16/0.30）提亮，禁用仅整体 `Opacity=0.45` 淡化（**不换灰底**，故仍不闪）。
- 每个调试按钮加 `Background` 语义色：运行 `#2E7D32`(绿)、中断 `#E69500`(琥珀)、停止 `#C62828`(红)、单步/步入/步出 `#0F6CBD`(蓝)、断点 `#E53935`(红)、清除断点 `#7A8794`(石板灰)、智能提示 `#8E44AD`(紫)、插入示例 `#0E7C7B`(青)。
- 去掉按钮内 glyph 的显式 `Foreground`（▶❚❚■↷↴↰●○✦📄），改由按钮 `Foreground=White` 继承，白图标 + 白文字在彩色底上清晰。

### 2) Lua 编辑器颜色对齐 LuaStudio
- 复查 `D:\wqz\code\LuaStudio\LuaStudio`：其 `Lua.xshd` 与 NoCodeMotion 的 `Assets/Lua.xshd` **逐字相同**，编辑器 `Background=White`、`FontSize=14`、行号 `#9AA5B1` 也与本应用一致 —— 语法高亮颜色本就与 LuaStudio 一致。
- 在原有“变量/函数/标准库”语义着色基础上，把**变量色**从青绿 `#0E7C7B` 改为鲜明蓝 `#0F6CBD`，与标准库青绿 `#1F7A8C` 区分开；函数棕 `#795E26`（=xshd FunctionCall）、标准库青绿 `#1F7A8C`（=xshd StdLib）保持与 LuaStudio 配色一致。
- 构建 **0 警告 / 0 错误**；冒烟测试退出码 **124**（干净启动）。

### 3) 控制流关键字（if 等）单独上色
- 用户要求 `if` 等控制流关键字也要有独立颜色。原 `Lua.xshd` 把所有关键字都归到 `Keyword`(蓝 #0000C0)，不区分。
- 新增 `ControlFlow` 颜色（橙色 #C2410C 加粗），把 `if/then/else/elseif/end/for/while/do/repeat/until/return/break/function/local` 拆到独立 `<Keywords color="ControlFlow">`；`and/or/not/in/goto` 留在原 `Keyword`(蓝)。
- 与既有语义着色不冲突：变量蓝、函数棕、标准库青、控制流橙，四者分明。
- 构建 0 警告 / 0 错误；冒烟测试退出码 124（干净启动）。

### 4) 控制流着色未生效的修复（colorizer 双保险）
- 用户反馈改了 `Lua.xshd` 把 `if/for/return/local` 拆到 `ControlFlow` 橙色后，编辑器里这些关键字"颜色没有变化"（仍呈深蓝/暗色）。
- 经查 dll 已正确嵌入新 xshd（ControlFlow/C2410C 各出现，`if</Word>` 仅 1 次在 ControlFlow 块中），高亮器也在生效（注释/字符串/数字均带色）。问题最可能是：用户正在运行的进程持有旧的内存中 xshd（dll 替换需要重启 App 才能让 HighlightingManager 拿到新定义）。
- 修复：在 `Editing/LuaSemanticColorizer.cs` 增加**独立于 xshd** 的控制流着色通路——用 `\b(if|then|else|elseif|end|for|while|do|repeat|until|return|break|function|local)\b` 正则在注释/字符串遮罩后的行上匹配，刷橙色 `#C2410C` 并加粗（沿用当前 run 的 FontFamily，避免字体替换）。
- 这样即便 xshd Keywords 规则因任何原因（缓存/未重启/HighlightingManager 单例）不生效，语义着色器也会保证 `if` 等以醒目橙色显示。
- 构建 0 警告 / 0 错误；冒烟测试退出码 124。
- 注意：用户需**完全关闭旧 NoCodeMotion.exe 进程并重启**，新 dll（带 colorizer 双保险）才能生效。

### 5) 气缸页面改苹果风 + 内容扩充
- 用户要求：气缸页面内容尽量多，使用苹果样式颜色。
- `Models/CylinderItem.cs` 大幅扩充字段（共 ~23 个可绑定属性）：DeviceId/Type(单/双作用)/Action/InitialState/Remark/OutPoint/SensorExtend/SensorRetract/SensorType(NPN/PNP)/BackupSensor/DelayMs/ExtendMs/RetractMs/ExtendSpeed/RetractSpeed/ToleranceMs/Interlock/DoubleCoil/AlarmEnable/ManualEnable/TimeoutMs/PulseOutput/PulseWidthMs/LinkedAxis，全部用 SetField 持久化。
- `ViewModels/CylinderViewModel.cs` 增加药丸可选项 string[]：TypeOptions/SensorTypeOptions/InitialStateOptions/ActionOptions。
- `Views/CylinderPage.xaml` 重写为 Apple 风分组卡片：灰底 CanvasBrush + 5 张白色圆角 AppleGroupCard（基本信息/IO/动作参数/安全逻辑/高级），卡内 AppleSectionHeader 小标题 + AppleRow（标签左/控件右）+ AppleHairline 发丝线分隔；类型/感应/动作用药丸 ApplePillList，开关用 AppleToggle（iOS 风），数值用 AppleTextField+AppleUnit(ms/%)；IO/关联轴用 App.xaml 既有 CellComboStyle 扁平下拉。复用 App.xaml 既有 Apple 调色板（AccentBrush/SurfaceBrush/LineBrush/TextPrimary/Muted/SuccessBrush/SoftShadow 等），`UserControl.Resources` 内嵌 Apple 样式块，未改动全局资源。
- 构建 0 警告 / 0 错误；冒烟测试退出码 124（干净启动）。注意：页面级 XamlParseException 只在真正打开该页时抛，需在 VS 实际打开气缸页确认观感。

### 6) 气缸页面去滚动条 + 紧凑布局
- 用户要求：不要滚动条，尽量布局紧凑。
- `Views/CylinderPage.xaml` 重写 Detail 区：移除原 `ScrollViewer`（这是之前滚动条来源），改为 **两列 Grid**（左列=基本信息+动作参数 12 字段，右列=IO配置+安全逻辑+高级 13 字段，高度近乎平衡）。
- 收紧间距：卡片间距 16→10、行内边距 14,10→12,6、卡内小标题 12→11.5、字段字号 13→12.5、数字框宽 80→60、下拉高 32→28、药丸 padding/圆角缩小。
- 确认 `EditorPage.Detail` 由 `*` 高度 Border 约束且本身无滚动容器（EditorPage.xaml 注释："表单型页各自包一层 ScrollViewer"），故移除气缸页 ScrollViewer 后整页不再有滚动条；常规编辑窗口高度内两列内容可完整显示。
- 构建 0 警告 / 0 错误；冒烟测试退出码 124（干净启动）。

### 7) 全局统一苹果风样式与配色（用户指令："整个软件都做成这种苹果的样式和颜色布局"）

- 把 CylinderPage/CommPage 的本地 `Apple*` 样式提升到 `App.xaml` 全局（AppleGroupCard / AppleRow / AppleLabel / AppleTextField / AppleUnit / AppleHairline / AppleNumBox / AppleCombo / ApplePillList / AppleToggle / AppleChip / AppleChipLabel / AppleNumField / AppleBtn / AppleBtnSecondary / AppleTerminal）。
- App.xaml 新增 `SuccessSoftBrush(#D7F0DD)` 供料盘"占用"单元态使用；修复 `ApplePillList` 的 `ItemContainerStyle` 内 XML 嵌套错误（MC3000，补缺失 `</Setter>` 并移除错位 `</Style>`）。
- `CylinderPage.xaml` / `CommPage.xaml`：删除本地 `Apple*` 样式块（仅保留 CommPage 的 `CommTypeVisibilityConverter`），改引用全局。
- `AxisPage.xaml` 重写：5 张 AppleGroupCard（基本信息/运动参数/回零/限位保护/电平编码）分组 + 发丝线 + AppleToggle(iOS 开关) 替代默认 CheckBox + CellComboStyle 下拉 + MaxWidth=720 ScrollViewer + CanvasBrush 灰底。
- `TrayPage.xaml` 重写：10 处硬编码蓝灰(#DDE3EA/#F4F7FB/#5A6B7B/#8895A5/#37B24D/#D7F0DD…) → 调色板(LineBrush/HoverBrush/TextSecondary/TextMuted/SuccessBrush/SuccessSoftBrush)，分组卡 + 灰底 + ScrollViewer；图例同步去硬编码。
- `FlowPage.xaml`：Detail 内容外包 `CanvasBrush` 灰底 Border；3 处硬编码浅蓝(#FAFBFC/#DDE9F7/#F2F7FE) → HoverBrush/AccentSoftBrush。
- `IoPage.xaml`：IoSectionTitleStyle `#2B3A4B`→`TextPrimaryBrush`、`Bold`→`SemiBold`。
- `EngineerPage.xaml`：占位椭圆 `Fill="#C7C7CC"`→`TextMutedBrush`。
- `ConfirmDialog.xaml` / `RenameDialog.xaml`：`Background="White"`→`SurfaceBrush`（卡片与输入框）。
- 构建 **0 警告 / 0 错误**；冒烟测试退出码 **124**（干净启动）。全局 Apple 调色板与共享样式现集中在 App.xaml，所有页统一引用。

### 8) 布局自适应紧凑、去滚动条（用户指令："布局尽量自适应紧凑，不需要滚动条"）

- `AxisPage.xaml`：移除整页 `ScrollViewer` + `MaxWidth=720 StackPanel`；改用 `WrapPanel`(Orientation=Horizontal, ItemWidth=372, Background=CanvasBrush) 自适应重排 5 张分组卡（列数随详情区宽度增减），卡加 `Margin="0,0,12,10"`，紧凑无滚动条。
- `TrayPage.xaml`：移除整页 `ScrollViewer` 与料盘内 `ScrollViewer(MaxHeight=380)`；改为 2 列 `Grid`（左=料盘参数卡 Width=300、右=二维料盘卡 `*`），二维单元格 `ItemsControl` 外包 `Viewbox`(Stretch=Uniform, StretchDirection=DownOnly) 填满可用区并随窗口缩放，无滚动条；图例加 `DockPanel.Dock=Top`、料盘卡内部改 `DockPanel LastChildFill`。
- `EngineerPage.xaml`：删除 3 处内部卡片 `ScrollViewer`（轴控制/气缸控制/IO 控制），内容直接填充卡片，依赖内部 `WrapPanel` 自适应重排。
- `OperatorPage.xaml`：左侧整列 `ScrollViewer` → 直排 `StackPanel`；固定 640×220 折线图 `Canvas` 外包 `Viewbox`(Stretch=Uniform, StretchDirection=DownOnly) 自适应缩放，无外部滚动条。
- `LuaEditorView.xaml`：编辑器内部滚动保留（代码编辑器，滚动是预期行为）。
- 构建 **0 警告 / 0 错误**；冒烟测试退出码 **124**（干净启动）。
- 注意：控制台页（Engineer/Operator）去内部滚动后，项数极多时卡片内可能裁切而非滚动；属"尽量自适应"取舍，如用户要求可针对特定面板恢复滚动。

### 9) 工程师页 4 个子页面内部统一加滚动条
- 用户要求：工程师页面的 4 个子页面内部都需要滚动条（覆盖第 5 节"去内部滚动"的取舍）。
- `EngineerPage.xaml` 结构：外层 2 列网格（左列=轴控制+气缸控制 各 *；右列=IO 控制+点位移动 各 *），4 块均为 Border(CardBorderStyle)+DockPanel(标题 Dock=Top)+内容区。
- ①轴控制：清理之前残留的脏 StackPanel 嵌套，改为「标题(Top) + 寸动/JOG 步长(Dock=Bottom 固定) + ScrollViewer(中间填满)」；轴卡片流 ItemsControl/WrapPanel 入 ScrollViewer(垂直 Auto / 水平 Disabled) 内部滚动。
- ③气缸控制：Cylinders 的 ItemsControl/WrapPanel 外包 ScrollViewer 内部滚动。
- ②IO 控制：输入/输出 IO 两个列表所在的内部 Grid 整体外包 ScrollViewer（垂直 Auto / 水平 Disabled）内部滚动。
- ④点位移动：本就是 DataGrid，自带内部滚动（父 Border 高度受 Grid.Row=* 约束），已满足，未改。
- 修复 XAML 错误：③气缸按钮 `RelativeSource="{RelativeSource AncestorType=ItemsControl}"` 带内层引号会导致 XML 非法（引号提前结束属性值），改为无引号嵌套 `RelativeSource={RelativeSource AncestorType=ItemsControl}}`；该文件相关按钮的 RelativeSource 现统一为无引号写法。
- 构建 **0 警告 / 0 错误**；冒烟测试退出码 **124**（干净启动）。

## 无头截图 harness + 闲鱼文案（用户指令："每个界面都截图放在一个文件夹，写一段几百字闲鱼文案"）

### 做法（可复用，已沉淀为流程）
- 问题：沙箱无显示器，无法用 `MainWindow()` 实际拉起窗口截图；WPF `Application` 是 AppDomain 单例，截图工程若直接加载 `App.xaml` 会因两个 `Application` 实例报 `XamlParseException`。
- 解法：
  1. 把 `App.xaml` 内全部资源（调色板/画刷/阴影/几何图标/转换器/所有 Style·DataTemplate）抽到纯 `ResourceDictionary` **`Resources/AppStyles.xaml`**（无 `x:Class`），`App.xaml` 仅 `MergedDictionaries` 引用它。
  2. 在 **`D:\wqz\code\Snap`**（NoCodeMotion 同级、不被主工程编译）新建独立 `Exe` 工程，`ProjectReference` 引用 `..\NoCodeMotion\NoCodeMotion.csproj`，合并 `pack://application:,,,/NoCodeMotion;component/Resources/AppStyles.xaml`。
  3. harness `Program.cs`：`[STAThread] Main` → `new Application()` → `ProjectStore.Load()` + 自己实现的 `SeedDemoData()`（填轴/气缸/通讯/料盘/IO/点位/变量/流程等示例数据，`Catalog.SyncAllFromData`）→ 用 `RenderTargetBitmap(1440,900,96,96,Pbgra32)` + `PngBitmapEncoder` 渲染 11 个页面到 `D:\wqz\code\NoCodeMotion\Screenshots\`。
  4. 渲染前 `Measure→Arrange(new Rect(0,0,W,H))→UpdateLayout→Render`；元素外包 `Grid` 宿主（W=1440,H=900,Background=CanvasBrush），`Horizontal/VerticalAlignment=Stretch`。
- 关键坑：`AppStyles.xaml` 的 `CellComboStyle` 与 `ApplePillList` 的 `ItemContainerStyle` 内 `<Setter>` 缺 `</Setter>`（MC3000），用 Python `xml.etree` 校验并补闭合后通过。
- 构建：dotnet 在沙箱被当 LOLBin 阻断 → 用 `"/c/Program Files/dotnet/dotnet" build` + `dangerouslyDisableSandbox:true` + `run_in_background:true`，**0 警告 / 0 错误**，11 张 PNG 全部生成（大小 23KB~112KB 不等，非平凡）。
- 说明：模型无法读取 PNG（"does not support images"），截图正确性以字节大小+已知设计推断，未做视觉确认；`05_流程编辑.png` 右侧 Lua 区空白（默认选中 Table 流程无步骤）、`04_料盘.png` 料盘网格稀疏均为 demo 预期，非缺陷。

### 交付物
- 截图文件夹：`D:\wqz\code\NoCodeMotion\Screenshots\`（11 张：`00_主界面` + `01_轴配置`～`10_变量表`）。
- 闲鱼文案：`Screenshots\闲鱼文案.txt`（约 400 字，主推"WPF 苹果风运动控制上位机全套可运行源码"，含 10 大功能模块卖点，附 11 张界面截图所见即所得）。

## 跳转按钮改成"跳转到鼠标所在行"（FlowPage 底部工具栏）

用户要求：流程编辑页底部"跳到行 [输入框] 跳转"控件改成"跳转到鼠标所在行"——即点击按钮时直接跳到用户在右侧步骤表格里点中的那一行，不再手动输入行号。

### 改动
- `Views/FlowPage.xaml`（原 346–354 行）：删除"跳到行" `TextBlock` + 行号 `TextBox` + 旧"跳转"按钮；替换为单个 `Button` 居中放在原 Grid 列 2，`Content="跳到选中行"`、`Command="{Binding JumpCommand}"`。
- `ViewModels/FlowViewModel.cs`：
  - 删除字段 `private int _jumpRow = 1;` 与属性 `public int JumpRow { get; set; }`（不再需要行号绑定）。
  - 新增 `public bool CanJump => !IsRunning && StepPanel.SelectedItem != null;`，用于按钮 CanExecute。
  - `JumpCommand` 改为 `new RelayCommand(_ => JumpToRow(), _ => CanJump)`，按钮自动在未选中步骤或运行中时灰掉。
  - `JumpToRow()` 改为取 `StepPanel.SelectedItem` 在 `StepPanel.Items` 中的索引赋值给 `CurrentStep`（用本地变量 `var target` 收住以消除 CS8604 nullable 警告）。
  - `RaiseRunState()` 追加 `OnPropertyChanged(nameof(CanJump));` 以在开始/停止运行时刷新按钮可执行态。

### 验证
- `dotnet build` 走 PowerShell + Windows 路径（Git Bash 路径 `/d/...` 会被 dotnet 当成非法开关报 MSB1001；且 `UseArtifactsOutput=true` 是误传，真实属性是 `UseArtifactsPath`，需先有 `Directory.Build.props`，否则 NETSDK1200）→ **0 警告 / 0 错误**。
- 冒烟测试 4s 不退出（启动正常，无运行期异常）。
- 行为：未选中步骤时按钮灰；选中任一非空步骤后按钮可用，点击即把 `CurrentStep` 跳到该步骤索引（流程执行高亮、状态文案同步刷新）。

### 备注
- RelayCommand 通过 `CommandManager.RequerySuggested` 监听 UI 交互事件，`StepPanel.SelectedItem` 变化（用户点击不同行）会自动触发按钮重评估 CanExecute，无需手动 raise，与项目里 `DeleteCommand` 等同模式一致。
- `dotnet build` 在沙箱被视为 LOLBin 阻断，必须 `dangerouslyDisableSandbox:true` + `run_in_background:true`；走 PowerShell 才能正确处理 Windows 风格项目路径。

## 流程步骤"耗时(ms)"列改为只读蓝色（与"实际值"列一致）

用户要求：流程编辑页 FlowStep 表格里"耗时(ms)"列也跟"实际值"一样是只读字段，统一蓝色调。

### 改动
- `Views/FlowPage.xaml` 第 287–294 行（`耗时(ms)` 的 `DataGridTemplateColumn`）：
  - 原结构：`CellTemplate` 是可编辑 `TextBox`（绑定 `DurationMs`）+ `FlowCellTextBoxStyle`。
  - 新结构：套用"实际值"列的只读蓝色模板——
    - `HeaderStyle`：基于 `FlowHeaderStyle`，`Background=AccentSoftBrush`、`Foreground=AccentDarkBrush`（与"实际值"列头完全一致）。
    - `CellTemplate`：去 TextBox 改为 `Border(Margin=1, CornerRadius=4, Background=AccentSoftBrush)` 包 `TextBlock(HorizontalAlignment=Center, VerticalAlignment=Center, Margin=6,0, Foreground=AccentDarkBrush, FontWeight=SemiBold, FontSize=12, Text={Binding DurationMs})`。数字居中显示，去掉"实际值"左侧的小圆点（那是 ActualValue 的标识符，整数不需要）。
- 没改 `FlowStep.cs`（`DurationMs` 仍是 public 双向属性，便于执行器回填结果）。
- 没改 `ExcelBatchEdit.cs`（导出导入范围保留 DurationMs/ActualValue，与本次 UI 只读化无关；如后续要禁止 Excel 改这俩字段，再单独调整 Headers/ExportProps 即可）。

### 验证
- `dotnet build` **0 警告 / 0 错误**。
- 冒烟测试 4s 不退出，启动正常。

### 备注
- "蓝色调"标准组合（项目里复用）：`AccentSoftBrush` 背景 + `AccentDarkBrush` 前景 + SemiBold + 字号 12 + Border Margin=1 CornerRadius=4，与"实际值"列头单元完全对齐，便于用户在两列之间视觉扫读。
- DataGridTemplateColumn 默认不是只读的（要靠 CellTemplate/IsReadOnly 控制），单纯把 TextBox 换成 TextBlock 即天然只读（TextBlock 不接受输入），同时样式立刻传达"这是系统回填值"语义。
