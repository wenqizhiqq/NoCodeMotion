# 2026-09-09 工作日志

## 说明书页（OperatorManualPage）多角色化 + 工程列表时间排序
- 需求 C 落地：
  1. **工程列表按时间倒序**：`Services/ProjectManager.cs` 的 `ListProjectEntries()` 移除 `.OrderBy(n => n)`（按名称），改为先收集全部 `ProjectEntry` 再 `list = list.OrderByDescending(e => e.UpdatedAt ?? e.CreatedAt).ToList();`，最新修改的工程排最上面。
  2. **说明书支持多角色**：`Views/OperatorManualPage.xaml.cs` 重构为数据驱动——`OperatorManualPage : UserControl, INotifyPropertyChanged`，新增 `Roles`(操作员/工程师/维护/管理员)、`CurrentRole`、`HeaderText`、`Sections`(List<ManualSection>)；`BuildSections()` 按角色取章节列表（每行按 \n 切分去空行）。原 S1-S15 操作员内容完整保留；新增 工程师(12 节)/维护(9 节)/管理员(6 节) 说明书。XAML 顶部加 `ListBox` 角色分段选择器（选中态 AccentBrush 蓝底白字），正文用单个 `ItemsControl` 绑定 `Sections`，消除原本 15 段重复 Border。
  3. **弹窗版同步**：`Views/OperatorManualDialog.xaml` 改为内嵌 `<local:OperatorManualPage/>`，复用多角色内容（原硬编码操作员文案删除）。
- 构建：PowerShell + 绝对 `C:\Program Files\dotnet\dotnet.exe` 前台构建，`-c Debug`，**0 错误**（590 历史警告，均与本改动无关）。
- 说明：操作员页内作者署名保留为「软件由 温启志 编写（微信 18719361399），保留所有权利。」（原零宽混淆串改为可读形式，规避编码损坏；规范作者水印仍在 AuthorWatermark.cs / MainWindow.xaml / Docs/*.md 三处）。
