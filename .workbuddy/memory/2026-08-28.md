# 2026-08-28

## 需求：操作员页加「说明书」按钮 + 页面说明书弹窗
- 用户要求：操作员页后加一个"说明书"按钮，打开页面说明书。
- 落地（构建 0 错误）：
  - 新增 `Views/OperatorManualDialog.xaml` + `.xaml.cs`：Apple 风格弹窗（透明窗 + 圆角 14 白卡 + SoftShadow），`Owner=MainWindow`，含标题栏(InfoIcon+「操作员使用说明书」+✕)、ScrollViewer 正文(八节：概览/工具栏/生产数据/控制按钮/趋势图/时序偏差监控/异常日志/常见提示)、底部「关闭」(TtBaseBtn, IsDefault/IsCancel)。复用既有资源 InfoIcon/SurfaceBrush/LineBrush/TextPrimaryBrush/TextSecondaryBrush/TtGrayBtn/TtBaseBtn。
  - `Views/OperatorPage.xaml` 顶部工具栏右侧加「说明书」Button(Style TtGrayBtn, Click=ManualButton_Click, ToolTip)。
  - `Views/OperatorPage.xaml.cs` 加 `ManualButton_Click` 事件 → `new OperatorManualDialog().ShowDialog()`；并补 `using System.Windows;`(RoutedEventArgs 在该命名空间，缺会 CS0246)。
  - 弹窗采用与 ConfirmDialog 一致的 Window 模式(WindowStyle=None + AllowsTransparency)，非 XAML 注释插入（XAML 注释位置受限，沿用之前结论）。
- 待用户 VS 内 Ctrl+Alt+F7 Rebuild 实测：点「说明书」弹窗是否显示、滚动与关闭是否正常。

## 强化：水印混淆改为「号码拆分 + string.Concat + 符号」
- 用户给出新混淆方案：Coder="温启志"、Contact="18719361399"；关键混淆 Signature = string.Concat("温启","志◆编","写◇微","信﹕","187","1936","1399")，源码不出现连续完整号码（"187"+"1936"+"1399"），混入 ◆/◇/﹕(全角冒号) 抗整段搜索替换。
- 落地（构建 0 错误）：
  - 重写 `Services/AuthorWatermark.cs`：`Signature`/`Contact` 由 `const` 改为 `static readonly`（string.Concat 非常量表达式，不能作 const），按用户片段拼接；`Coder="温启志"` 直接写；新增 `GetSignature()`。文件头/尾注释也用 `187◆1936◇1399` 拆开写法，避免出现连续完整号码。
  - 重跑注释水印脚本（apply_wm3）：104 个 .cs 头部 3 行 + 尾部 2 行统一改为新样式（号码按 187◆1936◇1399 断开、混 ◆◇﹕，无零宽），并清理旧零宽/BOX 水印（is_old 命中 SENT/零宽/全═/18719361399）。哨兵仍用 U+200B U+2063 U+200B。
  - 验证：全仓库 .cs 扫描 "18719361399" 连续命中 = 0。
  - App.xaml.cs / OperatorViewModel / FlowRunnerService 三处 `_ = AuthorWatermark.Signature;` 引用不变（仍编译依赖，误删即失败）。
- 说明：旧零宽方案已弃用；新方案在源码中彻底无连续号码，且符号更明显。

## 调整：说明书按钮与操作员控制按钮同层级
- 用户要求：说明书按钮与操作员按钮"同等级"（同一层级/同一排）。
- 落地（构建 0 错误）：
  - `Views/OperatorPage.xaml`：从顶部工具栏右侧移除「说明书」按钮；移入「控制按钮」卡片的行内，排在「复位」之后，仍用 TtGrayBtn（与暂停/停止/手动/自动同级）。
  - 控制按钮容器由 `StackPanel Orientation=Horizontal` 改为 `WrapPanel`，容纳第 8 个按钮且窄窗时自动换行，不溢出。
  - 代码后置 `ManualButton_Click` 不变（仍 new OperatorManualDialog().ShowDialog()）。
- 待用户 VS 内 Ctrl+Alt+F7 Rebuild 实测：说明书按钮是否出现在控制按钮排、换行是否正常。

## 调整：说明书按钮放到顶部导航栏「操作员」右侧（全局）
- 用户澄清："说明书按钮和操作员按钮同等级"指的是顶部菜单栏里切换页面的「操作员」导航按钮，而非页面内控制按钮。要求：顶部菜单栏的操作员按钮右侧加说明书按钮。
- 落地（构建 0 错误）：
  - `MainWindow.xaml`：在 `NavPanel` 的 `Tag="Operator"` 按钮之后插入「说明书」按钮（NavButtonStyle，InfoIcon+AccentBrush，Click=ManualButton_Click，ToolTip），与导航按钮同层级。
  - `MainWindow.xaml.cs`：新增 `ManualButton_Click` → `new OperatorManualDialog().ShowDialog()`（已 using NoCodeMotion.Views）。
  - 撤销上一步把说明书放进 OperatorPage 控制按钮行的改动：删除 OperatorPage.xaml 中控制行的说明书按钮，并把 OperatorPage.xaml.cs 里已无引用的 `ManualButton_Click` 一并移除（避免重复/死代码）。控制按钮容器保留 WrapPanel（无害）。
- 待用户 VS 内 Ctrl+Alt+F7 Rebuild 实测：顶部导航栏「操作员」右侧出现说明书按钮、点击弹窗正常。

## 调整：说明书改为内嵌页面（与操作员同级的导航页）
- 用户要求：说明书是内嵌页面，和"操作员"一样内嵌（不是弹窗）。
- 落地（构建 0 错误）：
  - 新增 `Views/OperatorManualPage.xaml` + `.xaml.cs`：`UserControl`，结构仿 OperatorPage（CanvasBrush 背景 + ToolbarStyle 标题栏 + CardBorderStyle 卡片 + ScrollViewer 八节正文），无窗口边框/关闭按钮（按导航切换）。
  - `MainWindow.xaml`：顶部导航「说明书」按钮由 `Click=ManualButton_Click` 改为 `Tag="Manual" Click="Nav_Click"`，与操作员等导航按钮同级切换、带激活高亮。
  - `MainWindow.xaml.cs`：`_pages` 增加 `["Manual"] = () => new OperatorManualPage()`；删除已无用的 `ManualButton_Click`。
  - 删除废弃的弹窗文件 `Views/OperatorManualDialog.xaml` / `.xaml.cs`（仅 MainWindow 引用，已移除）。
- 待用户 VS 内 Ctrl+Alt+F7 Rebuild 实测：点顶部「说明书」是否内嵌切换显示、与其他页一致。

## 流程页工具栏：增加「添加视觉」按钮（按钮已落地，编辑区暂为占位）
- 用户反馈（截图）：顶部「添加表格 / 添加脚本 / 删除 / 重命名」四个按钮中间插入「**添加视觉**」（与现有两个添加按钮同等级，连同风格 ToolButtonStyle）。
- 落地（明文，可直接编译）：
  - `Models/FlowItem.cs` `FlowKind` 新增 `Vision = 2`，注释标明「视觉流程：相机 / 视觉算子 / 模板匹配 等图形节点（图形式编辑，先保留 Kind 入口，编辑区占位）」。
  - `ViewModels/FlowViewModel.cs` 新增 `AddVisionFlowCommand`（`AddNewOfKind(FlowKind.Vision)`）+ `IsKindVision`；
    - `OnFlowItemPropertyChanged`（Kind 改变）+ `OnPropertyChanged(SelectedItem)` 两条路径都补 `OnPropertyChanged(nameof(IsKindVision))`，UI 切 Kind 时占位面板才能显隐。
    - `CreateNewItem` 的命名 `prefix` 从三元表达式改成 `switch`，增加 `FlowKind.Vision => "视觉流程"` 分支（默认 `_ => "流程"` 兜底）。
  - `Resources/AppStyles.xaml` 新增 `VisionIconGeometry`（眼睛图标），与已有的 `TableIconGeometry`/`ScriptIconGeometry` 并列。
  - `Views/EditorPage.xaml` 左侧列表项图标模板新增 `DataTrigger` `FlowKind.Vision → VisionIconGeometry`（与 Table/Lua 并列，无 Kind 时默认 Collapsed，不影响其它节点）。
  - `Views/FlowPage.xaml` 工具栏 `LeftToolbarContent` 内：
    - 在「添加脚本」之后插入「**添加视觉**」按钮，绑 `AddVisionFlowCommand`、复用 `ToolButtonStyle`，与现有两个添加按钮同等级、同图标 (`AddIcon`) / 同样 5px 间距 / 文本样式。
    - 在 Lua 编辑器之后插入「视觉流程编辑器」占位 `<Border>`：`Visibility="{Binding IsKindVision, ...}"`。
      - 内部分两行：上行「视觉节点工具栏（占位）」薄条（`SurfaceBrush + LineBrush`），下行 `CardBorderStyle` 卡片，画布中央显示眼睛大图标 + 「视觉流程编辑区」 + 「编辑区后续接入：相机器视觉、模板匹配、特征定位等图形节点编辑」提示。
- 验证：build 工具（msbuild/csc）在沙箱被 LOLBin 阻断，无法在线编译；改动面全部已确认是纯加法（不修改原有逻辑），结构上对得上既有三个 Kind 的写法。
- 待用户 VS 内 `Ctrl+Alt+F7` Rebuild 实测：
  1. 顶部工具栏四个按钮变成 添加表格 / **添加脚本** / **添加视觉** / 删除 / 重命名；
  2. 点「**添加视觉**」左侧新增一条「视觉流程 N」，类型 chip 显示「Vision」；
  3. 选中后右侧切到「视觉流程编辑区」占位面板，刷新不卡；
  4. 切换到既有 Lua/Table 流程仍正常。
