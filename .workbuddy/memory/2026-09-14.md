# 2026-09-14 工作日志

## 新增半导体工程模板（新建工程弹窗）
- 需求：新建工程例子加入半导体项目——固晶机、探针台、平移式分选机；共用设备清单：
  3 自动盘 + 3 手动盘 + 上料盘 + 下空盘 + 2 高温中转盘（共 10 盘）、
  2×4 上料吸嘴 + 2×4 下料吸嘴（共 16 真空吸嘴）、搬运料盘手臂 3 轴、2 个 Z 轴压力测、GPIB 测试机通讯获取 Bin。
- 改动文件：`Services/ProjectTemplateCatalog.cs`、`Views/NewProjectDialog.xaml`。
- 实现：新增「半导体」分类 + 3 个模板（die-bonder / probe-station / transfer-handler）；
  提取共用助手 `AddSemiEquipment(ProjectData)` 一次性填好 3 控制卡 + 5 轴 + 16 入 12 出 + 16 吸嘴 + 10 盘 + GPIB 通讯 + 8 变量；
  各模板再叠加相机(GPIB/对位)与专属主流程+复位流程。
- 吸嘴建模为 气缸「真空吸嘴」（伸出=吸/缩回=放），OutPoint=Y0..Y15，传感器共用「真空检测」输入。
- GPIB 通讯 CommType="GPIB"（CommManager 未知类型回退 TCP 仅日志；仿真/操作员模式走 StubHardwareBridge 仅记录，不崩溃）。
- NewProjectDialog 增加「半导体」分类配色（浅靛 #E0E7FF / 深靛 #4338CA）。
- 构建：`dotnet build -c Release` → EXIT_CODE 0，ERROR_CS_COUNT 0（BUILD_OK=True）。
- 模板总数 18→21（含空白）。

## 修正：固晶机/探针台不能用平移分选机的设备清单（用户反馈）
- 用户指出：之前把平移式分选机的整套设备（3自动盘+3手动盘+16吸嘴+搬运手臂+高温中转盘）套到了固晶机/探针台，这两类应有各自真实配置；且通讯类型下拉需有 GPIB 选项。
- 改动：
  1. `ViewModels/CommViewModel.cs` 的 `CommTypeOptions` 增加 "GPIB"（通讯类型下拉可选 GPIB）。
  2. 重写 `DieBoner()`：载台 X/Y/θ + 固晶头Z + 顶针Z；固晶吸嘴(真空)/顶针/点胶阀；晶圆环/基板条/收料盘(3盘)；固晶对位相机；点胶控制器 ModbusRTU；无 GPIB。流程：顶针顶晶→吸嘴取晶→视觉对位→点胶→固晶Z压合→放晶。
  3. 重写 `ProbeStation()`：载台 X/Y/θ + 探针Z；真空吸盘(吸晶)/探针卡夹紧；晶圆盘(1盘)；显微镜对位相机；测试机 GPIB(MEAS:BIN? 读 Bin)；流程：载台移到Die→探针Z下压→GPIB读Bin→抬起。
  4. 平移式分选机 `TransferHandler()` 保留原共用清单 `AddSemiEquipment`（那套设备本就属于分选机）。
- 构建：EXIT_CODE 0，ERROR_CS_COUNT 0。
- 结论：三类半导体模板现在配置各异；GPIB 出现在探针台与分选机（读 Bin），固晶机用点胶 Modbus。
