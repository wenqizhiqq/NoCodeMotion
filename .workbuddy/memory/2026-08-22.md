# 2026-08-22

## 新增「轴控制器」页面（位于轴页面之前）
需求：轴页面前面加一个轴控制器页面，可增删控制器，供轴页面选择归属。

改动：
- 新增 `Models/AxisControllerItem.cs`（继承 EditorItemBase：品牌/卡型号/卡号/轴数量/连接方式/备注）。
- `Models/ProjectData.cs` 加 `Controllers` 集合；`Services/Catalog.cs` 加 `ControllerNames` + `SetController` + 纳入 `RebuildAll`/`SyncAllFromData`；`ListEditorViewModel.SyncCatalog` 增加 `case "Controller"`。
- `Models/AxisItem.cs` 增加 `Controller` 字符串属性（归属控制器名）。
- 新增 `ViewModels/AxisControllerViewModel.cs`（`ListEditorViewModel<AxisControllerItem>` + `IEnsureDefaultSelection`，`CatalogCategory="Controller"`）。
- 新增 `Views/AxisControllerPage.xaml` + `.xaml.cs`（EditorPage 包装，自带增删/重命名工具栏）。
- `Views/AxisPage.xaml` 基本信息区加「控制器」ComboBox，绑定 `svc:Catalog.ControllerNames` → `SelectedItem.Controller`（XAML 加 `xmlns:svc`）。
- `MainWindow.xaml` 导航栏「轴」之前加「轴控制器」按钮（Tag=AxisController，ControllerIcon + NavIconControllerBrush）；`MainWindow.xaml.cs` 的 `_pages` 加 `["AxisController"] = () => new AxisControllerPage()`；`Resources/AppStyles.xaml` 加 `ControllerIcon` 几何与 `NavIconControllerBrush`。

坑：Python 批量补丁脚本中途失败时已成功文件已落盘，整脚本重跑会导致重复插入（ProjectData 被重复加 Controllers，已手动去重）。已将「只跑一次 / 拆剩余文件」铁律补进技能 `nocode-motion-edit-linkage`。

校验：所有 .cs 括号配平、XAML 解析通过；临时补丁脚本已删除。沙箱挡构建，需本机 VS rebuild 复核。控制器当前作为轴页面的选择/归属元数据；真实硬件桥后续可经 `AxisItem.Controller` 路由到对应控制卡。

## 默认首页改为「流程」页
`MainWindow.xaml.cs` 构造函数原 `Navigate("Axis", first)`（且在前面插入控制器页后，`first` 已变成控制器按钮，导致内容与高亮错位）。改为 `NavigateTo("Flow")`，使每次打开默认显示流程页面、并正确高亮流程导航按钮。

## 参数持久化到当前工程 + 启动自动打开上次工程
- 需求：其他页面（轴/IO/变量/流程/点位/气缸/通讯/托盘/轴控制器/工程师/操作员）的参数都要保存到当前工程，且软件启动时读取并显示。
- 现状确认：所有页面 ViewModel 都直接绑定 ProjectStore.Data.* 集合（单一真实来源），ProjectManager.SaveCurrent 已整体序列化 ProjectData；真正缺的是「启动时打开当前工程」与「自动保存落到当前工程文件」两个边界。
- 改动：
  - Services/ProjectManager.cs：新增 SaveLastProject / LoadLastProject / OpenLastProject（lastproject.txt 记录最后打开的工程名）；New/Open 写入当前名，Delete 清空，Rename 改为新名。
  - Services/ProjectStore.cs：Save() 当 ProjectManager.CurrentName 非空时改为调用 ProjectManager.SaveCurrent()，把全部页面参数写入当前工程文件；无工程时仍写旧 project.json（兼容）。
  - App.xaml.cs：App() 构造优先 ProjectManager.OpenLastProject()，失败回退 ProjectStore.Load()。
- 验证：三个 .cs 文件 brace/paren 平衡=0；沙箱禁止 dotnet/msbuild，需本地 VS rebuild 验证。
- 注意：打开/新建工程仍是「原地 CopyFrom」，会替换 ProjectStore.Data 各集合内容（旧 project.json 数据不会自动并入命名工程，除非先保存/读取）。

## 控制器页面：改名 + 双添加按钮
- 需求：把「轴控制器」页面改名为「控制器」；页面顶部「添加」按钮改为两个：「添加控制卡」与「添加扩展IO」。
- 改动：
  - Models/AxisControllerItem.cs：新增 Kind 属性（"控制卡"/"扩展IO"，默认"控制卡"）。
  - ViewModels/AxisControllerViewModel.cs：新增 AddCardCommand / AddExpansionIoCommand（各自 AddItem(kind,prefix)，名称 控制卡N / 扩展ION），不再使用默认 AddCommand。
  - Views/AxisControllerPage.xaml：EditorPage 设 ShowDefaultAddButton=false，LeftToolbarContent 注入两个按钮（绑定上述命令）；基本信息区加只读「类型」行（绑定 SelectedItem.Kind）。
  - MainWindow.xaml：导航按钮文字 轴控制器→控制器（Tag 与代码标识 AxisController* 保持不变，最小作用域）。
  - 顺带把 AxisPage.xaml ToolTip、AxisItem.cs/ProjectData.cs 注释里的「轴控制器」改为「控制器」。
- 复用：EditorPage 已原生支持 ShowDefaultAddButton + LeftToolbarContent（流程页「添加表格/添加脚本」同机制）。
- 校验：相关 .cs 括号配平、3 个 xaml XML 解析通过；沙箱禁构建，需本地 VS rebuild。
