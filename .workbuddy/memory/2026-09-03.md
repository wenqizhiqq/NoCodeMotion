# 2026-09-03

## Lua 编辑器异常显示位置修正
- 截图反馈：「异常需要 print 出来」误读——用户原意是异常要放到**「诊断」区**（`LuaEditorView.xaml` 的 `TxtDiagnostics`），不是 print 区。
- 之前（9-02 末轮）我曾改 `LuaEditorView.xaml.cs` 的 `AppendLog()` 让 `LogKind.Error` 也写 `TxtPrint`，方向错了，这次回退：
  - print 区（`TxtPrint`）只保留 `LogKind.Output` 回显（Lua `print()`），移除 Error 分支。
  - 异常改为写到「诊断」区（`TxtDiagnostics`）：`AppendLog(LogKind.Error)` 时 `Text = "✖ " + text`，`Foreground = BrushError`，`ToolTip = text`。
  - `TxtDiagnostics` xaml 加 `ToolTip="{Binding ElementName=TxtDiagnostics, Path=Text}"` 以便长消息悬停查看完整。
- 编译 0 错误（`NCMBuildDiag`）。诊断区仍然在 `CheckSyntaxNow()` 里被设为"语法正确"/语法错误 msg，运行异常与之共用同一个 `TxtDiagnostics` 通道——最近一次胜出（语法/运行谁后谁显示）。

## 待优化（未做，待反馈）
- 异常后点"运行"或重新启动调试：诊断区的红字不会自动清除（除非用户重新打开编辑器/手动点语法重检）。可在「开始调试」/「停止」按钮里加一句 `TxtDiagnostics.Text="诊断"; Foreground=BrushInfo` 重置。
- LuaDebugSession 启动/异常路径都进 `AppendLog(LogKind.Error)`，已覆盖 `SignalRuntimeException` / `InterpreterException`，无需改 session。

## Sim3DView 3D 仿真升级：智能相机跟随 + 多轴联合 + 异常视角 + 运动高亮
- 需求：仿真动画要更生动、多轴联合、哪个轴动视角就对准哪、出问题视角移动到出问题的轴、更智能。
- 现状已具备真 Viewport3D（`Sim3DView.xaml` 的 `Vp`/`Cam`）+ 龙门/桥架/主轴/转台/相机/气缸机台模型；`StubHardwareBridge.MoveAxisAbs/Rel/HomeAxis` 都会 `AxisRuntimeState.Set(name,val)`，所以运行流程时多轴联合本来就驱动机台（每帧 `_timer` → `UpdatePoseFromRuntime` 读 `AxisRuntimeState` 平移/旋转各轴组）。缺失的是：智能跟随、异常视角、运动高亮、平滑。
- 改动（`Sim3DView.xaml.cs`）：
  - 平滑插值：直线轴 `_dispX/_dispY/_dispZ`、旋转 `_dispRot` 对目标做低通（k=0.2），动画顺滑不跳变。
  - 运动检测：`UpdateSmartFocus()` 每帧算各轴归一化位置速度，取最快轴为活跃焦点。
  - 智能相机跟随：`_orbitCenter` 平滑（0.08）移到 `FocusPointFor(role)`（运动轴世界坐标）；手动拖拽暂停跟随（`ChkFollow` 关 / `MouseDown` 关），松开 2.5s 恢复。
  - 运动高亮：每个轴组加发光外壳 `AddHighlight`（DiffuseMaterial + _hlBrushActive 橙）；运动时 `host.Content=gm` 可见 + `brush.Opacity` 呼吸脉冲；静止 `host.Content=null` 隐藏。
  - 异常视角：`FocusAxis(name,isError)` 锁定把 `_orbitCenterTarget` 设为该轴坐标 + 红高亮（`_hlBrushError`）；`ClearFocus()` 恢复。`FocusAxisName` 依赖属性由 `OperatorPage.xaml` 绑定 `OpFocusAxis`。
  - 工具条：XAML 顶加 `智能跟随` CheckBox + `复位视角` 按钮（TtPillBlueBtn）。
  - 旋转轴 U 主轴世界坐标 = (_dispX, 38+_dispY, _dispZ)；X 龙门嵌套 D/U，故 U 世界含 X 位移。
- 改动 `OperatorViewModel.cs`：新增 `OpFocusAxis`（`SetField`），`AddLog(Error/Warn)` 用正则 `([XYUZRABC])\s*轴` 解析轴名设置焦点；`IsRunning=false` 时清空（运行结束解除锁定恢复跟随）。
- 编译坑（已踩已修）：`ModelVisual3D` **没有** `Visible`/`Visibility` 属性（继承 `Visual3D`，非 `UIElement`）；隐藏 3D 元素改用 `host.Content = null`。`DiffuseMaterial` **没有** `Opacity` 属性；透明靠 `SolidColorBrush.Opacity`。`_hlBrushActive/_hlBrushError` 用非冻结 `SolidColorBrush` 以便每帧改 Opacity 做脉冲。
- 构建 0 错误（`NCMBuildSmart3Dd`），启动 2s 窗口出现，13s 存活无异常。
- 多轴联合验证：运行 `仿真演示` 模板（4轴+气缸+相机）时各轴经 `AxisRuntimeState` 联动，相机自动对准运动轴。

## Lua API 注册（Request D）：Variable/IO/Axis/Cylinder 命名空间 + EStop/Delay/Print
- 用户贴「脚本流程」示例脚本（内置 `ProjectTemplateCatalog.cs` 的「脚本流程」Lua flow），用到 `Variable.Get/Set`、`IO.Get/Set`、`EStop`、`Delay`、`Axis.MoveAbs`、`Cylinder.Out/Back`、`Print`，要求「这些方法都要注册、并且实际有使用（驱动桥）」。
- 入口：`LuaDebugSession.Execute()` 调 `HardwareApi.Register(_script, new HardwareApi(...))`；`print()` 已通过 `Options.DebugPrint` 接到输出面板。
- 关键坑：MoonSharp 把 **CLR 实例直接赋给全局**（`script.Globals["Variable"] = new VariableApi()`）会抛 `cannot convert clr type VariableApi`——必须先 `UserData.RegisterType` 或用 **Table + CallbackFunction.FromDelegate**。最终采用后者：每个命名空间建 `Table`，方法用 `DynValue.NewCallback(CallbackFunction.FromDelegate(script, (Func/Action<...>)api.Method))` 暴露，再 `script.Globals[...] = DynValue.NewTable(t)`。
- 新增 `HardwareApi` 内容（`Services/HardwareApi.cs`）：
  - 命名空间类 `VariableApi`（`Get` 返回 `DynValue`（数字/字符串/nil），`Set` 写 `ProjectStore.Data.Variables` 的 `VariableRow` Name1..5/Value1..5，找不到则新建行）、`IoApi`（`Get` 读 `IHardwareBridge.ReadInput` 返回 "0"/"1"，**未配置该输入时返回 "1" 让纯仿真脚本直接跳过等待启动信号**；`Set` 调 `WriteOutput`，找不到输出给警告不崩溃）、`AxisApi`（`MoveAbs(name,pos,speed)` 先 `SetAxisSpeed` 再 `MoveAxisAbs`，另含 `MoveRel/Home/Stop/WaitDone/SetSpeed/Enable`）、`CylinderApi`（`Out`→`CylinderMove(name,1)`、`Back`→`CylinderMove(name,0)`、`Reset`）。
  - 全局函数：`IsEStop()`（`StatusBarService.EStopped`）、`Delay(double)`（`Thread.Sleep`，封顶 60s）、`LuaPrint(object)`（大写 `Print` 别名，接 `_log`）。
  - `Register()` 内除原有 flat 函数（AxisMove/SetIO/ReadIO/CylinderMove…）外，新增上述命名空间表 + `EStop`/`Delay`/`Print`。
- `StubHardwareBridge` 增加 `Dictionary<string,int> _ioState` 做 **IO 实时状态**：`ReadInput` 首次返回 `io.Value` 并缓存，`WriteOutput` 写入，使 `IO.Get/Set` 真正有状态（"实际有使用"）。
- 修复：内置「多气缸装配」模板原本没有轴，导致示例 `Axis.MoveAbs('X'/'Y')` 抛"找不到轴"；已补加 `X`/`Y` 两轴，使内置 `脚本流程` Lua 示例可直接跑通。
- 校验：写独立 `D:\LuaApiProbe` 用真实 `MoonSharp.Interpreter.dll` 跑用户脚本原样——`Variable.Get('循环数')` 返回 3→cycle=4→`Set` 落 "4"，`IO.Get('启动')` 自动 "1" 跳过等待，`Axis.MoveAbs`/`Cylinder.Out`/`Delay`/`Print` 全部触发，无 `attempt to call a nil value`。最终构建 0 错误（`NCMBuildLuaApi4`），启动存活有窗口。

## STP/STEP 导入到操作员仿真页（Request E）
- 需求：读取 `D:\wqz\code\NoCodeMotion\IR-R10-140S-INT-3D-3D.stp`（Creo AP203 BREP，45.66 MB，~693k 实体），显示到操作员的仿真页（`Sim3DView`），并在工具条加按钮打开 STP。
- **关键决策：AssimpNet 4.1.0 失败 → 换 OcctNet.Wrapper 0.1.1**：
  - Assimp 即使改 `.p21` 强制 STEP reader，对 Creo BREP 也产出 0 面（它的 BREP 三角化器太弱）；它还把 `.stp` 按扩展名误判为 IFC，先撞 `IFC: Unrecognized file schema: CONFIG_CONTROL_DESIGN`。
  - OcctNet.Wrapper 是 .NET 8/10 兼容的 OpenCASCADE 7.9.3 封装 NuGet，自带 `runtimes/win-x64/native/` 下的 51 个 TK*/Occt* DLL，API：`OcctShape.ImportStep(path)` → `using` 释放；`shape.Triangulate(linearDeflection:1.0)` → 返回 `OcctMesh`（**非 IDisposable**，无 `Dispose`），`mesh.Vertices[i]` 结构体 `.X/.Y/.Z`、`mesh.TriangleIndices[i]`、`mesh.Vertices.Count`、`mesh.TriangleCount`。
  - 探针 `D:\StpProbe` 验证：IMPORT_MS=14678, TRIANGULATE_MS=3607, VERTS=387693 TRIS=466350。
- **csproj 改动（`NoCodeMotion.csproj`）**：
  - 替换 `<PackageReference Include="AssimpNet" Version="*" />` → `OcctNet.Wrapper Version="0.1.1"`（旁边 MoonSharp 2.0.0 / NModbus 3.0.83 保留）。
  - 新增 Content 项：`<Content Include="IR-R10-140S-INT-3D-3D.stp" Link="Models\CAD\IR-R10-140S-INT-3D-3D.stp" CopyToOutputDirectory="PreserveNewest" />`。CSPROJ 缩进敏感（行 74-77），Edit 时需匹配原 8 空格。
- **`Sim3DView.xaml` 改动**：原工具条 StackPanel 替换为 `ChkFollow` + `BtnResetView` + `BtnOpenStp`（`TtPillBlueBtn`）+ `BtnClearStp`（`TtPillGrayBtn`），并加底部 `TxtStpStatus`（半透明字、绕行、不挡事件）。
- **`Sim3DView.xaml.cs` 改动（`Views/Sim3DView.xaml.cs`）**：
  - 字段（行 180-185）：`_cadMode`、`_stpModel`、`_stpContent`（冻结跨线程安全的 `Model3DGroup`）、`_stpCenter`、`_stpRadius`。
  - **Z-up→Y-up**：静态 `_zUpToYUp = new RotateTransform3D(new AxisAngleRotation3D(new Vector3D(1,0,0), -90)).Value`（CAD Z 朝上→WPF Y 朝上）。
  - 构造尾部（行 251）：`Loaded += (s,e) => TryAutoLoadDefaultCad();`，路径 `AppContext.BaseDirectory/Models/CAD/IR-R10-140S-INT-3D-3D.stp` 存在即自动 `LoadStepFile`，让操作员仿真页默认展示真实 3D。
  - `BuildScene()` CAD 分支（行 290-297）：`_cadMode && _stpContent != null` 时 `Root.Children.Clear` 后只 `_stpModel = new ModelVisual3D{Content=_stpContent}; Root.Children.Add(_stpModel);`（清空参数化机台/轨迹/点位；`_machineGroup = null`）。
  - `UpdatePoseFromRuntime()` 行 524 已有 `if (_machineGroup == null) return;` 守护，CAD 模式下定时器调用安全空操作。
  - `BtnResetView_Click`（行 683）CAD 分支：`_theta=0.7; _phi=0.5; _radius=Math.Max(_stpRadius*2.6, 120); _orbitCenter=_orbitCenterTarget=_stpCenter;`。
  - `BtnOpenStp_Click`（行 708）：`Microsoft.Win32.OpenFileDialog` `*.stp;*.step`，try/catch 防止 EDR 静默拦截（`OpenFileDialog` 是已知 EDR 拦截点，必须把异常写回 UI 状态文本）。
  - `BtnClearStp_Click`（行 726）：清 CAD 模式、置空、关 `_followEnabled`、重建场景。
  - `LoadStepFile(path)`（行 739）：`SetStpStatus("正在解析 STP…")` → `Task.Run` 后台 → `BuildStepModel(path, out group, out center, out radius, out tris)` → `group.Freeze()` → `Dispatcher.Invoke` 设字段、`_cadMode=true`、`_followEnabled=false`、`ChkFollow.IsChecked=false`、`BuildScene()`、`UpdateCamera()`、状态文本显示三角面数；异常捕获后 `SetStpStatus("STP 解析失败：" + ex.Message)`。
  - `BuildStepModel(path, out group, out center, out radius, out totalTris)`（行 773）：`using var shape = OcctShape.ImportStep(path)`；`var mesh = shape.Triangulate(1.0)`（**不 dispose mesh**，非 IDisposable）；读 `vc/ic` → `Point3D[vc]` 烘培 Z-up→Y-up、算 min/max；`MeshGeometry3D` 填 Positions+TriangleIndices；按三角形累加平滑顶点法线（`Vector3D.CrossProduct`，零向量 fallback `(0,1,0)`）；`MakeStepMaterial()` 钢蓝 `0xB8C2CE` + Diffuse + Specular 18，`GeometryModel3D(mg, mat){ BackMaterial = mat }`（双面）；center/radius 由 AABB 推算，NaN 兜底 100。
- **踩坑修复**（构建 NCMBuildStp1/2/3 序列）：
  - 1：`OcctMesh.Dispose` 不存在 → 移除 `try/finally mesh.Dispose()`。
  - 2：删 try 后留 `}` 多余导致行 827 后面 brace 不平 → 把最后 `}` 与 `if (!double.IsFinite(radius)...) radius = 100; }` 接齐。
- **构建验证**：`D:\NCMBuildStp3\bin\NoCodeMotion\debug\` 下 `NoCodeMotion.dll=True`、**51** 个 Occt*/TK* 原生 DLL 自动拷贝、`Models\CAD\IR-R10-140S-INT-3D-3D.stp=True`。启动 `Start-Process -PassThru` PID=61500，55s 后 `ALIVE PID=61500 HANDLE=2238746 NAME=NoCodeMotion` 窗口存活。
- **视觉验证：WPF PrintWindow 抓不到 WPF 客户端区**（DWM 标题栏抓到，WPF DirectComposition 硬件加速区空白——已知坑，PrintWindow + WPF 普遍抓不到硬件合成层）。
  - 解决：写独立 `D:\StpRenderProbe\Program.cs` 用**完全相同的 `OcctNet.Wrapper` + `_zUpToYUp` 路径**装配 `MeshGeometry3D` + 灯，`RenderTargetBitmap` 直接渲染为 PNG，绕过运行中窗口。
  - 相机参数复用 Sim3DView 行 978-985 公式：`pos = center + r*(cos(phi)*cos(theta), sin(phi), cos(phi)*sin(theta))`，`theta=0.7, phi=0.5, r=radius*2.6`。
  - 跑通：`OK tris=466350 verts=1 center=(0.0,-206.6,2201.6) radius=2369.5 -> D:\wqz\code\NoCodeMotion\stp_render2.png`（121 KB）。
  - 渲染结果（`stp_render2.png`）：圆顶 + 圆柱孔 = **该 STP 文件本身就是 ABB 机器人底座/集成基座**（`IR-R10-140S-INT` 含 `INT` 标识，base 部件），不是完整机械臂，所以看到的就是基座圆顶 + 中孔轴承位的真容。`466k` 三角面齐全、平滑法线工作、Z-up→Y-up 转换正确（中孔方向与渲染视角一致）、材质/光照都生效。
- **临时清理**：`D:\wqz\code\NoCodeMotion\ncm_shot.py`（PrintWindow 脚本）、`stp_render.png` 早期版本——保留 `stp_render2.png` 作为可视化证据、`ncm_shot.py` 留着以后抓图。
- **可选后续**（用户未要求，未做）：让 LoadStepFile 也可驱动运动可视化、把基座叠加到机台坐标系做碰撞检测、把 CAD 模式存到工程文件。

