# 2026-09-22 工作记录

## 点位「移动条件」表头加小字说明
- 用户要求：「每一个控件旁边小字说明」。条件表（PointPage.xaml 右侧，与点位表同级并列）原列头只有标题。
- 实现：把 6 个 DataGridTemplateColumn 的 `Header="xxx"` 字符串改为两行 `StackPanel`（粗体标题 11 + 灰色小字说明 9，`TextMutedBrush`/`TextSecondaryBrush`，居中）。
  - 使用 → 勾选才判断；类型 → IO/气缸/轴等；名称 → 目标对象；比较 → ==/!=等；期望值 → 要满足的状态；实际值 → 设备当前值。
- 注意：列头样式 `PointHeaderStyle` 仅设背景/字重/居中，自定义 StackPanel 头内容可正常渲染，无需改样式。
- 构建：PowerShell + 绝对 `C:\Program Files\dotnet\dotnet.exe -c Debug` → **0 error**（build_cap.log）。

## 提醒（项目约束）
- Bash 的 PortableGit 外壳损坏；本环境一律用 PowerShell 跑 dotnet，构建日志 Out-File utf8 后读。
- safe-delete 拦截删除，根目录残留 build_*.log 无法清理（不影响构建）。
