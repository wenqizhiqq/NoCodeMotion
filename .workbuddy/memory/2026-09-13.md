# 2026-09-13 工作日志

## 输出 IO 页面：改回左右布局（已修）

- 需求：上轮把 IO 页面从左右两列改为上下两行（解决输出 IO 宽度不够），用户反馈「还是需要左右显示」——即希望保持「输入 IO | 输出 IO」左右两栏结构。
- 修改：`Views/IoPage.xaml`
  - Grid 改回两列 `ColumnDefinitions = 1* / 1.6*`（输出面板占更宽比例，约 62%），`RowDefinitions = * / Auto`。
  - 输入面板 `Grid.Column=0`，输出面板 `Grid.Column=1`；PageHintBar 回到 `Grid.Row=1, Grid.ColumnSpan=2`。
  - 输入/输出两表列宽整体收紧（卡类56/卡号44/控制器104/模块44/序号44/套码58/电平58/功能76/值36），并把「IO名称」列设为 `Width="*" MinWidth="80"` 自适应剩余空间。
  - 输出表保留上轮「切换」列（开/关药丸按钮，置位绿/复位红），移除「运行时」列。
- 效果：左右两栏结构恢复；输出面板更宽 + IO名称列自适应，列横向溢出显著降低（宽屏下基本无横向滚动，窄屏下 DataGrid 自带横向滚动条兜底）。
- 构建：`dotnet build -c Release -v minimal` → EXIT_CODE 0，0 个错误（XAML 编译通过；其余 295 警告均为 CA1416 平台兼容，非错误）。
