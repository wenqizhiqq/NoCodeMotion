# 2026-09-24 工作日志

### 修复：连接后真实轴/IO 显示异常 + 轴列表未按底层重建（#225-#227）
- 用户反馈 UI 异常：「无法对 IoViewModel 只读属性 DetectedOutIo 进行 TwoWay 或 OneWayToSource 绑定」；且「轴数量没有变化，应该先清空之后显示轴，轴页面参数对应底层，IO 也先清空再添加」。

#### 绑定异常修复
- 根因：`IoPage`/`AxisPage` 顶部横幅把只读计算属性（`IoViewModel.DetectedInIo`/`DetectedOutIo`、`AxisViewModel.DetectedAxisCount`、`AxisControllerViewModel.Detected*`）绑到 `Run`/`TextBlock`，被 WPF 当 TwoWay 校验失败。
- 修法：给这些显示绑定显式加 `Mode=OneWay`（`IoPage.xaml` 两处 Run、`AxisPage.xaml` 一处 Run、`AxisControllerPage.xaml` 三处 TextBlock）。只读属性一律用 OneWay 显示。

#### 轴列表连接后清空重建（之前只显示数字、从未重建轴）
- `AxisControllerViewModel.Connect()`：在 `FetchDetectedCounts(ctl)` 之后新增 `GenerateAxisPoints(ctl)`，并补 `OnPropertyChanged(DetectedAxisCount/InIo/OutIo)` 让控制器页「真实检测」卡片刷新。
- 新增 `GenerateAxisPoints(ctl)`（对齐 `GenerateIoPoints`）：清掉 `Controller==ctl.Name` 的旧轴 → 按 `DetectedAxisCount>0 ? DetectedAxisCount : 配置 AxisCount` 生成 `轴1..轴N`（`Controller` 绑定、`AxisNo`、`Enabled=true`）→ `Catalog.SetAxis` + `ScheduleSave`。
- `ConnectMessage` 增加轴生成结果文案（`axisMsg`）。
- IO 早已是「先清空本卡 IO 再按主板+扩展模块数量生成」（`GenerateIoPoints`），本次确认其行为即用户要的「先清空再添加」；真实卡上报 IO 数时（`FetchDetectedCounts` 回写 `InIoCount/OutIoCount`）IO 也用真实数量。

#### 行为说明（告知用户）
- 连接会**清空并重建**该控制卡名下的轴/IO（按底层真实数量，真实为 0 时回退配置数量）。若此前手动调过某轴的运动参数，连接后会被重置为默认——这是「先清空再显示」的必然结果；如需保留手动参数，后续可改为「仅补齐缺失轴、不删已有轴」。
- 无实物卡（虚拟卡）时 `GetCardTotalAxisNum` 返回 0，`DetectedAxisCount=0`，轴数回退到配置 `AxisCount`；要看到轴，需在控制器页设好轴数或接真实卡。

#### 验证
- PowerShell + 绝对 `C:\Program Files\dotnet\dotnet.exe -c Debug`：0 错误（604 警告，均既有 CA1416 LuaEditorView）。#225-#227 全 completed。

### 改为「连接时全局清空所有轴/IO 再重建」（用户确认）
- 用户确认清空范围要选「全局清空所有轴/IO 再重建」：连接某控制卡时，把工程里**全部**轴 / 输入IO / 输出IO 先 `Clear()`，再只按本卡真实/配置数量重建本卡的。
- `GenerateIoPoints`：`data.Inputs.Clear(); data.Outputs.Clear();`（原为按 `Controller==卡名` 只删本卡）。
- `GenerateAxisPoints`：`data.Axes.Clear();`（原为按 `Controller==卡名` 只删本卡）。
- 注意：多控制卡场景，连接第二张卡会清空第一张卡已生成的点（只保留最后连接的卡）。用户已知并接受。构建 0 错误（build7）。

### 状态栏加「控制器正在连接」进度条（同日，最新）
- 需求（用户）：控制器连接要在后台跑、不要卡住进度条，且**状态栏也要有一个进度条显示控制器正在连接**。
- 后台连接逻辑昨日已落地（`Connect()`/`AutoConnectAll()` 均 `Task.Run`，页面进度条 + `IsConnecting` 防重入）。本轮补**状态栏侧**连接进度指示。
- `Services/StatusBarService.cs`（CRLF）：新增 `ControllerConnecting`(bool) + `ControllerConnectingText`（"正在连接…"）+ `SetControllerConnecting(bool)`（变值时 `Raise()`）。
- `ViewModels/StatusBarViewModel.cs`（LF）：`RaiseAll()` 增 `ControllerConnecting`/`ControllerConnectingText`；类末加转发属性。
- `Views/StatusBarView.xaml`（CRLF）：「控制器」StackPanel（Grid.Column=3，复用 `BoolToVis`）内加 `IsIndeterminate` 的细 `ProgressBar`(宽56/高4/蓝 #1D4ED8) + 「正在连接…」文字，仅 `ControllerConnecting` 时可见。
- `ViewModels/AxisControllerViewModel.cs`（LF）：`Connect()` 起始 `SetControllerConnecting(true)`、dispatcher 内 `finally` 置 `false`；`AutoConnectAll()` 起始置 `true`、外层 `finally`(dispatcher) 置 `false`。
- 编码：CRLF 两文件用 Python 脚本（utf-8-sig 归一化、写回保 CRLF）改；LF 两文件用 Edit。
- 构建（PowerShell + 绝对 dotnet）：**0 错误**（606 警告，均既有 CA1416）。

### 自动连接不阻塞载入进度条（同日，最新）
- 用户要求：打开/切换工程时控制器连接必须后台跑，**不要卡住载入项目的进度条**；状态栏显示「正在连接」→ 显示已连接。
- 现状排查：`OpenProjectAsync`/`NewProjectAsync` 在 `Task.Run` 读完 xlsx 后回 UI 线程调 `LoadInto`→`DataReloaded?.Invoke()`（UI 线程，且在 `LoadingService.Hide()` 之前）；原 `DataReloaded` 处理 `() => AutoConnectAll()` 虽内部 `Task.Run`，但触发点落在载入同步链内。连接逻辑本身（`EnsureInitialized`/`TryConnect` 重）已在后台线程，本不阻塞；为彻底消除「载入条被连接影响」的隐患，把触发点改为**投递到 UI 消息循环末尾（低优先级）**。
- `ViewModels/AxisControllerViewModel.cs`（LF）：新增 `RequestAutoConnect()`（`Application.Current.Dispatcher.BeginInvoke(new Action(AutoConnectAll), DispatcherPriority.Background)`）；构造函数订阅与首屏 `Items.Count>0` 分支都改调 `RequestAutoConnect()`。`AutoConnectAll()` 仍内部 `Task.Run` 后台跑；`IsConnecting` 守卫保证多次投递只连一次（页面重建会新建 VM、旧 VM 未退订，避免重复并发连接）。
- 效果：载入遮罩隐藏后，连接才在后台启动；状态栏「正在连接…」不确定进度条显示，连完切回「N/M 在线」。
- 构建（PowerShell + 绝对 dotnet）：**0 错误**（606 警告，既有 CA1416）。

### 点位表「不移动」语义（同日，最新）
- 需求（用户，附截图）：点位表某轴位置单元格**留空也要能保存成「不移动」**；移动该点位时**跳过留空轴（不移动它）**，并给出**提示文字**。
- 核心改动：`Models/PointAxis.cs` 的 `Position` 由 `double` 改为 `double?`。`null` = 「不移动」。`PointItem.EnsureSlots()` 新建轴槽默认 `Position=null` → 新建点位未填即「不移动」（旧工程空单元格还原为 null 而非 0）。
- 序列化：`Services/XlsxProjectStore.cs` `RestorePointTableSheet` 已（前置）对空/非数字位置存 `null`（位置列空串→不移动）；`BuildPointTableSheet` 用 `pVal?.ToString()` 自动写空串，无需改。
- XAML：`PointPage.xaml` 4 个位置 TextBox 绑 `Positions[i].Position` 加 `TargetNullValue=''`（空文本框⇄null 双向，不显 0）；`EngineerPage.xaml` 此前已有 `TargetNullValue=''`。
- 移动跳过 + 提示（Position==null 时 `continue` 不 `MoveAxisAbs`，并 `StatusBarService.ReportInfo("点位「名」轴 X 未填位置，已跳过（不移动）。")`）：
  - `ViewModels/PointViewModel.cs` `MoveToPoint`（仿真写 `AxisStates[i].CurrentPosition=slot.Position.Value`）、`RebuildSim`（`null→0`）、`ExportCsv`（null 输出「不移动」文本）。
  - `ViewModels/EngineerViewModel.cs` `MoveToPoint`（仿真）。
  - `ViewModels/OperatorViewModel.cs` `ExecutePoint`/`ExecutePointForTable`（真实硬件，`MoveAxisAbs(axis, slot.Position.Value)`；另 3D 预览读取 `?? 0`）。
  - `ViewModels/FlowRunnerService.cs` 与 `ViewModels/FlowViewModel.cs` 点位下发（真实硬件，`.Value` + 日志提示）。
  - `Services/NgRunner.cs` `PointGo`（轴1 简化，`_pos!=null` 才 `MoveAxisAbs`）。
  - `Services/SimFlowPlayer.cs`（仿真轨迹：`posVal==null` 直接 `continue` 跳过该轴，不生成 SimAction）。
- `Speed` 保持 `double` 不变（仅 Position 决定移动与否；不移动轴不消费 Speed）。
- 构建（PowerShell + 绝对 dotnet `-c Debug`）：**EXIT=0，0 错误**；唯一新警告 `PointViewModel.cs(703,89) CS8629`（nullable 值类型隐式转换，无害）。

### 控制器页「获取 + 连接」按钮合并为「连接控制器」+ 弹窗进度条（同日，最新）
- 需求（用户）：把控制器页的「获取卡参数」和「连接」两个按钮合并成一个「连接控制器」，点击后**弹窗进度条显示正在连接**。
- `Views/AxisControllerPage.xaml`（BOM CRLF）：删除「获取」(`FetchModulesCommand`)/「连接」(`ConnectCommand`) 两按钮，换为单个绿色药丸按钮「连接控制器」→ `ConnectControllerCommand`；「断开」保留。页面内联进度条保留（供自动连接时显示）。
- 新增 `Views/ConnectProgressDialog.xaml`(+`.xaml.cs`)：Apple 风格透明圆角白卡弹窗（同 `ConfirmDialog` 套路），标题「正在连接控制器」+ `IsIndeterminate` 进度条 + 绑定 VM 的 `ConnectStatusText`/`ConnectMessage`。`OnContentRendered` 里订阅 VM `PropertyChanged` 并调 `_start()`（=VM.Connect）；监听到 `IsConnecting` 变 false → `Close()` 自动关闭；`OnClosed` 退订。兜底：`_start()` 后若 `IsConnecting` 仍 false 直接关闭。
  - 坑：WPF `StackPanel` **无 `Padding` 属性**（首次构建报 MC3072）→ 改用 `Margin`。
- `ViewModels/AxisControllerViewModel.cs`（LF）：新增 `ConnectControllerCommand` → `ConnectController()`：校验 `SelectedItem`（空则 `StatusBarService.ReportInfo` 提示）→ `IsConnecting` 守卫 → ①`FetchModules()`（获取卡参数）→ ②`new NoCodeMotion.Views.ConnectProgressDialog(() => Connect(), this).ShowDialog()`（弹窗 + 后台连接）。`FetchModulesCommand`/`ConnectCommand` 属性保留（不再被 UI 绑定，避免影响其它引用）。
- 交互：弹窗 `ShowDialog` 走嵌套消息循环，后台 `Task.Run` 的 `dispatcher.Invoke` 仍可回 UI，连接结束弹窗自动关；状态栏「正在连接…」同步显示（既有）。
- 构建（PowerShell + 绝对 dotnet `-c Debug`）：编译 **0 错误**（EXIT=0；正式 bin 被运行中的 `NoCodeMotion.exe` 锁（MSB3021），改用 `-p:OutDir=C:\tmp\nocode_out\` 复核，编译通过；要写回 bin 需先关闭正在运行的程序）。

### 修复：主界面仍要等控制器连接完成才进入（同日，最新）
- 用户反馈：载入项目/启动时，**主界面非要等控制器连接完成才进去**；要求先进主界面、控制器纯后台连接，状态栏显示「正在连接」→「已连接」。
- 根因：`AxisControllerViewModel` 在**页面预初始化**（`MainWindow.PreloadAllPagesAsync` 的加载遮罩期间）被构造，构造函数里 `RequestAutoConnect()` 直接 `BeginInvoke(Background)`；而 `PreloadAllPagesAsync` 也用 `Dispatcher.Yield(Background)` 逐页让帧，于是 `AutoConnectAll` **与预初始化同时跑**。`AutoConnectAll→HardwareSetup.EnsureInitialized→CardFamilyCatalog.DetectPresent()` 会枚举/加载大量底层原生 DLL，与 UI 线程的页面构造**争用原生加载器锁**，把预初始化拖住 → 遮罩迟迟不隐藏 → 表现为「连上控制器才进主界面」。
- 修法（`ViewModels/AxisControllerViewModel.cs`，LF）：`RequestAutoConnect()` 改为**显式等待加载遮罩结束**再后台连接——`if (!LoadingService.IsLoading)` 才 `BeginInvoke(Background)`；否则挂**一次性** `LoadingService.StateChanged` 回调，等 `IsLoading` 变 false 后取消订阅并投递 `AutoConnectAll`（用 `_autoConnectPending` 防重复订阅）。这样`Show/Hide` 的引用计数语义保证：打开/新建工程、启动预初始化**全部结束、遮罩隐藏后**才开始连接。
- 效果：先进主界面 → 状态栏「正在连接…」（不确定进度条）→ 后台连完切「N/M 在线」。连接与载入彻底分时，不再抢硬件初始化。
- 构建（PowerShell + 绝对 dotnet `-c Debug`，`-p:OutDir=C:\tmp\nocode_out\`）：**EXIT=0，0 错误**。

### 修复：ConnectProgressDialog 运行时 XamlParseException（找不到资源 SubLabel）（同日，最新）
- 用户反馈（截图）：点「连接控制器」弹窗时抛 `System.Windows.Markup.XamlParseException`：「无法找到名为"SubLabel"的资源。资源名称区分大小写。」，堆栈在 `ConnectProgressDialog..ctor → InitializeComponent()`。
- 根因：`SubLabel` 是 **`AxisControllerPage.xaml` 的 `<UserControl.Resources>` 页面级样式**（FontSize=11 / Foreground=TextMutedBrush），而 `ConnectProgressDialog` 是顶层 `Window`，`StaticResource` 只沿自身→Application 查找，看不到任何页面的局部资源 → 解析即抛异常。
- 修法：把弹窗里 `Style="{StaticResource SubLabel}"` 改成内联 `FontSize="11" Foreground="{StaticResource TextMutedBrush}"`（`TextMutedBrush` 在全局 `Resources/AppStyles.xaml`）。
- 校验：写脚本抽 `ConnectProgressDialog.xaml` 里所有 `{StaticResource X}` 键（AccentBrush/ControllerIcon/HoverBrush/LineBrush/SoftShadow/SurfaceBrush/TextMutedBrush/TextPrimaryBrush 共 8 个），逐个比对全局 `AppStyles.xaml` 的 `x:Key`——**全部存在**；其余 `ConfirmDialog` 同款键本就全局可用。
- 构建（PowerShell + 绝对 dotnet `-c Debug`，`-p:OutDir=C:\tmp\nocode_out\`）：**EXIT=0，0 错误**。
- ★ 通用坑：**Window 弹窗只能用 `AppStyles.xaml` 全局键**；页面级 `<UserControl.Resources>` 里的键（SubLabel/MiniNumBox 等）在 Window 里解析不到，会运行时炸。

### 控制器页：改为从上到下 + 「卡型号→扩展IO→连接」门控 + 连接硬件读数后台化（同日，最新）
- 需求（用户，附控制器页截图）：
  1. 页面布局**改成从上到下**（原来 5 张卡片横向 WrapPanel 并排）；
  2. 流程改为「**先选卡型号 → 再选扩展 IO → 再点连接**」；
  3. 载入工程时控制器连接会**卡住页面点不动**，连接必须真正后台线程。
- ① 布局：`Views/AxisControllerPage.xaml` 的 `<WrapPanel Orientation="Horizontal" ItemWidth="372">` → `<StackPanel Margin="14">`；5 张卡片 `Margin="0,0,12,10"` → `Margin="0,0,0,10"`（纵向堆叠、全宽）。
- ② 门控：VM 加 `HasCardType` / `CanConfigureExpansion`（=HasCardType）/ `CanConnect`（=HasCardType），在 `WireSelected` 与 `OnTrackedCardChanged`（CardType 变化）里 `OnPropertyChanged` 刷新。XAML：扩展 IO 配置块（主板IO数 / 已挂模块 / 添加模块）包一层 `<StackPanel IsEnabled="{Binding CanConfigureExpansion}">`；未选型号时显示橙色提示「请先在上方「基本信息」选择「卡型号」，再配置扩展 IO」（DataTrigger 绑 CanConfigureExpansion==False）；「连接控制器」按钮加 `IsEnabled="{Binding CanConnect}"`。
- ③ 连接后台化（关键）：把 `FetchDetectedCounts` 拆成 **`ReadRealCounts`（纯硬件读，后台线程可调）+ `ApplyRealCounts`（写回绑定属性，UI 线程）**。`Connect()` 与 `AutoConnectAll()` 里 `TryGetRealCounts`（硬件查询，最耗时）与在线数统计全部挪到 `Task.Run` 后台；UI 线程只 `ApplyRealCounts` + 触发 INPC + `StatusBarService.SetControllerStatus(后台算出的 online,total)`（不再在 UI 线程 `UpdateStatusBar()` 读硬件）。
  - 根因：`Connect()`/`AutoConnectAll()` 的 `dispatcher.Invoke` 里原来直接 `FetchDetectedCounts(c)`（`TryGetRealCounts` 是硬件查询）+ `UpdateStatusBar()`（逐个 `IsControllerReady` 读硬件）——全跑在 UI 线程，连接时页面卡死。
- 构建（PowerShell + 绝对 dotnet `-c Debug`，`-p:OutDir=C:\tmp\nocode_out\`）：**EXIT=0，0 错误**。
- ★ 铁律：**连接/自动连接里凡是碰硬件（TryGetRealCounts / IsControllerReady / TryConnect / Reconnect / EnsureInitialized）一律放后台线程**；UI 线程只做赋值与通知，否则载入工程后连接期间页面卡死。

### 真根因：连接卡界面 = 硬件桥 `_gate` 在慢初始化期间被独占（同日，最新）
- 用户反馈：上一轮把硬件读数挪到后台后，**控制器连接时界面还是卡**。
- 真根因（不在 VM，在硬件桥 `Services/Hardware/Cards/SamsunCardBridge.cs`，`WenQiZhiCardBridge`）：桥只有一个全局锁 `_gate`，而 `TryConnect(ctl,out)` 与 `SlotOf()` 都是 **`lock(_gate){ … Initialize(slot) }`** —— `Initialize` 里做 `family.Create()` / `SeedCardParams` / `InitCard()` / **`OpenCard()`**（真硬件操作，可几百 ms~秒级）。与此同时 **UI 线程的绑定 `IsOnline`→`IsControllerReady`、`ConnectionStatusLines`→`ControllerStatus`、`AnyReady` 都要 `lock(_gate)`** —— 所以连接期间 UI 线程在等同一把锁 → 整个界面冻结。VM 侧再怎么后台化都没用（UI 绑定自己会去查桥）。
- 修法（只动锁作用域，不改变行为）：
  - `CardSlot` 加 per-slot `InitLock`。
  - `TryConnect` / `SlotOf`：`_gate` 内只做「取/建槽位 + 判断是否已初始化」，**慢初始化 `Initialize(slot,…)` 移到 `_gate` 之外的 `lock(slot.InitLock)` 里**（同槽位仍串行，不会双重初始化；`Runtime==null` 判断保留原语义）。
  - `Disconnect` / `Reset` 同理：`CloseCard()` 关卡的硬件操作移到 `_gate` 之外。
  - VM `Disconnect()` 也改成 `Task.Run` 后台 + `dispatcher.Invoke` 刷 UI（关卡片同样是硬件操作）。
- 效果：连接/断开期间 UI 线程读桥状态只等一次极短的 `_gate`（字典读），不再被 `InitCard/OpenCard` 卡住 → 界面不冻。
- 构建（PowerShell + 绝对 dotnet `-c Debug`，`-p:OutDir=C:\tmp\nocode_out\`）：**EXIT=0，0 错误**。
- ★★ 通用铁律：**共享硬件桥的全局锁绝不能跨「慢硬件调用（InitCard/OpenCard/CloseCard）」持有**；锁内只做快速状态读写，慢调用挪到锁外（必要时加 per-resource 锁串行）。否则任何在 UI 线程查该锁的绑定都会让界面冻结。

### 控制器页：操作放左栏、只读状态放右栏（同日，最新）
- 需求（用户）：控制器页「需要操作的部分放左侧，不需要操作的状态显示放右侧」。
- 改法（`Views/AxisControllerPage.xaml`，BOM CRLF）：detail 由单个纵向 `StackPanel Margin="14"` 改为 **2 列 `Grid`**（`1.1*` / `1*`）：
  - **左列** `StackPanel Grid.Column="0"`：`基本信息` + `扩展 IO 模块`（含连接/断开按钮）——需要操作的。
  - **右列** `StackPanel Grid.Column="1"`：`底层连接总览` + `数量汇总` + `连接后真实检测`——纯只读状态。
- 补：用户反馈「页面显示不下，需要滚动条」→ `<local:EditorPage.Detail>` 内容外面套一层 `<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">`（表单页标准做法，同 `CylinderPage.xaml`；**不要在 `EditorPage` 的 Detail 里全局套 ScrollViewer**，会给 Detail 无限高上下文、破坏 FlowPage 表格的高度约束）。
- 构建（PowerShell + 绝对 dotnet `-c Debug`，`-p:OutDir=C:\tmp\nocode_out\`）：**EXIT=0，0 错误**。

### IO「功能」列：输入/输出两套目录 + 默认「无」+ 真实接入运行时（同日，最新）
- 需求（用户，附 IO 页「功能」下拉截图）：功能要**按输入 IO / 输出 IO 用不同的列表**，**默认「无」**，且功能**要真实有效**（不是摆设）。
- 现状：`IoItem.Function` 只是被模板/AI 写入、**运行时无任何消费**（`IoItem.Level` 反之是真实生效的——`ApplyLevel(raw, io.Level)` 决定常开/常闭取反）。旧的单选项目录（无/动点/原点/光栅/安全门/启动按钮/复位按钮/停止按钮/急停按钮/到位点）对输入输出共用，且「动点」含糊无效。
- 新增 `Services/IoFunctionCatalog.cs`：`InputFunctions`（无/急停按钮/安全门/光栅/原点开关/正限位/负限位/启动按钮/停止按钮/复位按钮/暂停按钮/手自动切换/到位信号/来料检测/气压检测/报警反馈）、`OutputFunctions`（无/轴使能/运行指示/就绪指示/报警指示/三色灯-红/绿/黄/蜂鸣器/气缸伸出/气缸缩回/真空吸/真空破/夹爪夹紧/夹爪松开/电磁阀）；`None="无"`；`IsValid/Normalize(非法→无)/IsSafetyInput/IsIndicatorOutput/DesiredIndicator`。
- 默认「无」+ 归一：`Models/IoItem.cs` 默认 `_function="无"`；`AxisControllerViewModel.MakeIo`、`AiProjectExchange` 默认 → `IoFunctionCatalog.None`；`IoPanelViewModel` 加 `IsInput => Title != "输出"`，构造与 `OnAfterExcelReplace` 里 `NormalizeFunctions()` 把旧值（如「动点」）归一为「无」；`MakeNew` 默认「无」。
- 真实生效（`ViewModels/OperatorViewModel.cs`）：在既有 150ms `_uiTimer` 的 `UiTimerTick` 末尾加第 4 步 `EvaluateIoFunctions()`（带 `_ioFnBusy` 防重入）：
  - ① 安全类输入（急停按钮/安全门/光栅）逻辑 `Value!=0` → `AddLog(Error)` + `StatusBarService.ReportException` + `EStop()`（锁存，需复位）→ `return`；
  - ② 指示类输出（运行/就绪/报警/三色灯/蜂鸣器）按 `StatusBarService.IsRunning/EStopped` 推导目标值，变化时 `HardwareBridge.Current.WriteOutput`（未接卡只更新 `io.Value`）。
  - 因为默认「无」，不显式改成安全/指示功能就完全不影响既有行为（opt-in，安全）。
- `Views/IoPage.xaml`：`FunctionOptions` 单数组拆成 `InputFunctionOptions` / `OutputFunctionOptions`；输入表 ComboBox 绑前者、输出表绑后者。
- 构建（PowerShell + 绝对 dotnet `-c Debug`，`-p:OutDir=C:\tmp\nocode_out\`）：**EXIT=0，0 错误**。

### 气缸页简化：删时序动作表 + 参数左 / 状态右 + IO 与 IO 表联动（同日，最新）
- 需求（用户，附气缸页截图）：不要「气缸时序动作表」；尽量简化；**用户修改的参数放左侧、状态显示放右侧**；**要和 IO 表实际功能/点位联动**。
- 删：整块「气缸时序动作表」（DataGrid + 添加/删除/播放/停止 + 进度）+ `CylinderViewModel` 全部时序成员（`Sequence/SelectedSeqStep/IsSeqPlaying/SeqCurrentIndex/SeqProgressText/AddSeqStepCommand/RemoveSeqStepCommand/PlaySequenceCommand/StopSequenceCommand/PlaySequence/OnSeqTick/ApplySeqStep/StopSequence/RenumberSeq/SeqActionOptions`）+ 页面里只服务它的两个转换器资源（`CylinderRuntimeState/Brush`）。
- 布局：detail 变 **2 列**（左 `1.25*` / 右 `1*`）——**左列=可编辑参数**（基本信息 / IO 配置 / 动作参数 / 安全与逻辑 / 高级），**右列=状态显示（只读）+ 手动测试**。整页仍在 `ScrollViewer` 里。
- IO 联动（关键 bug 修复）：原来「输出点/伸出感应/缩回感应/备用感应」四个下拉都绑合并的 `Catalog.IoNames`（输入+输出混在一起，所以「输出点」会列出「输入1」）。新增 `Catalog.InIoNames`/`OutIoNames` + `SetIoIn/SetIoOut`，`Catalog.SyncAllFromData` 与 `IoPanelViewModel.SyncIoCatalog` 同时维护「分开 + 合并」两份；气缸页：输出点/第二线圈输出 → `Catalog.OutIoNames`，伸出/缩回感应 → `Catalog.InIoNames`。
  - 另发现：`CylinderItem.BackupSensor`（原「备用感应」）在运行时其实是**「双线圈」的第二路输出**（`SamsunCardBridge`/`LeadshineHardwareBridge` 里 `FindIo(cyl.BackupSensor, isOutput:true)` 写反相值），故改名为「第二线圈输出」并按**输出 IO** 选点。
- 状态显示（右列只读，真实读 IO 表）：`CylinderViewModel` 加 `StatusStateText/StatusOutText/StatusExtendText/StatusRetractText/StatusBackupText`（按名称在 `ProjectStore.Data.Inputs/Outputs` 里找 IoItem 读 `Value`）+ 300ms `DispatcherTimer` 刷新；选中项变化 / 手动动作后也刷新。
- 简化掉的字段（运行时确实没用，代码里只有模型属性）：感应类型、到位容差、伸出速度、缩回速度、关联轴（`LinkedAxis` 全工程仅模型出现）。保留运行时真正用到的：动作延时/伸出延时/缩回延时（`DelayMs/ExtendMs/RetractMs`）、动作超时、双线圈、脉冲输出/脉冲宽度、输出点、伸出/缩回感应、第二线圈输出。
- 构建（PowerShell + 绝对 dotnet `-c Debug`，`-p:OutDir=C:\tmp\nocode_out\`）：**EXIT=0，0 错误**。

### 气缸：2 路输出（伸出/缩回）+ 超时时间 + 超时报警方式（同日，最新）
- 需求（用户）：气缸输出要 **2 个**；还要 **超时时间** 与 **超时报警方式**。
- 2 路输出：`CylinderItem.OutPoint` 作为「**伸出输出**（第 1 路，1=伸出/0=缩回）」，`CylinderItem.BackupSensor` 作为「**缩回输出**（第 2 路，与第 1 路互斥）」。两个桥（`SamsunCardBridge` / `LeadshineHardwareBridge`）的 `CylinderMove` 改为：**只要配置了缩回输出就写**（去掉原来的 `if (cyl.DoubleCoil)` 门控）；未配置则保持单输出旧行为（向后兼容）。
- 超时报警方式：`CylinderItem` 新增 `TimeoutAction`（默认「报警并停止」）；VM 加 `TimeoutActionOptions = { 报警并停止, 仅报警, 忽略 }`。两个桥的 `WaitCylinder` 超时分支由「直接抛异常」改为按 `TimeoutAction` 分支：
  - 「报警并停止」（默认）：`AlarmService.Raise(Error,"气缸超时",…)` + `throw ScriptRuntimeException`（终止流程，保持原行为）；
  - 「仅报警」：`AlarmService.Raise(...)` 后 `return`（继续）；
  - 「忽略」：只 `Log`，继续。
  - `AlarmService.Raise` 会进流程页报警列表 + 状态栏 + CSV（跨线程安全）。
- 页面（`Views/CylinderPage.xaml`）：IO 配置里「输出点」→「**伸出输出**」、「第二线圈输出」→「**缩回输出**」（tooltip 说明第 1/2 路）；「安全与逻辑」去掉「双线圈」，「动作超时」→「**超时时间**」，其后新增「**超时报警方式**」（药丸选择）；右列状态显示标签同步改名。
- 构建（PowerShell + 绝对 dotnet `-c Debug`，`-p:OutDir=C:\tmp\nocode_out\`）：**EXIT=0，0 错误**（首次构建偶发 MSBuild 节点崩 EXIT=-1073741757，`dotnet build-server shutdown` 后重试即过）。

## 轴页：参数与底层对齐 + 轴状态与控制表（2026-09-24 续）

- 需求（用户，两张截图）：轴页「需要和底层的参数一样」+「需要显示轴的状态」+「这些内容都需要正常显示和设置」。
  - 澄清结果：状态表放在**轴页内、参数卡下方**；参数「**补齐关键缺失项并分组**」（不做 CAxisParameter 全量）。
- **模型**（`Models/AxisItem.cs`，+21 个标量字段，xlsx 零改动自动落盘、向后兼容）：
  运动：`StartVel` `StopVel` `SpeedCurve`(梯形/S型) `SPara`；
  回零：`HomeDir` `HomeTimeoutMs`；
  限位：`SoftLimitEnable` `LimitLevel`；
  电平/编码器：`AlarmEnable` `OriginLevel` `OriginStopMode` `EncoderEnable` `EncoderDeviation`；
  轴权限：`AllowEnable` `AllowManual` `AllowHome` `AllowSetZero`；
  手动：`JogStep` `ManualSpeed`。
- **新增 `Services/Hardware/AxisMonitor.cs`**：`AxisRawRead`(struct) + `AxisStatusSnapshot`(解释后，可空布尔表示「读不到 → 界面显示 —，绝不编造」) + `AxisMonitorService`（`Read` / `Enable` / `Stop` / `Home` / `Inch` / `StartJog` / `SetZero`）。**不改 `IHardwareBridge` 接口**，按 `HardwareSetup.Mode` 分派到具体桥；仿真模式返回「未连接」。
  轴状态字位定义（dmc_axis_io_status 标准，与卡族 AxisRealization 注释一致）：bit0 报警 / bit1 正限位 / bit2 负限位 / bit3 急停 / bit4 原点 / bit5 到位 / bit6 正软限位 / bit7 负软限位 / bit8 使能。总线轴用 CiA402 状态机==4 覆盖「使能」。
- **桥新增方法**（各 3 个，签名一致）：`TryReadAxisRaw` / `StartAxisJog` / `SetAxisZero`。
  - `SamsunCardBridge`：走 `IAxis` 现成接口 `GetCardAxisCurrentPosition(0/1)`、`GetCardAxisCurrentState()`、`GetCardAxisAlarmState()`；**轴状态字用反射调卡族自身的无参 `GetAxisCurrentState()`**（不在 IAxis 上，按 Type 缓存 MethodInfo，取不到就只给位置/编码器/运动中）。Jog → `CardAxisSerialMovement(Dir)`；设零 → `SetCardAxisCurrentPosition(...,0)`。
  - `LeadshineHardwareBridge`：`dmc_axis_io_status` / `dmc_check_done` / `dmc_get_stop_reason` / `dmc_get_axis_state_machine`；Jog → `dmc_vmove`；设零 → `dmc_set_position_unit`。
  - ★ **踩坑 CS1628**：`out AxisRawRead raw` 不能写进 lambda / 本地函数，必须先读本地变量再组装（两个桥都改成这种写法）。
- **VM**：新增 `ViewModels/AxisRowViewModel.cs`（每轴一行：状态只读属性 + 颜色 hex 字符串（与状态栏 `RunColor/ControllerColor` 同套路，WPF 自动转 Brush）+ 点动距离/手动速度 + 9 个命令）。`AxisViewModel` 加 `AxisRows`（与 `Items` 同步重建）、`IoWordHint`、`SetStatusRefreshEnabled(bool)`、300ms `DispatcherTimer` 轮询：**后台 `Task.Run` 读所有轴 → `BeginInvoke` 回 UI 赋值**，`Interlocked` 防重入，且 `!HardwareSetup.IsCardReady` 时直接置「未连接」不去读卡。
- **页面**：`Views/AxisPage.xaml` 参数卡按用途重组为 6 张（基本信息 / 运动参数 / 回零参数 / 限位与保护 / 电平与编码器 / **轴权限**），整页套 `ScrollViewer`；下方新增全宽「**轴状态与控制**」`DataGrid`（18 列：轴·正极限·负极限·原点·急停·报警·使能·运动中·位置·编码器·点动距离·手动速度·使能·点动−+·Jog−+·回零·停止·设零点），`IsReadOnly="True"`（避免 DataGrid 抢编辑态）+ `CellStyle=PointCellStyle` / `RowStyle=PointRowStyle`（选中态仍是深色字，符合「选中态黑色字体」约定）。本地 `UserControl.Resources` 补了 `CellPill*`（BasedOn TtPillBase 的紧凑药丸）与 `CellMiniBox`。
  `Views/AxisPage.xaml.cs`：`Loaded/Unloaded` 开关轮询（离开轴页就停，避免在别的页面空转读卡）。
- 资源键校验：脚本比对 AxisPage 用到的 26 个 `StaticResource` 全部存在于 `AppStyles.xaml`(164 键) + 页内 8 个局部键 → **无缺失**（上一轮弹窗崩就是缺 `SubLabel`，故这次专门校验）。
- 构建（PowerShell + 绝对 dotnet `-c Debug`，`-p:OutDir=C:\tmp\nocode_out\`）：**EXIT=0，0 错误**；新增代码 0 警告（先修掉一处 `CS1628` 与一处 `CS8622`）。
- **用户返工（同日）**：「不需要列表显示全部轴啊，只需要一个容器放一个轴的状态和控制就好了，而且状态和控制需要有实际的功能的」→ 撤掉「全部轴一张表」的方案，改成**只服务当前选中轴的单一容器**：
  - `AxisViewModel` 删除 `AxisRows`/`RebuildRows`/`Items.CollectionChanged` 订阅，改为 **`AxisRowViewModel? Monitor`**（`HasMonitor` 供显隐）；用 **`override OnPropertyChanged`** 捕捉 `nameof(SelectedItem)` → 重建 Monitor（选中/新增/删除轴都会走到，`SelectedItem=null` → Monitor=null）。300ms 轮询只读 Monitor 这一个轴（`ReferenceEquals(Monitor, m)` 防期间换轴回填）。
  - `Views/AxisPage.xaml`：删掉 18 列 `DataGrid`，改为 Apple 卡片内**左右两列**（左=只读状态：选中轴/控制器/连接/正极限/负极限/原点/急停/报警/使能/运动中/位置/编码器/状态字；右=点动距离·手动速度·轴权限 + 按钮组 使能/回零/设零点、点动−/点动+/停止、Jog−/Jog+ 按住），未选中轴时显示提示；页内 `UserControl.Resources` 全部删除（不再需要，所有键都走全局）。
  - **「有实际功能」加强**：`AxisRowViewModel.NotConnected(action)` 前置检查 —— `HardwareSetup.Mode != Simulation && IsCardReady` 不成立时**直接状态栏报「未执行：控制器未连接」，不再对着桩/log 谎报「已下发」**；点动距离为 0 也直接提示。四个动作（Invoke/Inch/StartJog/SetZero）全部接上前置检查。
  - 构建：**EXIT=0，0 错误，我的文件 0 警告**（`Monitor` 声明为可空 `AxisRowViewModel?` 以匹配本文件 nullable-enabled 风格）；资源键复检：用到的 23 个 `StaticResource` 全部在 `AppStyles.xaml` 命中，页内局部键 0 个。


