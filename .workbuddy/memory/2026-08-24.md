
- 运动控制卡多厂商支持（脉冲+总线）：新增 Services/Hardware/CardVendorRegistry.cs —— 中央厂商登记表，覆盖主流脉冲卡+总线主站（雷赛/固高/正运动/研华/汇川/台达/倍福/虚拟），每厂商含支持的总线类型(CardBusType 枚举：Pulse/EtherCAT/CANopen/ModbusTcp/ModbusRtu/Profinet/Other)、预期驱动DLL、是否已集成实时对接(HasLiveBridge)、DllPresent 探测、BusTypeName 中文名。Models/AxisControllerItem 增 BusType 字段；Catalog 增 VendorNames/BusTypeNames + RefreshControllerStandards（从登记表刷新下拉）；AxisControllerPage 品牌/总线类型下拉改绑 Catalog（IsEditable 可自定义）；AutoDetectCommand 改为扫描全部登记厂商——雷赛真实扫描卡数，其它厂商按驱动DLL是否存在登记控制器并标注「待接入实时对接」。新增厂商=登记表加一条。实时驱动仅雷赛已集成，其余待对应 SDK。BOM/CRLF 保留，XML/括号校验通过；沙箱禁构建需本地 VS rebuild。

- 点位表布局优化（紧凑+表格占比高）：① 共享 AxisCardTemplate 收窄——Width 236→196、Padding 12→8、Margin 6→4、内部间距 8→5、标题 10→6，使 4 张轴卡在常见屏宽排成一行、整体更矮，顶部「轴控制」区高度砍约一半，下方 DataGrid 多得 ~300px；② PointPage 左栏 232→200（表格更宽）；顶部大 StackPanel 边距 16→12,10；三段长说明压缩为单行；各分区标题/工具栏间距收紧；表格 RowHeight 28→26、外边距 16→12。远景：工程师页轴卡同步变紧凑（共用模板）。BOM/CRLF 保留、XML 良构；沙箱禁构建需本地 VS rebuild。

- 寸动/JOG 步长输入框改为明显可见：原 PointCellTextBoxStyle（Height=24/无边框/透明背景）只露数字、不露框。改为 86×30 圆角 Border 包裹（SurfaceBrush 底+LineBrush 1px 边框+CornerRadius=4），内嵌无边框 TextBox（透明底、数字居中、CaretBrush=AccentBrush、SelectionBrush=AccentSoftBrush）；Border 加 IsKeyboardFocusWithin 触发器：聚焦时边框变 AccentBrush（蓝色高亮）。表格内联编辑框保持隐形样式不受影响。BOM/CRLF 保留、XML 良构；沙箱禁构建需本地 VS rebuild。
