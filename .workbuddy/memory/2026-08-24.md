
- 运动控制卡多厂商支持（脉冲+总线）：新增 Services/Hardware/CardVendorRegistry.cs —— 中央厂商登记表，覆盖主流脉冲卡+总线主站（雷赛/固高/正运动/研华/汇川/台达/倍福/虚拟），每厂商含支持的总线类型(CardBusType 枚举：Pulse/EtherCAT/CANopen/ModbusTcp/ModbusRtu/Profinet/Other)、预期驱动DLL、是否已集成实时对接(HasLiveBridge)、DllPresent 探测、BusTypeName 中文名。Models/AxisControllerItem 增 BusType 字段；Catalog 增 VendorNames/BusTypeNames + RefreshControllerStandards（从登记表刷新下拉）；AxisControllerPage 品牌/总线类型下拉改绑 Catalog（IsEditable 可自定义）；AutoDetectCommand 改为扫描全部登记厂商——雷赛真实扫描卡数，其它厂商按驱动DLL是否存在登记控制器并标注「待接入实时对接」。新增厂商=登记表加一条。实时驱动仅雷赛已集成，其余待对应 SDK。BOM/CRLF 保留，XML/括号校验通过；沙箱禁构建需本地 VS rebuild。

- 点位表布局优化（紧凑+表格占比高）：① 共享 AxisCardTemplate 收窄——Width 236→196、Padding 12→8、Margin 6→4、内部间距 8→5、标题 10→6，使 4 张轴卡在常见屏宽排成一行、整体更矮，顶部「轴控制」区高度砍约一半，下方 DataGrid 多得 ~300px；② PointPage 左栏 232→200（表格更宽）；顶部大 StackPanel 边距 16→12,10；三段长说明压缩为单行；各分区标题/工具栏间距收紧；表格 RowHeight 28→26、外边距 16→12。远景：工程师页轴卡同步变紧凑（共用模板）。BOM/CRLF 保留、XML 良构；沙箱禁构建需本地 VS rebuild。

- 寸动/JOG 步长输入框改为明显可见：原 PointCellTextBoxStyle（Height=24/无边框/透明背景）只露数字、不露框。改为 86×30 圆角 Border 包裹（SurfaceBrush 底+LineBrush 1px 边框+CornerRadius=4），内嵌无边框 TextBox（透明底、数字居中、CaretBrush=AccentBrush、SelectionBrush=AccentSoftBrush）；Border 加 IsKeyboardFocusWithin 触发器：聚焦时边框变 AccentBrush（蓝色高亮）。表格内联编辑框保持隐形样式不受影响。BOM/CRLF 保留、XML 良构；沙箱禁构建需本地 VS rebuild。

- 通用 TableToolbar 搜索改造（删除「共 X 行」+ 增加搜索/上下/X）：Resources/AppStyles.xaml 新增 XCloseIcon/PrevIcon/NextIcon（SF Symbols 风）+ TtIconBtn（30×30 仅图标按钮）+ TtSearchBox（180px 圆角白底聚焦高亮）+ TtMatchInfo（命中数/总数）；Views/TableToolbar.xaml 删去原 共/Count TextBlock，在 Excel编辑 后追加 Grid（含 5 列：搜索框 / MatchInfo / 上一条 / 下一条 / 清空 X）；Views/TableToolbar.xaml.cs 新增 TargetGrid / SearchText / SearchPlaceholder / MatchInfo 4 个 DP，Loaded 时若 TargetGrid 未设置则沿可视化树向上找最近 DataGrid，挂载 CollectionViewSource.GetDefaultView 设置 Filter（ToString 包含匹配，大小写不敏感），按钮 / Enter/Shift+Enter / Esc 处理上一条/下一条/清空，全部作用域：IO（输入/输出）、变量、流程步骤 三页通用。BOM/CRLF 保留、XML 良构、38 个大括号/120 个圆括号配平；沙箱禁构建，需本地 VS rebuild。

- 工具栏搜索框移到最右侧 + 修复嵌套 bug：上一轮把搜索 Grid 误插在「添加」按钮内部（首个 </StackPanel> 锚点命中了按钮内 StackPanel 的闭合），本轮回写为干净结构——外层 Border> DockPanel(LastChildFill=False)，左侧 StackPanel(DockPanel.Dock=Left) 装全部 9 个功能按钮，右侧 Grid(DockPanel.Dock=Right) 装搜索框+命中信息+上一条/下一条/清空 X；搜索现位于工具栏最右。构建用 dotnet build 真实跑通（沙箱本次未拦截）：修复 2 个真实编译错误——① ICollectionView 命名空间应为 System.ComponentModel（非 System.Windows.Data）；② XAML 里 TextBlock 用了 x:Name="MatchInfo" 与 code-behind 的 MatchInfo DP 重名，改名 MatchInfoText。另修 1 处 CS8601（FindNearestDataGrid 返回可空 DataGrid 赋值给非空 TargetGrid，加判空）。最终 Debug 构建 0 错误；残留 2 个警告在 OperatorViewModel.cs（与本次无关）。headless 沙箱无法渲染 WPF 窗口，GUI 实际运行需本机桌面会话（且本地 EDR 可能拦截 exe 启动，属环境干扰非代码 bug）。

- 工具栏搜索框定位调整：按用户要求从「最右端」移到「Excel 编辑 按钮右侧紧邻」。结构由 DockPanel(Left=按钮/Right=搜索) 改回单一横向 StackPanel，9 个功能按钮按原序排列，搜索 Grid 作为 StackPanel 第 10 个子元素直接接在 Excel编辑 按钮之后；移除 DockPanel 标签。重建 0 错误（OperatorViewModel.cs 的 2 个无关警告保留）。

- 工具栏搜索框定位调整：按用户要求从「最右端」移到「Excel 编辑 按钮右侧紧邻」。结构由 DockPanel(Left=按钮/Right=搜索) 改回单一横向 StackPanel，9 个功能按钮按原序排列，搜索 Grid 作为 StackPanel 第 10 个子元素直接接在 Excel编辑 按钮之后；移除 DockPanel 标签。重建 0 错误（OperatorViewModel.cs 的 2 个无关警告保留）。

- 工具栏搜索改为「定位/导航」语义（不筛选隐藏行）+ 增加可见「搜索」标签：① TableToolbar.xaml 在搜索框前加 TextBlock 标签「搜索」（TextSecondaryBrush），并重写搜索区为 6 列 Grid（标签/输入框/命中信息/上一条/下一条/清空）；② TableToolbar.xaml.cs 移除 CollectionView.Filter/Refresh 过滤逻辑，改为记录原始集合中所有匹配项（_matches 列表），上一条/下一条在匹配项间循环跳转并 ScrollIntoView，所有行始终可见；MatchInfo 显示「第 k/m 个匹配」或「无匹配」；空搜索显示「共 N 项」。③ AppStyles TtSearchBox 增加 Trigger(Text="") 显示 Tag 占位文字「搜索…」水印。重建 0 错误（OperatorViewModel.cs 2 个无关警告）。

- 工具栏搜索改为「定位/导航」语义（不筛选隐藏行）+ 增加可见「搜索」标签：① TableToolbar.xaml 在搜索框前加 TextBlock 标签「搜索」（TextSecondaryBrush），并重写搜索区为 6 列 Grid（标签/输入框/命中信息/上一条/下一条/清空）；② TableToolbar.xaml.cs 移除 CollectionView.Filter/Refresh 过滤逻辑，改为记录原始集合中所有匹配项（_matches 列表），上一条/下一条在匹配项间循环跳转并 ScrollIntoView，所有行始终可见；MatchInfo 显示「第 k/m 个匹配」或「无匹配」；空搜索显示「共 N 项」。③ AppStyles TtSearchBox 增加 Trigger(Text="") 显示 Tag 占位文字「搜索…」水印。重建 0 错误（OperatorViewModel.cs 2 个无关警告）。

- 修复工具栏搜索「无匹配」根因：Models（IoItem/VariableRow/FlowStep/AxisItem/CylinderItem/CommItem/TrayItem/PointTable/FlowItem/AxisControllerItem 等）均未重写 ToString()，旧 ItemMatches 拿 o.ToString()（返回类型全名如 NoCodeMotion.Models.IoItem）与用户输入比对，永远不命中。改为反射 GetSearchableText(o) 拼接对象所有可读 public 实例属性值作为检索文本，按任意列（名称/值/类型…）搜索均命中。重建 0 错误（OperatorViewModel.cs 2 个无关警告保留）。
