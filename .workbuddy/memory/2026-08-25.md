
- 专利交底书阅读 + 加密源屏障发现：用户要求「根据专利优化软件」。专利核心=表格化时序确定性编排引擎（流程表加「时序标记」+「同步组」两列 → 编译为实时任务调度表/EtherCAT DC 同步帧 → 运行时回显时序偏差 → 编译期冲突检测）。已落地 Services/TimingSequenceEngine.cs（Compile + CheckConflicts，与 Model 解耦，纯逻辑，dotnet build 0 错误）。障碍：本仓库全部 .cs（含 Models/FlowStep.cs、FlowViewModel、ProjectData、MainWindow.xaml.cs）均以 marker 88 7d 1c 加密；.xaml 为明文可编辑。解密发生在定制版 Roslyn 编译器（C:/Program Files/dotnet/sdk/10.0.102/Roslyn/Microsoft.CodeAnalysis.CSharp.dll 内含 Decrypt），故 `dotnet build` 透明解密编译通过，但 Read/Edit/Python 只能看到密文，无法读取/改写 .cs。结论：AI 只能编辑 .xaml；任何 .cs 改动需用户在 VS（看到明文）里粘贴补丁，或运行其解密工具让文件转明文。AI 不应尝试破解加密。

## 顶部导航栏顺序调整 + 相机按钮（多次反复，注意可能被 revert）
- 用户要求：①「变量」放「流程」前面；②「相机」按钮放「变量」前面（即顺序 …料盘/相机/变量/流程/工程师/操作员）。
- 改动（仅 .xaml，可被 revert）：AppStyles.xaml 增 `CameraIcon` 几何 + `NavIconCameraBrush #30B0C7`；MainWindow.xaml 在料盘后插入相机按钮(Tag=Camera)、并把变量前移到相机后、流程前。
- 现象：用户曾把 MainWindow.xaml + AppStyles.xaml revert 回原状（相机按钮/图标/色全消失），后又要求继续。已重新应用。构建 0 错误（仅 OperatorViewModel.cs 既有 CS0414 警告）。
- 关键阻塞（加密 .cs，AI 改不了）：点相机按钮报 KeyNotFoundException，因为 MainWindow.xaml.cs 的 `_pages` 字典无 "Camera" 键。用户必须在 VS 明文里给 _pages 加一行 `["Camera"] = () => new CameraPage(),`。不补则相机按钮一按就崩。
- 相机页文件（明文，已建且构建通过，没被 revert）：Models/CameraItem.cs、ViewModels/CameraViewModel.cs、Views/CameraPage.xaml、Views/CameraPage.xaml.cs。CameraViewModel 用 CommandManager.InvalidateRequerySuggested() 刷新 CanExecute（项目自带 RelayCommand 无 RaiseCanExecuteChanged）。SDK 未接入，连接/拍照仅切状态。
