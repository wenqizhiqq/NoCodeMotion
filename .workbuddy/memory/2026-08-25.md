
- 专利交底书阅读 + 加密源屏障发现：用户要求「根据专利优化软件」。专利核心=表格化时序确定性编排引擎（流程表加「时序标记」+「同步组」两列 → 编译为实时任务调度表/EtherCAT DC 同步帧 → 运行时回显时序偏差 → 编译期冲突检测）。已落地 Services/TimingSequenceEngine.cs（Compile + CheckConflicts，与 Model 解耦，纯逻辑，dotnet build 0 错误）。障碍：本仓库全部 .cs（含 Models/FlowStep.cs、FlowViewModel、ProjectData、MainWindow.xaml.cs）均以 marker 88 7d 1c 加密；.xaml 为明文可编辑。解密发生在定制版 Roslyn 编译器（C:/Program Files/dotnet/sdk/10.0.102/Roslyn/Microsoft.CodeAnalysis.CSharp.dll 内含 Decrypt），故 `dotnet build` 透明解密编译通过，但 Read/Edit/Python 只能看到密文，无法读取/改写 .cs。结论：AI 只能编辑 .xaml；任何 .cs 改动需用户在 VS（看到明文）里粘贴补丁，或运行其解密工具让文件转明文。AI 不应尝试破解加密。
