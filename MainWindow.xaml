<Window x:Class="NoCodeMotion.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:NoCodeMotion"
        xmlns:views="clr-namespace:NoCodeMotion.Views"
        Title="NoCodeMotion · 无代码运动控制"  
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI, Microsoft YaHei, PingFang SC, sans-serif"
        >
    <Grid Background="{StaticResource CanvasBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部导航栏 -->
        <Border Grid.Row="0" Style="{StaticResource NavBarStyle}">
            <StackPanel Orientation="Horizontal" x:Name="NavPanel">
                <Button Tag="Axis" Click="Nav_Click" Style="{StaticResource NavButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource AxisIcon}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                        <TextBlock Text="轴" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Tag="Io" Click="Nav_Click" Style="{StaticResource NavButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource IoIcon}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                        <TextBlock Text="IO" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Tag="Cylinder" Click="Nav_Click" Style="{StaticResource NavButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource CylinderIcon}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                        <TextBlock Text="气缸" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Tag="Point" Click="Nav_Click" Style="{StaticResource NavButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource PointIcon}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                        <TextBlock Text="点位表" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Tag="Comm" Click="Nav_Click" Style="{StaticResource NavButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource CommIcon}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                        <TextBlock Text="通讯" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Tag="Tray" Click="Nav_Click" Style="{StaticResource NavButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource TrayIcon}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                        <TextBlock Text="料盘" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Tag="Flow" Click="Nav_Click" Style="{StaticResource NavButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource FlowIcon}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                        <TextBlock Text="流程" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Tag="Variable" Click="Nav_Click" Style="{StaticResource NavButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource VariableIcon}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                        <TextBlock Text="变量" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Tag="Engineer" Click="Nav_Click" Style="{StaticResource NavButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource EngineerIcon}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                        <TextBlock Text="工程师" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Tag="Operator" Click="Nav_Click" Style="{StaticResource NavButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource OperatorIcon}" Width="16" Height="16" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                        <TextBlock Text="操作员" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- 页面内容区 -->
        <ContentControl Grid.Row="1" x:Name="PageHost"/>
    </Grid>
</Window>
