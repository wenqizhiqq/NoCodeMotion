# 2026-09-21

## 需求（当日两轮，同一主题）：流程页「复制JSON / 粘贴生成」AI 往返

原始需求：「所有的流程页面都需要有 2 个按钮：复制JSON粘贴到AI对话软件，AI根据需求生成内容，
粘贴到软件就可以生成对应的流程」→ 第二轮追加：「还需要说明怎样操作啊，粘贴按钮需要弹窗确定」。

### 1) 两个按钮放在哪：流程页 Row-0 的 Detail 头部

四种流程（运控 Table / 脚本 Lua / 视觉 Vision / 节点图 NodeGraph）都在同一个 `FlowPage` 里切换，
所以按钮放在 `FlowPage.xaml` Row-0 的 `DockPanel` 里、紧跟类型 chip 之后 —— **一处实现，四种流程全都有**。
内嵌的 `VisualFlowPage` / `NodeGraphPage` 通过 `FlowContentReplaced` 事件跟着 `Reload()`。

**坑（必记）：`EditorPage.Detail` 的内容区里不能给元素加 `x:Name`。**
会报 `MC3093: 无法对元素"X"设置 Name 特性值…在另一范围内定义它时，已注册了名称`
（Border / Detail 内容会被注册进两个 NameScope）。所以那行操作说明的 `TextBlock` 只能匿名，
提示信息走 `ToolTip`。

### 2) 后端：`Services/AiProjectExchange.cs`

- 导出：`ExportFlowJson` / `ExportSteps`（**7 个字段**：逻辑/功能/对象/属性/运算/值/超时）。
  带上「属性 + 运算」是为了往返无损；只给「动作」也能进，由 `MapAction` 映射成内置模板实际在用的
  (属性, 运算, 值) 三元组，再用 `ClampTo` / `ClampProperty` / `ClampOperation` 夹回合法值，
  保证 DataGrid 单元格不会渲染成空白。
- 导入判定：**恰好 1 个流程 + 当前有选中流程 → 覆盖（保留原名称）**；否则追加为新流程、名称去重
  （`UniqueFlowName`）。覆盖时 `keepName: true`——名称是流程在左侧列表里的身份，被 AI 悄悄改名
  用户就找不到自己刚改的流程了。
- 解析统一走 `ReadFlowItems(json, body)`（去 ```json 围栏 / `AllowTrailingCommas` / 注释跳过 /
  数组或裸对象或 `{"流程":[…]}` 解包）。`ApplyFlowGenerated`、`CountFlowsInJson`、`PreviewFlowJson`
  全部委托它，**判定口径不会漂**。

### 3) 【重要坑】JSON 中文被写成 `\uXXXX`

默认 `JsonSerializer` 编码器会把「运控」序列化成 `\u8FD0\u63A7`，而这段 JSON 是要粘进 AI 对话给人看的。
修复：`JsonSerializerOptions { Encoder = JavaScriptEncoder.UnsafeRelaxedJsonEscaping }`
（与 `Services/ProjectJsonAnnotator.cs`、`Services/UserStore.cs` 的既有约定一致）。
`ExportGraph` 也从 `GetRawText()` 改成重新 `Serialize(..., JOpts)`，否则节点图里仍是 `\uXXXX`。

### 4) 弹窗 `Views/PasteFlowDialog.xaml(.cs)`

Apple 风 `WindowStyle="None" AllowsTransparency="True"` 圆角白卡弹窗，内含：
- 「操作说明」四步（① 在流程页点【复制JSON】… ④ 点【确定生成】）；
- 可编辑的 JSON 输入框（自动填入剪贴板，可 Ctrl+V / 手改）；
- 【重新读取剪贴板】按钮；
- **实时预览**（`PreviewFlowJson`）：识别到几个流程 / 类型 / 步骤数，并预告
  「将【覆盖】当前流程「X」（名称保留）」还是「将【新增】到工程」；
- 内容解析不出流程时【确定生成】**禁用**。

剪贴板为空也照常弹窗（弹窗里有说明和「重新读取剪贴板」，用户不用猜下一步干什么）。

---

## 第二轮修复：验证时发现的两个真实缺陷

### 5) 【关键】`TableToolbar` 的「共 N 项/步」标签根本不受 `Count` 控制

`Views/TableToolbar.xaml` 里显示的文本绑的是 **`MatchInfo`**（`TextBlock x:Name="MatchInfoText"`），
而 `MatchInfo` 只在 **`OnLoaded` 和 `SearchText` 变化**时重算 —— 所以行数一变（粘贴生成整批换掉步骤集合、
或点「添加」加一行）标签就**一直停在打开页面时的数字**。
`Count` / `CountLabel` 两个 DP 只是参与 `MatchInfo` 的文案拼接，**从来没被直接显示过**。
（IoPage×2 / VariablePage / FlowPage 都写着 `Count="{Binding Items.Count}"`，全都一直是死的。）

修复（`Views/TableToolbar.xaml.cs`）：
- `TargetGridProperty` 加 `PropertyMetadata(null, OnTargetGridChanged)`，在其中对
  `DataGrid.ItemContainerGenerator.ItemsChanged` **订阅/退订**（换 grid 时先退旧的）；
- `OnGridItemsChanged` → 无关键字走 `UpdateMatchInfo()`，有关键字走完整 `ApplySearch()`；
- `Count` / `CountLabel` 也加 `OnCountChanged` 回调；
- `UpdateMatchInfo` 以 `TargetGrid.ItemsSource` 为准（用户看到的就是它），没挂上 grid 时才退回 `Count`。
- 需要 `using System.Windows.Controls.Primitives;`（`ItemsChangedEventArgs`）。

**验证方式**：连续粘两次不同步数（7 → 3），断言标签跟着 7 → 3。
只断言一次 7 是不够的 —— 工程里本来就可能已经是 7 步，看不出标签是死的还是活的。

### 6) 名称列空白：`ComboBox` 只列 Catalog 里的名字，值不在库里就渲染空白

`Views/FlowPage.xaml` 的「名称」列是 `ComboBox ItemsSource="{Binding Function, Converter={StaticResource FunctionToNamesConverter}}"`，
而该 converter 返回 `Catalog.AxisNames` / `IoNames` / `CylinderNames` / …。
`Catalog.AxisNames` 来自 `ProjectStore.Data.Axes.Select(a => a.Name)` —— 工程 24 里轴名是 **X / Y**，
而 AI 生成的是 **X轴 / Y轴 / 来料检测 / 夹爪缸**，全都不在库里 → 单元格显示空白。

**已核实：值本身是正确落盘的**（`bin/Debug/net10.0-windows/Projects/工程24.xlsx` 的「流程」sheet 里
`名称` 列写着 X轴/Y轴/来料检测/夹爪缸）。所以这是**显示问题，不是数据丢失**。
代码注释里作者本来就写明了「模板预设值不在列表则该格为空」——是既定设计。

**决定不改渲染层**：试过用 `IMultiValueConverter` 把当前值并进 `ItemsSource`，但
`ComboBox.SelectedItem` 默认 TwoWay，**ItemsSource 一换实例 WPF 就把 SelectedItem 置空并回写 null**，
会把用户刚选的名称清掉 —— 数据丢失风险，不值得。改为「如实提示」：
- 提示词：`BuildNameContextText` 里空类别原来直接 `return` 省略，AI 看不到「气缸一个都没配」就去编名字。
  现在改成显式写 `- 气缸：（工程里未配置，请不要引用这一类）`。
- 导入提醒：新增 `UnknownObjects(f)` / `UnknownObjectsHint(f)`，按
  `ProjectStore.Data`（**不是 Catalog**）逐条比对「功能 → 合法对象名」，对不上的拼成
  `注意：轴[X轴]、气缸[夹爪缸] 在工程里不存在，「名称」列会显示空白，请先配置该对象或在表格里改选`，
  同时挂在**弹窗预览（带 ⚠，点确定前就能看到）**和**状态栏**上。
  「注释」行的对象是自由文本，跳过校验；系统/相机等不需要引用具体对象的功能也跳过。

---

## 环境 / 工具教训（本轮最重要，跨项目可复用）

### 7) 【大坑】这台机器的桌面**没有交互式输入**，合成鼠标点击会打空

症状：UIA 找得到按钮、`IsEnabled=True`、坐标也正常，但 `b.Click(simulateMove=True)` /
`mouse_event` 点了**毫无反应**（复制JSON 点了剪贴板不变、粘贴生成点了不弹窗）。
排查结论：

    GetForegroundWindow() == NULL        # 没有任何窗口在前台
    GetCursorPos()        == (0, 0)      # 光标根本不在桌面上
    OpenInputDesktop()    != NULL        # 会话本身在，只是没有输入桌面

**解法：用 UIA 的 `Invoke` 模式直接调 WPF 命令，完全绕开鼠标和前台窗口。**

    ctrl.GetInvokePattern().Invoke()      # 返回 bool

- `uiautomation` 包 **没有** `GetSupportedPatterns()`（那是裸 UIA COM API），别照抄 C#/C++ 示例。
  2.0.29 有 `GetInvokePattern / GetSelectionItemPattern / GetValuePattern / GetTogglePattern / …`。
- **导航项本身是 `TextBlock`，Invoke 不了**：要沿 `GetParentControl()` 往上找到外层
  `ButtonControl` 再 Invoke（实测 L1 就是 Button）。
- 同理 **`PrintWindow` 的客户区在这种状态下会全白**（标题栏正常），截图不再能作为视觉证据 ——
  改以 UIA 读到的控件文本/状态为准（比截图更硬）。

### 8) 补丁脚本：CRLF 文件里用 `\n` 匹配会 0 命中

`ui_e2e5.py` 是 **CRLF**。用 `bash -c "... python -c '...'"` 内联脚本、`old` 里写 `\n`，
`text.count(old)` 恒为 0（真机是 `\r\n`）。而且内联脚本里带中文引号容易被 shell 吃掉。
**规矩：一律把补丁写成 `.py` 文件再跑**，并在文件里做

    if crlf:
        old = old.replace('\r\n','\n').replace('\n','\r\n')

（与 `edit-bom-crlf-source-files` skill 的 `patch()` 同一套做法。）

### 9) 别用 `git show HEAD` 来隔离「本次改动」

本轮 HEAD 比工作区**落后约 1000 行**（上一轮的改动还没提交），
所以 `git diff HEAD` 里 996 行"新增"其实大部分是上一轮的。
据此算的花括号差值（HEAD `+1` → 工作区 `+2`）也**毫无意义**：
`Services/AiProjectExchange.cs` 里有大量 JSON 模板字符串，`{` `}` 本来就不配平。
**结论：这类文件的括号/差值只能横向比 HEAD 判断"有没有新增失衡"，本轮连这个都不成立 ——
直接信 `dotnet build`。**

---

## 验证结果

- `dotnet build`：**0 错误 / 590 警告**（与基线一致，全是既有 CA1416）。
- 冒烟测试 `ncm_smoke`：**121 PASS / 0 FAIL**（新增 N 段 9 条：提醒点名到「功能[对象]」、
  解释名称列会空白、原值照样写进流程（只提醒不偷改）、弹窗预览带 ⚠、对象都存在时**不**提醒（防误报）、
  注释行不参与校验、提示词写明「未配置」、有配置的类别照常列名）。
- UI 端到端 `ui_e2e5.py`：**26 PASS / 1 FAIL → 修完 harness 自身的取消点击后应为全绿**。
  已验证：一行操作说明可见；弹窗四步说明；实时预览；⚠ 提醒；未点确定前不改动任何流程；
  状态栏报告「已覆盖流程…7 个步骤」+ 对象不存在提醒；**标签 7 → 3 实时刷新**；多流程追加去重；
  不合法内容时【确定生成】禁用。

## 未决

- 名称列「不在库里就空白」目前只做提示，没做模糊自动匹配。若用户想要「X轴 自动认成 X」，
  需要再加一层保守的近似匹配 + 二次确认。
- 更早的一个未决问题仍挂着：条件不满足时是中止整条流程（当前行为）还是只跳过该步。

### 10) 【勘误】「`.bt/` 会被编进产品」是错的

`MEMORY.md` / `2026-09-19.md` / `edit-bom-crlf-source-files` skill 里都写着
「主工程默认 `**/*.cs` glob 不排除 `.bt/` 之类目录，写进去会被编进产品」。**实测证伪**：
往 `.bt/` 放一个内容为 `this is definitely not valid C# at all !!!` 的 `.cs`，
`dotnet build -t:Rebuild` 仍是 **0 错误 / 590 警告** → **点开头的目录默认不参与编译**。

真正危险的是**不带点**的临时目录：csproj 里只显式 `<Compile Remove="artifacts\**" />`，
所以 `scratch/` / `tmp/` / `test/` 会被 `**/*.cs` 收进去编进产品。
结论不变（冒烟测试仍放 `%TEMP%`），但**理由要改**——已同步修正 `MEMORY.md` 与 skill。

顺带发现仓库里还留着上一轮的 scratch 目录 `.bt/`（`add_buttons*.py` / `splice_flow.py` /
`wire_flow_vm.py` / `flow_exchange_block.cs` 等，均未跟踪、不影响构建）。已向用户报告，未擅自删除。

---

# 第二轮（同日）：把「Lua API 说明」写进 AI 提示词 + 新增【回退】

## 11) 用户需求

- 「还需要把那些api和AI说明啊，」——`复制JSON` 的提示词里要带上真实可用的 API 说明。
- 「粘贴生成按钮右边还需要一个回退按钮，弹窗确定是否回退」

## 12) 【重大发现】提示词一直在教 AI 写沙箱里不存在的 Lua

`BuildFlowPrompt` / `BuildPrompt` 的硬性要求与样例里写着 `Log.Info / Log.Warn / Log.Error`、
`Camera.Grab / Vision.Match / Vision.Defect`，`ProjectJsonAnnotator` 的 `["Cameras"]` 注释里也写着
「视觉流程的 Lua 里用 `Camera.Grab("相机名")` 按 Name 取图」。

实测（`Script(CoreModules.Preset_Complete)` + `HardwareApi.Register`）：
- `Log` / `Camera` / `Vision` **全都没注册** → 脚本一跑就是
  `attempt to call a nil value` / `attempt to index a nil value`。
- 真正注册的只有 `HardwareApi.Register` 里那批：24 个硬件全局函数、
  `Variable` / `IO` / `Axis` / `Cylinder` 四个子表、`EStop` / `Delay` / `WaitStep` / `Print`。
- `Services/LuaTemplates.cs` 的类注释其实早就把「只使用真正注册的全局函数」写清楚了，
  而且那 5 个模板本身用的都是 `print` + 真函数 —— **只有 AI 提示词这一路是错的**。

处置：
1. **补注册 `Log`**（`Info`/`Output`/`Warn`/`Error`/`Debug` 五个成员，前缀 `[警告]`/`[错误]`/`[调试]`）。
   比「禁止 AI 写 Log.Info」更稳：AI 无论如何都会写它，注册成本 ~10 行。
2. **`Camera` / `Vision` 不注册**（要真相机 SDK + 图像对象，是另一个 feature）。
   改成在提示词里明说「沙箱里没有 Camera / Vision」，并把**视觉流程的契约从 Lua 改成 `视觉步骤` 数组**。
   原来的工程级 SchemaText 里视觉流程给的是 `"脚本": "-- ... Camera.Grab ..."`，
   而 `ApplyFlows` 对 Vision 走的是 `FillVisualSteps`（读 `视觉步骤`）—— **那段 Lua 会被整段丢掉**。
3. 新增 `BuildLuaApiText()`，**由 `Editing.LuaApi.HardwareList` 生成**轴/IO/气缸/通讯/料盘/硬件
   六组的「签名 —— 中文描述」，再手写子表 / 日志 / 标准库 / 「沙箱里没有的 API」四节。
   这样以后往 `LuaApi` 加函数，提示词自动跟上，不会漂移。
   → 冒烟测试 O2 断言：`HardwareList` 里 24 个函数的 `Signature` **一个不漏**地出现在提示词里。

## 13) 【坑】`ExportSteps` 漏了「耗时」，往返一圈会把每步耗时清零

`ExportSteps` 只写 7 个字段（逻辑/功能/对象/属性/运算/值/超时），
但 `FillFlow` 会读 `DurationMs = Int(s, "耗时", "durationMs")`。
**导出少一个、导入多一个 → 每次「粘贴生成 / 回退」都把 `FlowStep.DurationMs` 清零。**
已补 `", "耗时": " + JN(s.DurationMs)`。冒烟 P 段断言 `DurationMs == 250` 能原样回来。

## 14) 【回退】的设计

- 快照 = **整份流程列表** 的 JSON（`AiProjectExchange.SnapshotFlows`），恢复 = 整体替换
  （`RestoreFlows`）。用「导出 → 再导回」而不是手写深拷贝：`ExportFlowJson`/`FillFlow`
  本来就是无损往返，以后 `FlowItem` 加字段时快照自动跟上。
- **`RestoreFlows` 的硬保证：快照解析失败时返回 false 且完全不动 sink**，
  避免「原来的被清空了、新的又没导回来」这种最糟结果。空快照 `"[]"`（粘贴前一个流程都没有）是合法目标。
- 回退栈 `Stack<PasteUndo>`：每回退一次弹一层 → 连点能一路退回更早状态。
  快照里存 `Owner`（拍快照时的 `Items` 引用），工程重载后 `ReferenceEquals` 不成立就清栈，
  防止跨工程串数据。
- `PasteFlowJson` 里**只有 `SnapshotFlows(Items) != undoSnapshot` 才压栈**：
  解析失败或 AI 回的内容与现状一模一样时不该让【回退】亮起来。
- 命令用 `new RelayCommand(..., _ => CanRevertPaste)`：`RelayCommand` 的 `CanExecuteChanged`
  挂在 `CommandManager.RequerySuggested` 上，改完栈再 `CommandManager.InvalidateRequerySuggested()`。
- 确认框复用 `Views.ConfirmDialog`（`DeleteAll` 同款），**确认按钮文案写「确定回退」而不是「回退」**，
  跟页面上那个入口按钮区分开 —— 顺带让 UIA 测试能唯一定位到它。
- 页面上按钮放 `Views/FlowPage.xaml` Row-0 的 `StackPanel`（`粘贴生成` 之后），
  操作说明文案改成 `① 复制JSON → ② 贴给 AI 补一句需求 → ③ 粘贴生成 → ④ 生成不对可回退`。

## 15) 【坑】UI harness 拿「过期 UIA 元素」去取 BoundingRectangle

`ui_e2e5.py` 里 `for it in flow_items(): it.Click(simulateMove=True)` 这种写法会抛
`_ctypes.COMError: (-2147220991, '事件无法调用任何订户')` —— 枚举出来的元素在点击前已经被重建了。
改成按**名字**现查现点（`click_flow(name)`，失败重试 5 次），并给 `texts()` 加 `safe_texts()` 包一层重试。
`dump()` 里会取 `c.BoundingRectangle`，树一变就会抛，所有走 `dump()` 的辅助函数都得防这一手。

## 验证结果（本轮）

- `dotnet build`：**0 错误 / 590 警告**（与基线一致）。
- 冒烟 `ncm_smoke`：**179 PASS / 0 FAIL**（O 段 39 条 + P 段 20 条）。
  O 段包括：**真的起一个 MoonSharp 沙箱**，把提示词里的样例 Lua 原样跑通、
  `Log.*` 五个成员真的可调、清单里点名的每个写法（轴 8 / IO 4 / 气缸 3 / 通讯 2 / 料盘 2 /
  命名空间表 / 标准库）都能跑、`Camera`/`Vision` 确实是 nil。
  P 段包括：四种流程（运控/脚本/视觉/节点图）快照 → 恢复 → 再快照**字节级一致**、
  整体替换不追加、空快照清空、坏快照原地不动、覆盖/新增两种真实情形的完整往返。

## 16) 需求：「新建工程的例子里面需要有节点图」

用户原话：**「新建工程的例子里面需要有节点图」**。

- 现状：`ProjectTemplateCatalog` 里 23 个模板，只有 3 个（`SimDemo` 的「视觉定位(节点图)」/
  「阵列循环(节点图)」、`VisionSort` 的「视觉分拣(节点图)」、`Dispensing` 的「阵列点胶(节点图)」）
  自带节点图，其余 19 个新建出来后在流程页里看不到节点图的例子。
- 做法：新增 `ProjectTemplateCatalog.EnsureNodeGraphFlow(ProjectData d)`，
  挂在 `Models/ProjectTemplate.cs` 的 `Build()` 里（`SeedSampleConditions` 之后）：

      var data = Factory();
      Services.ProjectTemplateCatalog.SeedSampleConditions(data);
      Services.ProjectTemplateCatalog.EnsureNodeGraphFlow(data);   // ← 新增
      return data;

  `Build()` 是模板的**唯一公共出口**（`Services/ProjectManager.cs:114/132` 走的就是它），
  所以一处挂点覆盖全部 23 个模板，以后新增模板也不用记得单独补。

- 幂等：`if (d.Flows.Any(f => f.Kind == FlowKind.NodeGraph)) return;` —— 自带节点图的 3 个模板不重复加；
  重复调用也不会多出第二条（冒烟里连调两次断言 `count == 1`）。
- 只引用**真实存在**的对象：`d.Axes` / `d.Inputs` / `d.Outputs` / `d.Cylinders` / `d.Cameras` /
  `d.PointTables[0].Points[0]` 各取第一个，缺哪类就少放哪个节点。
  空白模板（这几类全空）直接 early-return，保持「0 个流程」。
- 生成的主线（横向排布，`const double Col = 260`，每加一个节点往右推一列）：

      Start → [Home → MoveAxis(绝对,100,100) → WaitAxis]          有轴时
            → [MoveAxis(第二根轴, 绝对,50)]                        有第二根轴时
            → [PointGo(点位表, 第一个点位)]                        有点位表且有点位时
            → [CamCapture(相机, 曝光ms=10) → TemplateMatch(灰度匹配,0.8,360)]  有相机时
            → [WaitInput(第一个输入, 高电平, 3000)]                有输入时
            → [Cylinder(伸出) → Delay(300) → Cylinder(缩回)]       有气缸时
            → [IoWrite(第一个输出, 1)]                             有输出时
            → End

  写入 `new FlowItem { Name = "示例(节点图)", Kind = FlowKind.NodeGraph, Role = FlowRole.Main, GraphJson = ng.ToJson() }`。
- 实测分布：**空白 1 / 自动补 19 / 自带 3**（共 23）。

## 17) 【坑】补丁锚点命中 0 次，先逐字符 diff，别先怀疑 shell 把中文搞坏了

`patch_round6.py` 报 `2a: 锚点命中 0 次（应为 1）`，`Models/ProjectTemplate.cs` 那一处没打上
（好在脚本是 fail-safe 的：全部锚点通过后才 `flush()`，所以**一个文件都没被改**）。

第一反应是「heredoc / `python -c` 又把中文注释搞坏了」——**这次是错的**。真正原因是文件里的注释写的是

    /// （Services/ProjectTemplateCatalog.SeedSampleConditions），

是**斜杠**，而我的锚点写的是 `Services.ProjectTemplateCatalog`（**点**）。逐字符 diff 一下立刻现形：

    [(21, '0x2f', '0x2e')]      # '/' vs '.'

教训：
- **定位锚点用短 ASCII 标记 + `repr()` 打印真实行**，不要凭记忆重打那一行；
- **代码改动用纯 ASCII 锚点**（`        public ProjectData Build()` … `            return data;`），
  中文注释改动拆成单独的 `sub1(...)`，一处失败不会连累其它块；
- 补丁脚本一律 **fail-safe**：`FAIL`/`PENDING` + 全部通过才 `flush()`。

顺带：`ncm_smoke\Program.cs` 是**带 BOM** 的（实测 `BOM True / CRLF 0`），
写 `load(..., bom=False)` 会在完全正常的文件上 abort。

## 18) 冒烟 Q 段：逐个体检 23 个模板建出来的节点图

`ncm_smoke\Program.cs` 新增 `NodeGraphTemplateTests()`，对每个模板断言：
流程名不重复；有可引用对象就必须有节点图流程；自带节点图的不重复补；自动补的必须恰好 1 条；
是主流程；`GraphJson` 非空；节点数 > 1；**连线数 == 节点数 - 1**（线性主线）；
首节点是 `Start`、末节点是 `End`；每条连线两端都是图内真实节点；
Start 无入边、End 无出边、其余每个节点都恰好被连上/都有后继；
**节点属性只引用该模板里真实存在的对象**（轴/相机/气缸/信号/输出/点位表/点位逐类比对，越界就报名字）；
`NgDoc.FromJson(doc.ToJson())` 往返节点数与连线数一致；
再走一遍「复制JSON/粘贴生成/回退」那条路（`SnapshotFlows` → `RestoreFlows`）节点图不丢、节点数不变。
最后断言幂等（连调两次不重复加）、`EnsureNodeGraphFlow(null)` 不抛。

顺带发现并修掉一处**注释漂移**：`ProjectTemplateCatalog` 文件头写「模板总数：21 个」「综合(7)」，
实际是 **23 个 / 综合(9)**（`All` 里综合段有 PointPick / DualStation / AssemblyLine / VisionGuided /
MultiProduct / FullFeatured / SimDemo / VisionSort / Dispensing 九个）。已改成 23 / (9)。

## 验证结果（round 6）

- `dotnet build`：**0 错误 / 590 警告**（与基线一致）。
- 冒烟 `ncm_smoke`：**576 PASS / 0 FAIL**（A–P 段 179 条 + 新增 Q 段 397 条）。
