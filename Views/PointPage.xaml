<UserControl x:Class="NoCodeMotion.Views.PointPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NoCodeMotion.Views"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">

    <local:EditorPage>
        <local:EditorPage.Detail>
            <StackPanel Margin="16" MinWidth="360">
                <StackPanel Style="{StaticResource SectionHeaderStyle}">
                    <Path Data="{StaticResource PointIcon}" Width="14" Height="14" Stretch="Uniform" Fill="{StaticResource AccentBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="点位配置" Style="{StaticResource SectionHeaderText}"/>
                </StackPanel>

                <DockPanel Style="{StaticResource FieldGrid}">
                    <TextBlock DockPanel.Dock="Left" Text="点位名称" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding SelectedItem.Name, UpdateSourceTrigger=PropertyChanged}"/>
                </DockPanel>

                <StackPanel Style="{StaticResource SectionHeaderStyle}">
                    <TextBlock Text="各轴目标坐标" Style="{StaticResource SectionHeaderText}"/>
                </StackPanel>

                <ItemsControl ItemsSource="{Binding SelectedItem.Positions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <DockPanel Style="{StaticResource FieldGrid}">
                                <TextBlock DockPanel.Dock="Left" Text="{Binding AxisName}" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding Position, UpdateSourceTrigger=PropertyChanged}"/>
                            </DockPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="坐标为该轴在点位中的目标值；流程「功能 = 点位」时可引用此点位名称。"
                           Margin="0,12,0,0" Foreground="{StaticResource TextMutedBrush}" FontSize="12" TextWrapping="Wrap"/>
            </StackPanel>
        </local:EditorPage.Detail>
    </local:EditorPage>
</UserControl>
