<UserControl x:Class="NoCodeMotion.Views.CylinderPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NoCodeMotion.Views"
             xmlns:svc="clr-namespace:NoCodeMotion.Services"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">

    <local:EditorPage>
        <local:EditorPage.Detail>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="16" MinWidth="320">
                <StackPanel Style="{StaticResource SectionHeaderStyle}">
                    <Path Data="{StaticResource CylinderIcon}" Width="14" Height="14" Stretch="Uniform" Fill="{StaticResource AccentBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="气缸配置" Style="{StaticResource SectionHeaderText}"/>
                </StackPanel>
                <DockPanel Style="{StaticResource FieldGrid}">
                    <TextBlock DockPanel.Dock="Left" Text="名称" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding SelectedItem.Name, UpdateSourceTrigger=PropertyChanged}"/>
                </DockPanel>
                <DockPanel Style="{StaticResource FieldGrid}">
                    <TextBlock DockPanel.Dock="Left" Text="输出点" Style="{StaticResource FieldLabel}"/>
                    <hc:ComboBox ItemsSource="{x:Static svc:Catalog.IoNames}"
                                 SelectedItem="{Binding SelectedItem.OutPoint, UpdateSourceTrigger=PropertyChanged}"
                                 hc:InfoElement.Placeholder="选择 IO 输出点"/>
                </DockPanel>
                <DockPanel Style="{StaticResource FieldGrid}">
                    <TextBlock DockPanel.Dock="Left" Text="伸出感应" Style="{StaticResource FieldLabel}"/>
                    <hc:ComboBox ItemsSource="{x:Static svc:Catalog.IoNames}"
                                 SelectedItem="{Binding SelectedItem.SensorExtend, UpdateSourceTrigger=PropertyChanged}"
                                 hc:InfoElement.Placeholder="选择 IO 输入点"/>
                </DockPanel>
                <DockPanel Style="{StaticResource FieldGrid}">
                    <TextBlock DockPanel.Dock="Left" Text="缩回感应" Style="{StaticResource FieldLabel}"/>
                    <hc:ComboBox ItemsSource="{x:Static svc:Catalog.IoNames}"
                                 SelectedItem="{Binding SelectedItem.SensorRetract, UpdateSourceTrigger=PropertyChanged}"
                                 hc:InfoElement.Placeholder="选择 IO 输入点"/>
                </DockPanel>
                <DockPanel Style="{StaticResource FieldGrid}">
                    <TextBlock DockPanel.Dock="Left" Text="默认动作" Style="{StaticResource FieldLabel}"/>
                    <ComboBox SelectedValue="{Binding SelectedItem.Action, TargetNullValue=伸出}" SelectedValuePath="Content">
                        <ComboBoxItem Content="伸出"/>
                        <ComboBoxItem Content="缩回"/>
                    </ComboBox>
                </DockPanel>
                <DockPanel Style="{StaticResource FieldGrid}">
                    <TextBlock DockPanel.Dock="Left" Text="动作延时(ms)" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding SelectedItem.DelayMs}"/>
                </DockPanel>
            </StackPanel>
            </ScrollViewer>
        </local:EditorPage.Detail>
    </local:EditorPage>
</UserControl>
