<UserControl x:Class="NoCodeMotion.Views.OperatorManualPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NoCodeMotion.Views"
             mc:Ignorable="d" d:DesignHeight="900" d:DesignWidth="1000">

    <Grid Background="{StaticResource CanvasBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Border Grid.Row="0" Style="{StaticResource ToolbarStyle}" Margin="16,16,16,0">
            <DockPanel LastChildFill="False" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Path Data="{StaticResource InfoIcon}" Width="20" Height="20" Stretch="Uniform"
                          Fill="{StaticResource AccentBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="操作员使用说明书" FontSize="20" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- 15 章节滚动列表 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                      Padding="22,12,22,16">
            <StackPanel>
                    <!-- 章节 1 -->
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="{Binding S1_Title}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding S1_Lines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" LineHeight="22"
                                                   Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <!-- 章节 2 -->
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="{Binding S2_Title}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding S2_Lines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" LineHeight="22"
                                                   Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <!-- 章节 3 -->
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="{Binding S3_Title}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding S3_Lines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" LineHeight="22"
                                                   Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <!-- 章节 4 -->
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="{Binding S4_Title}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding S4_Lines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" LineHeight="22"
                                                   Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <!-- 章节 5 -->
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="{Binding S5_Title}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding S5_Lines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" LineHeight="22"
                                                   Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <!-- 章节 6 -->
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="{Binding S6_Title}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding S6_Lines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" LineHeight="22"
                                                   Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <!-- 章节 7 -->
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="{Binding S7_Title}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding S7_Lines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" LineHeight="22"
                                                   Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <!-- 章节 8 -->
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="{Binding S8_Title}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding S8_Lines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" LineHeight="22"
                                                   Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <!-- 章节 9 -->
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="{Binding S9_Title}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding S9_Lines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" LineHeight="22"
                                                   Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <!-- 章节 10 -->
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="{Binding S10_Title}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding S10_Lines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" LineHeight="22"
                                                   Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <!-- 章节 11 -->
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="{Binding S11_Title}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding S11_Lines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" LineHeight="22"
                                                   Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <!-- 章节 12 -->
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="{Binding S12_Title}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding S12_Lines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" LineHeight="22"
                                                   Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <!-- 章节 13 -->
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="{Binding S13_Title}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding S13_Lines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" LineHeight="22"
                                                   Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <!-- 章节 14 -->
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="{Binding S14_Title}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding S14_Lines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" LineHeight="22"
                                                   Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    <!-- 章节 15 -->
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12" Padding="18,14">
                        <StackPanel>
                            <TextBlock Text="{Binding S15_Title}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,0,8"/>
                            <ItemsControl ItemsSource="{Binding S15_Lines}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap" LineHeight="22"
                                                   Margin="0,0,0,3"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
            </StackPanel>
        </ScrollViewer>
        <local:PageHintBar Grid.Row="2" OperationText="查阅本机操作方法与注意事项说明；变更流程后同步更新手册内容。" PrecautionText="以本手册为准；内容与程序不一致时以实际程序行为为准并反馈修正。"/>
    </Grid>
</UserControl>