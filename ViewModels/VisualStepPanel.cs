// 该文件已废弃：原 VisualStepPanel 依赖已删除的 VisualStepType 枚举与 VisualFlowStep.DisplayName。
// 视觉流程编辑现由 Views/VisualFlowDetailViewModel（字符串化 StepType）承载，左侧列表由 VisualFlowPage 提供。
// 保留空命名空间桩以保证工程可编译；如需旧表格面板逻辑可从版本库恢复。
namespace NoCodeMotion.ViewModels
{
}
